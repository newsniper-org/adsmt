---
name: nlsat-algebraic-reduction-kb
description: EXPERIMENT branch in external/oxiz adding a leveled algebraic-reduction KB to oxiz-nlsat so it solves coupled equality systems with irrational/algebraic roots (was spurious-unsat). Not on main/testing; not pushed.
metadata: 
  node_type: memory
  type: project
  originSessionId: 32a1dc0d-7730-4862-8df4-6958199ce84f
---

**Experiment branch `0.2.4-redesign+fix-algebraic-solution` (external/oxiz submodule, 2026-06-22) — a leveled ALGEBRAIC-REDUCTION KB for `oxiz-nlsat`.** Branched off `0.2.4-redesign` (on top of `3d72b96` i128). **MERGED 2026-06-23: verus-fork validated GREEN (perfect-square + SOS both verify under `-V adsmt`, A2 11/11, clear-to-merge) → FF-merged into external/oxiz's local `0.2.4-redesign` (`3d72b96`→`b72729e`), 6 commits, NO push (origin/0.2.4-redesign still `3d72b96`; submodule-pointer bump + push = user's job).** Disposition fulfilled (was: user validates on verus-fork, if green merge into local mainline). The "local main" target = `0.2.4-redesign` (OxiZ has no `main` branch; `0.2.4-redesign` is the redesign mainline adsmt's submodule pointer tracks).

**core-NRA bilinear spurious-UNSAT — FIXED 2026-06-23 (`1090b3f`, on `0.2.4-redesign` after the merge; user said "바로 착수해").** Surfaced by the §G-SOS adversarial audit (NOT caused by it — confirmed pre-existing on clean HEAD via git-stash): under `QF_NRA`, a BILINEAR strict inequality was decided spuriously `unsat` — `(* x y) > 5` → `unsat` (SAT at x=10,y=0.5); `(* x y) < 0` → `unsat`. The `unsat` came from the core `dispatch_nra_constraints`/`NlsatSolver` bilinear path (§G-SOS correctly classifies the `xy` Gram indefinite + DECLINES). ROOT CAUSE: the NRA `unsat_is_trustworthy` gate was `poly_atoms.iter().all(|a| a.kind != AtomKind::Eq)` — it LACKED the univariate guard the NIA gate has. So a REAL invalid bilinear goal (`RMul`, e.g. `rx*ry >= 0`) could be spuriously VERIFIED on the NRA path (the Int `Mul→NIA` path was already guarded → sound; latent on the real-verus path, verus fixtures so far Int-only). FIX = mirror the NIA guard: trust NRA `Unsat` ONLY when every atom is `is_univariate()` (and non-`Eq`). Definite multivariate forms that ARE genuinely unsat (`x²+y²=−1`, `(x−y)²<0`) are decided up-front by §G/§G-SOS, so completeness cost is low. After: `x*y>5`→`sat`, univariate `x*x<0`→`unsat` (unchanged), both verus bars→`unsat` (unchanged). Regression `nra_bilinear_strict_inequality_not_false_unsat`; oxiz-theories 1191/oxiz-solver 446/oxiz-nlsat 404 green; the 2 pre-existing `nlsat_integration` NIA/NIRA failures (`test_nia_x_squared_eq_3_unsat`, `nira_pure_integer_nonlinear_unsat_still_caught`) unchanged (git-stash verified — NOT this change; NIA integer-specific incompleteness, real-domain §G can't catch). Pairs with [[oxiz_relationship]] #288 (NRA *Sat* side). `0.2.4-redesign` now at `1090b3f` (origin still `3d72b96`, NO push).

**전수점검 (4-agent adversarial audit, 2026-06-23) + spurious-SAT fix (`3d5bb70`).** Before the verus reply, ran an exhaustive soundness+completeness audit (4 parallel adversarial agents + empirical sweep). VERDICT: the verus-critical direction is SOUND — NO false-unsat/false-verify (§G/§G-SOS one-sided, PSD classifier cross-checked vs eigenvalues on 60k matrices/200k forms = 0 false-pos; spine bridge-gated; bilinear gate holds; both bars unsat; algebraic KB no false-Sat/Unsat; every atom an entailed conjunct). BUT surfaced a PRE-EXISTING spurious-**SAT** class on the explicit QF_NIA/QF_NRA dispatch (`x²=3` Int, `Σ7 x_i²<0`, `x²=25∧x>6`, `x·y>0∧x·y<0`) — **SAT-direction = verus-SAFE** (Mul-bridge maps Sat→None; spurious-sat on a negated goal = verus incompleteness, never false proof; confirmed pre-existing on parent). FIX (`3d5bb70`, SAT-side only — only ever downgrades Sat→Unknown, ZERO false-unsat risk; user "지금 수정"): (1) **Sat-model verification backstop** completing #288's gate — `model_satisfies_atoms`/`eval_poly_at_model` (panic-free) re-check the core's returned model against every atom in BOTH `dispatch_nia` (inner-nlsat model) and `dispatch_nra` (NlsatSolver model); unverified/incomplete model → None. → `x²=3` Int now `unsat`, **the 2 pre-existing `nlsat_integration` NIA failures now PASS (17/0, was 15/2)** — incidentally closes the #289-class integer-infeasibility on the nonlinear path. (2) **§G-SOS `MAX_FORM_VARS` 6→10** → `Σ7…Σ10 x_i²<0` now `unsat`. 3 of 4 spurious-sats closed; remaining (`x²=25∧x>6`, `x·y` conflict, `Σ11+`) are MAIN-solver opaque-nonlinear after dispatch returns None — fixing needs loosening the unsat gate (verus-DANGEROUS) or exact LDLᵀ PSD; deliberately NOT done. oxiz-nlsat 405/oxiz-theories 1193/oxiz-solver 446/nlsat_integration 17/0 green; verus bars unsat; legit sat preserved. **Completeness frontier next steps (audit-ranked): degree-4/even-degree SOS (verus quartic lemmas), exact LDLᵀ to lift the var cap, strict-affine PSD-not-PD.** `0.2.4-redesign` now `3d5bb70` (origin `3d72b96`, NO push).

**2026-06-23 — "no spurious verdict either direction" campaign + the P0 false-UNSAT discovery (user "찝찝하다" about ANY remaining spurious sat/unsat).** Three sound deciders landed on `0.2.4-redesign` + a **major P0 soundness fix**. (1) `479c336` **trichotomy / same-term bound-infeasibility** decider (`Solver::check_term_bound_infeasible`, called pre-dispatch in `mod.rs`): a single hash-consed TermId pinned to mutually-infeasible LITERAL bounds (`t>0∧t<0`, disjoint intervals, conflicting eqs) → UNSAT regardless of what `t` denotes (the user's "삼분법" fact made operational) — kills the opaque-product spurious-sat `x·y>0∧x·y<0`. Sound (only directly-asserted conjuncts, walks positive `And` only, strict/non-strict boundary exact). (2) `79dd5ce` **exact O(n³) LDLᵀ inertia classifier** (`discriminant.rs matrix_inertia`, Sylvester's law) replacing §G-SOS's exponential `2^n` all-principal-minors PSD test; `MAX_FORM_VARS` 10→128 → `Σ_{i=1}^{N} x_i²<0` decides UNSAT at any dim (the `x²≥0`/`Σ≥0` fact, any dimension). (3) **decider #3 (univariate-`Eq` unsat, drop the NRA `!= Eq` gate) ABANDONED** — a z3-differential PROVED it unsound (119/600 false-unsat; the core nlsat over-reports unsat on univariate-`Eq`, e.g. `-2x²=-5`⟺`x²=5/2` real roots → spurious unsat). The agent's 13/13 unit battery MISSED it; only the z3-diff caught it. **PROCESS LESSON: any change trusting an engine's UNSAT needs a z3-DIFFERENTIAL, not a unit battery — see [[feedback_z3_differential_for_unsat_trust]].** ★ **(4) `2e86546` P0 — the z3-diff exposed that BOTH core solvers (`NlsatSolver` real + `NiaSolver` int) are BROADLY UNSOUND on nonlinear `unsat` (single-atom false-unsat: NRA deg-2 13%/deg-3 32%/deg-4 16%; NIA 58/400 — `3x²<5`, `x⁴>4`, `3x²≥25` all decided spurious `unsat`), and the `unsat_is_trustworthy` gates TRUSTED them → a latent verus-DANGEROUS false-`unsat` (false-VERIFY on `-V adsmt`). FIX: both gates → trust core `Unsat` ONLY when every atom is LINEAR (`total_degree()<=1`); sound nonlinear unsat comes from §G/§G-SOS (`definite_sign_unsat`) + the trichotomy pre-check + the legacy perfect-square pattern (`x²=3` int). After: 1500-case z3-diff = 0 FALSE_UNSAT (was 76+58), all 374 remaining divergences are FALSE_SAT = verus-SAFE; verus bars + `x²<0`→unsat + `x²=3`int→unsat preserved.** Regressions `test_term_bound_*`(6), `test_n{ra,ia}_*_not_false_unsat`(6), `test_matrix_inertia_classification`. oxiz-theories 1193/oxiz-solver 446/oxiz-nlsat 406/nlsat_integration 29 green. **These are BAND-AIDS — the real fix = clean-room nonlinear-solver redesign, see [[oxiz_nlsat_redesign]].** z3-diff harness: `$CLAUDE_JOB_DIR/tmp/diff_*.py`.

**verus-fork VALIDATED the whole merged `0.2.4-redesign` arc (2026-06-23, GREEN, FINAL):** SOS `(x−y)²≥0` verifies e2e (unknown→verified), bar/perfect-square/invalid unchanged, A2 11/11 zero-regression; NRA bilinear fix CONFIRMED via direct SMT (`(* x y)>5`→sat==z3, false-proof direction closed); NO new completeness leads (will send degree-4 SOS when it appears). **NATIVE-PREEMPT insight (verus-fork-flagged, verus-SAFE, NOT to fix per their request):** through `lu-smt`, my OxiZ dispatch sat-backstop for `x*x=3` Int is UNREACHABLE — (a) the **native adsmt engine decides `x*x=3` as a decisive `sat`** (itself a native spurious-sat, SAT-direction/verus-safe), and the OxiZ fallback fires only on native `Unknown`, so OxiZ is never consulted; (b) `lu-smt` **downgrades `QF_NIA`/`QF_NRA`→`ALL`** (parser warns "logic outside supported table"), so OxiZ's explicit-logic Path-1 (where my backstop/cap-raise live) is NEVER exercised via lu-smt — only native + the term-based Path-2 (= the verus no-logic path) run. So the Path-1 spurious-sat fixes are validated in OxiZ's own corpus but not surfaced through lu-smt; fixing the native `x*x=3` decisive-sat would touch the native unsat-trust path (verus-DANGEROUS side) → deliberately deferred per verus-fork. See [[oxiz_relationship]] (delegation: native-decisive preempts OxiZ consult).

**Trigger (NOT a redesign regression):** the standalone experimental crate `oxiz-nlsat` (separate from the integrated `oxiz-theories/nlsat`) had a PRE-EXISTING `#[ignore]`d test `test_solver_circle_and_line` (`x²+y²=25 ∧ y=x`) that returned **spurious Unsat** — surfaced by a `cargo test --workspace -- --ignored` sweep. `oxiz-nlsat` is untouched by the 124-commit CCFV redesign (0 commits) and the test was already ignored at the upstream fork point, so it is a pre-existing upstream limitation, not the redesign's doing. ROOT CAUSE: nlsat's variable assignment is `BigRational`-only (`assignment.rs arith_values`, `decide.rs pick_arith_value -> Option<BigRational>`); a free variable gets a rational sample (0) so the second variable's feasible region = ∅ → spurious Unsat at `solver/mod.rs:653`. The only solutions (`x=y=√12.5`) are irrational.

**The fix = a leveled "reduction KB" (mirroring `oxiz-solver/src/calculus.rs`'s leveled-KB pattern), as a SAT-only ADDITIVE recognizer** — it returns only `Some(Sat)` or `None`, **NEVER Unsat**, never fabricates Sat. 3 commits:
- `b44b561` **foundation**: `oxiz-nlsat/src/reduction_kb.rs` — Level-0 linear-equality elimination + Level-1 resultant → triangularize to a univariate eliminant `q` → confirm a real root via the existing Sturm isolation → exact `oxiz_math AlgebraicNumber` model → back-substitute and VERIFY every original equality exactly by symbolic divisibility `pseudo_remainder(reduced,q).is_zero()`. `solver/mod.rs try_algebraic_reduction` SAT-only pre-pass; `Model.algebraic_values`. test_solver_circle_and_line un-ignored + passes.
- `fa31ed4` **rule D** (`x^k=a` integer→real escalation, catalog §D): `try_rule_d` returns the irrational real root via Sturm, gated by `has_rational_root` (exact rational-root theorem — STRONGER than `is_rational()`, declines reducible `(x−1)(x²−2)`); fixes the formerly-ignored `test_quadratic_roots` (`x²−2 ⇒ ±√2`); even-k/a<0 (`x²+1`) → no real root → fall back (complex is NOT a real-SAT). + **conic recognizer** `discriminant::recognize_conic`/`ConicForm`/`ConicKind` (B²−4AC; circle ⊂ ellipse ⊂ conic, no special case) — pure recognizer; conic∩conic SOLVE uses `augment_with_linear_combinations` (the sound radical-axis `p−λ·r` implied combination) routed through the exact algebraic path.
- `1c5cf66` **rule F** (catalog §F): `reduction_hints::suggest_transforms` — an ADVISORY, PURE/read-only invertible-transformation HINT engine (rotation `tan2θ=B/(A−C)` to kill `B·xy`; translation to centre) for `Unknown` cases. Never on a verdict path → cannot change a verdict; "abduction for the transformation" (same advisory philosophy as adsmt's [[abductive_smtlib_surface]] `(abduce)`).
- `37f14a6` **REACHABILITY from verus** (verus-fork P2): `oxiz-solver dispatch_nl_solver` (`check_nlsat.rs`) now reaches nlsat **term-based**, not on the logic string. KEY (verus-fork's captured-stream finding): verus `by(nonlinear_arith)` emits NO `(set-logic …)` and carries nonlinearity as the UNINTERPRETED `Mul`/`RMul` UF (`(>= (Mul x! x!) 0)`), NOT native `(* x y)` (which lives only in the bridge axioms `(= (Mul x y) (* x y)) :pattern`). Path 2 (logic absent only) scans for `Mul`/`RMul` Apply nodes (NOT into quantifier bodies), rewrites `(Mul a b)→(* a b)` ONLY when the bridge axiom is structurally asserted (else uninterpreted → sound Unknown), routes by symbol (`Mul`→NIA, `RMul`→NRA, never integerize a real), and returns ONLY `Unsat` (`Sat`/`None`→`None`). Captured streams: `nl-provable`(`x*x≥0`)→`unsat`=provable, `nl-invalid`(`x*y≥0`)→`unknown` (sound, not false-unsat). Adversarial-audit clean; oxiz-solver lib 442/0 + theories 1178/0; QF_NIA/NRA path byte-identical; 2 nlsat_integration failures pre-existing (git-stash verified). THIS is what makes the verus e2e test (the merge prerequisite) possible. **verus-fork CONFIRMED GREEN: `x*x>=0` verifies under `-V adsmt` (==z3), A2 11/11, clear-to-merge — flagging ONE completeness lead: the perfect-square `x²−2x+1 ≥ 0` falls to sound `unknown`.**
- `ee69bc5` **rule §G (definite-sign by discriminant) + verus polynomial-spine bridge** — CLOSES the perfect-square lead (`x²−2x+1 ≥ 0` now decides UNSAT via the OxiZ CLI; was `unknown`). TWO fixes, because the lead exposed a deeper reachability gap than verus-fork realised: in the verus prelude `Add`/`Sub` are ALSO uninterpreted UFs (`:qid prelude_add`/`prelude_sub`, alongside `Mul`/`EucDiv`/`RAdd`/`RSub`/`RMul`/`RDiv`), so the Mul-only rewrite left `(Add (Sub (* x x)(* 2 x)) 1)` with `Add`/`Sub` uninterpreted → the poly translator dropped the whole atom → no quadratic reached the KB. (1) **§G recogniser** (`discriminant.rs` `recognize_univariate_quadratic`/`DefiniteSign::definite_sign`/`quadratic_atom_is_unsat` — the killed workflow's +323-line landing, sound, matches the hand-derived classification) wired as a pre-check in BOTH `oxiz-theories::nlsat::{dispatch_nia_constraints,dispatch_nra_constraints}` over the entailed `PolyAtom`s (`poly_atom_cmp`+`definite_sign_unsat`), serving the explicit `QF_NIA`/`QF_NRA` path AND the verus term-based path. Encodes the user's KB facts: `x²≥0 ∀x` (the a=1,b=0,c=0 instance) + the 4 rules {D=0,a>0⟺f≥0; D=0,a<0⟺f≤0; D<0,a>0⟺f>0; D<0,a<0⟺f<0}. (2) **polynomial-spine bridge**: generalised `check_nlsat.rs`'s bridge-rewrite from `Mul`/`RMul` to the whole spine `Add`/`Sub`/`Mul`+`RAdd`/`RSub`/`RMul` (`SpineRewrite` struct, `spine_native_op`, `rewrite_spine`; `eq_sides_are_bridge` checks the native side matches the per-symbol op so wrong-operator/spoofed bridges are rejected), each folded ONLY under its bridge axiom; `EucDiv`/`EucMod`/`RDiv` stay uninterpreted (non-polynomial → soundly dropped). SOUNDNESS (adversarial-audit clean, 7/7 probes PASS, 0 holes): one-sided UNSAT-only, exact rationals, univariate-deg-2-only (D>0/non-quad/multivar decline), each atom is a top-level conjunct (single-contradiction ⟹ formula UNSAT even with drops), real-UNSAT⟹int-UNSAT (sound NRA+NIA), explicit path byte-identical bar new correct decisions. oxiz-nlsat 397/0, oxiz-theories 1186/0, oxiz-solver 445/0; the Mul/RMul detector regressions all still pass. Catalog §G appended to `REDUCTION_KB_RULES.md`. **verus-fork CONFIRMED GREEN e2e (2026-06-23): `x*x - 2*x + 1 >= 0` flips unknown→VERIFIED, bar/invalid unchanged, A2 11/11. (Pre-existing unrelated ignored failure surfaced by `--ignored`: oxiz-solver `test_bv_comparison_model_generation`, fails on clean HEAD too — see [[feedback_test_ignored_pass]].)**
- `b72729e` **rule §G-SOS (multivariate quadratic-form definite sign, PSD)** — CLOSES the verus-fork SOS lead (`(x−y)² ≥ 0` = `x²−2xy+y² ≥ 0` now decides UNSAT via the OxiZ CLI; was `unknown`). §G's univariate `(sign a, sign D)` generalises to a multivariate FORM `f=xᵀAx+bᵀx+c` via the symmetric Gram/bordered matrix `M=[[A,b/2],[(b/2)ᵀ,c]]` (`f(x)=[x;1]ᵀM[x;1]`, `[x;1]≠0`): M PD⟹f>0∀; M PSD⟹f≥0∀ (SOS); −M PD/PSD⟹f<0/≤0∀; else indefinite→decline. `(x−y)²` Gram `[[1,−1],[−1,1]]` is PSD. `discriminant.rs`: `recognize_quadratic_form` (cross-term split k/2, total_degree==2, `MAX_FORM_VARS=6`), `matrix_is_pd` (Sylvester leading-minors>0), `matrix_is_psd` (ALL principal minors≥0 — non-leading required, diag(0,−1) trap), `quadratic_form_is_unsat` reusing §G's DefiniteSign table; exact `BigRational` (reuses `gaussian_elimination_det`). Wired into `definite_sign_unsat` as `quadratic_atom_is_unsat || quadratic_form_is_unsat` (serves explicit QF_NRA/NIA + verus spine; handles nested `(Mul 2 (Mul x y))`). SOUNDNESS (adversarial-audit clean 7/7; exact PSD classifier cross-checked vs eigenvalues on 60k matrices + 200k random forms, 0 false-pos): one-sided, sufficient PD/PSD⟹definite, indefinite/non-quad/>6-var decline, real-UNSAT⟹int-UNSAT. oxiz-nlsat 404, oxiz-theories 1190, oxiz-solver 446 green. Catalog §G-SOS appended. **This is the LAST commit before the FF-merge into `0.2.4-redesign` (b72729e is now the redesign-mainline tip).**

**Soundness:** TWO adversarial-audit phases (in the building workflows) caught + fixed a P0 false-SAT (the gate ignored asserted inequalities/root atoms → `circle∩line∩x>4` reported Sat). FIX = `try_algebraic_reduction` fires ONLY when every asserted atom is a single-factor `Eq` asserted true (the exact set the verify covers) + all bool vars assigned + complete model; anything else falls back to the complete CDCL+CAD path. NIA-safe (integer-typed `x²=2` → NIA branch-and-bound, the branch inequality atoms re-block the gate → sound Unsat). Permanent guards `audit_false_sat_circle_line_with_inequality/_disequality`, `audit_conic_false_sat_with_inequality`. `cargo test -p oxiz-nlsat`: 387 + 22(2 ignored) + 13, 0 failed.

**Rule catalog `oxiz-nlsat/REDUCTION_KB_RULES.md`** (user-co-designed) documents A0/A1 (linear+resultant, LANDED), A2 (product-of-forms), A3 (discriminant conic — classifier LANDED, transcendental trig parameterisation = documented FRONTIER, no unsound trig solver), B/C (hyperbolic/exp-log normalisation, frontier), D (LANDED), E (range-intersection search bound — suggestion 2, E-light resultant-prune applicable / E-full a transcendental-frontier lever, DOCS-ONLY), F (LANDED). The frontier rules connect to [[oxiz_mbqi_rewrite]]/calculus.rs's transcendental-symbol handling. See [[oxiz_relationship]].
