---
name: cic-hol-lowering
description: "#325 (M3-8) CIC→adsmt-core-HOL lowering — WHERE it lives (separate sibling repo, NOT AD1/adsmt-ir), its current frontier, and the non-obvious Var/Const leaf invariant + the bare-engine vs CLI-preprocessing test boundary."
metadata: 
  node_type: memory
  type: project
  originSessionId: 32a1dc0d-7730-4862-8df4-6958199ce84f
---

**#325 "M3-8 lowering: kernel CIC → adsmt-core HOL" lives in `adsmt-ir-lower`, now an AD1 WORKSPACE MEMBER** (`/home/ybi/AD1/adsmt-ir-lower`, absorbed 2026-06-27 `0f9b007` — the faces-in-workspace Phase-2 gate; see [[faces_in_workspace]]). The old `/home/ybi/adsmt-ir-lower` is a **frozen history archive**. Path-deps are now intra-workspace (`../adsmt-core`, `../adsmt-theory`, `../adsmt-engine`). The kernel `adsmt-ir` itself is also an AD1 member now; the early `adsmt-ir` commits that "claim the lowering landed" only edited DESIGN.md (the source was always in the separate `adsmt-ir-lower`). Single module `src/lower.rs`. Pipeline: SMT-LIB → `adsmt-ir-smtlib` face → checked kernel `Env` → `lower()` → adsmt-core `Bool` terms → `adsmt_engine::Solver`. `tests/lower.rs` = fidelity (alpha_eq vs hand-built terms), `tests/solve.rs` = end-to-end verdict.

**Frontier (after arith `42522d7` + match `d629c1f`):** lowers the FO/EUF/quantifier core + Bool-ite + datatype DECLARATIONS + **the linear-arith core** (Int/Real `+ - * div mod / < <= > >= neg abs` → engine LIA/LRA) + **non-recursive Bool-valued `Match`**. Still **`Unlowerable` (whole-query abstain → Unknown):** `Elim`/`Fix`/`MutElim` (recursion/induction), data-valued `Match`, recursive `def`, dependent/higher-order. (**non-Bool term-`ite` NO LONGER abstains — LIFTED 2026-07-02, see below.**) Sound-but-uninterpreted EUF: `int2real`, `pow`/`odd`/`prime`. **NOT EUF anymore — the `Nat`/`WNat` injections lower to the IDENTITY (§3b) and the carrier carries its positivity guard (§3c): #338 LANDED 2026-06-28 (`5449e33`).**

**`Match` lowering (`lower_match`, `d629c1f`):** a Bool-valued `Match(ind, motive, minors, major)` (post-whnf → symbolic `major`) → `⋀_j (major = c_j(sel_{c_j,0}(major),…) ⇒ minor_j[fields:=sel(major)])` — the datatype generalization of Bool-ite. The antecedent is a **tester encoded by EQUALITY** (`c_j(sel(x))` rebuilds x from its fields), NOT a native tester: the engine has no tester term — `((_ is c) x)` is opaque to it (z3-test: sat), but `x = c_j(sel x)` works (the form its selector-reduction + disjointness reason about). `Frame` was generalized from `CVar` → `CTerm value` so a minor's field binders resolve to the selector terms (selectors named `{cname}!sel{i}`, matching `spec_to_decl`). Sound under an incomplete engine (under-approx selector reduction → can't false-unsat). **⚠️ ENGINE-SIDE: the real Phase-2 chokepoint was the trusted Datatypes theory (`adsmt-theory/src/datatypes.rs`), z3-differential-investigated + FIXED this session (`AD1` `3510f7f`).** Routing `Match`→engine exposed TWO trusted-engine bugs the 16 unit tests missed (the [[feedback_z3_differential_for_unsat_trust]] lesson recurred — a randomized z3 differential over ~2400 datatype queries found them): **(1) P0 SPURIOUS-UNSAT (soundness):** `assert` pushed the eq-pair to `asserted_eqs` IGNORING `lit.polarity`, so a same-ctor DISEQUALITY `succ b ≠ succ c` was treated as the *equality* → injectivity derived `b=c` → congruence → contradiction → FALSE unsat (`(not (= (succ b) (succ c)))` wrongly unsat; z3 sat). **57/400 false-unsat → 0**; fix = gate the push on `lit.polarity`. **(2) Spurious-SAT (the original target):** `collect_selector_reductions` reduced a selector only on a *literal* `sel(C(..))`, not `sel(x)` with x~C(args); `x=succ zero ∧ pred x≠zero` wrongly sat. Fix = new `congruent_selector_reductions` (union-find the asserted eqs → constructor rep per class → emit `sel(y)=args[idx]`). 2 regression tests; 18 dt + 87 theory + 202 engine green; **0 false-unsat across 4 seeds × 500 + 400 random queries**. **REMAINING = COMPLETENESS not soundness:** ~15% false-SAT (lu sat, z3 unsat — sound, verus=not-verified) from datatype-theory under-derivation + the SAT-core "does not refine across theory conflicts" (disjunctive/match goals → unknown). That = #331, the Phase-2 *verdict*-completeness work. So `Match` lowering is validated by a FIDELITY test; its verdict-completeness rides on #331.

**VERDICT GATE CLOSED — 2026-06-27 (#334, AD1 commits `bde5dc2`+`9881b21`).** The "SAT-core does not refine → unknown" gap is GONE: `solver.rs::check_via_theories_with_model` now runs a **bounded DPLL(T) theory-refinement loop** (`TheoryCheck::Infeasible{block}` → add the theory-valid blocking clause → re-solve, ≤ `THEORY_REFINE_BOUND=16` rounds; CDCL-exhaustion ⇒ sound `unsat`, cap ⇒ `Unknown`→delegation). Datatype case-split obligations (`n_unsat`/`m_unsat`/`t1`/`t3`) now reach a sound `unsat`. **OxiZ delegation is NOT the answer for datatypes — it's UNSOUND there:** the in-process `Context::execute_script` path returns spurious `sat` even for `(= zero (succ zero))` (constructor distinctness; treats the datatype as uninterpreted, `(get-value (x))` echoes `x`). The native refinement loop is the only sound path. **Two further fixes the loop forced out (both via the z3-differential):** (a) **the false-unsats were actually a PRE-EXISTING P0 in the native CDCL** (`analyze_conflict_1uip` UIP fallback re-picked an already-resolved literal → spurious unsat on a trivial 3-clause sat set; fix `bde5dc2`, see [[feedback_z3_differential_for_unsat_trust]]) — NOT the datatype theory; (b) `congruent_selector_reductions` was order-dependent (kept one arbitrary `HashMap`-order ctor rep per class → nondeterministically dropped the load-bearing reduction → flaky spurious-sat), fixed to emit against ALL ctor apps. **Recognizer desugaring (parser, `9881b21`):** `(is-C t)` / `((_ is C) t)` → the shape biconditional `t = C(sel0 t,…)` (sound+complete, polarity-safe) instead of a fresh uninterpreted predicate; selectors threaded into the `SymbolTable`. Post-fix z3-differential: **0 false-unsat** (datatype 1600 + pure-SAT 6500). **#331 CLOSED (commit `f588966`):** datatype false-SAT **~15% → ~0.1%** via two sound `Theory::check` axioms — **acyclicity** (occurs-check: cycle in the class-containment graph `class(C(args))→class(argᵢ)`; catches `a=succ a`, `succ b=succ(succ b)`, `xs=cons(_,xs)`; does NOT over-fire on the is-cons identity `xs=cons(head xs,tail xs)`) + **exhaustiveness** (a value excluded from every constructor via recognizer-shaped disequalities `x≠c(sel_c x)` is unsat; needs a scoped `disequalities` store). 7200+ random datatype queries → 0 false-unsat; residual ~0.1% = the single-survivor→acyclicity chain (`b≠zero ∧ pred b=b`) which needs selector-term synthesis the theory has no field-type info for (conservative). **#336 CLOSED (`8d0d5c7`):** the EUF↔LRA interface-equality gap `(=x y)∧(x>y)`→false-sat — LinArith now intersects same-pair two-var bounds (canonicalizing the `y−x`↔`x−y` mirror); 4000 random LIA/LRA+eq → 0 false-unsat. Remaining lu↔z3 disagreements are ALL lu-conservative — see [[lu-vs-z3-disagreement-analysis]] (no lu-better cases on the flat-sort SMT-LIB path; a new LIA singleton-disequality gap `0<x<2 ∧ x≠1` tracked).

**TWO non-obvious invariants (cost a debug cycle each):**
1. **Var/Const leaf rule** (`leaf()` in `lower.rs`): a declared `open` value/function symbol lowers to a free **`Var`**, NOT `Const` — matching the native SMT-LIB parser (`adsmt-parser-smtlib2/src/convert.rs::convert_symbol`: only numeric **literals** (digit-leading name) + datatype **constructors** are `Const`, everything else is `Var`). **Load-bearing for arith:** `adsmt-theory/src/arith.rs::parse_comparison` claims `(< x k)` ONLY when `x` is `TermInner::Var`; a `Const` operand → `LinArith` returns `Ignored` → the engine's polite-dispatch (`adsmt-theory/src/polite.rs`, routes a comparison by its OPERAND sort via `arith_comparison_operand_sort`, then `handles_sort("Int")`) flags `had_uninterpreted_atom` → `solver.rs` downgrades to `Unknown` ("native theory abstraction: a theory atom was assigned without theory interpretation"). **EUF is indifferent** (congruence handles `Var` or `Const`) — which is why the EUF/datatype slices passed for a full cycle with the wrong `Const` leaf, masking the bug. Literal recognition: `arith.rs::int_lit` accepts bare `"5"` AND `"int:5"`.
2. **Arith operators map to the SMT-LIB names the engine recognizes**, NOT the kernel `theory.rs` names: `Int.add`→`"+"`, `Int.lt`→`"<"`, …, `Real.div`→`"/"`; build `const_(op, T→T→T or T→T→Bool)` applied (mirror `convert_arith_binop`/`convert_arith_compare`). The lowered term is byte-identical to native → no new soundness surface; the verdict-differential must AGREE on pure arithmetic.

**Test boundary:** the BARE `Solver::new()` path (`tests/solve.rs`) decides only what `LinArith` claims directly — `(op var literal)` comparisons + their sums (`x>0 ⊢ x+y>0` ✓) — PLUS any **GROUND** arithmetic/(dis)equality/comparison, which **the lowering itself now constant-folds** (#317, 2026-06-29 — see below). Comparison re-orientation of a NON-ground `5<x` (literal-on-left + free var), Real-decimal literals, and unsat-via-OxiZ-delegation remain the **native lu-smt CLI's preprocessing / fallback** (confirmed: `lu-smt` solves `5<x∧x<3` unsat, the bare engine returns Unknown) — they belong at the full-pipeline differential, not the unit gate. (Also caught a real native bug: `lu-smt` says `x=-5 ∧ x>0` is **sat** — wrong; unary-minus folding gap — now part of #348.)

**2026-06-28 (`5449e33`) — the #325 Match VERDICT gate + #338 positivity wiring CLOSED end-to-end:** (1) the `lower.rs`/`solve.rs` comments claiming "the bare engine cannot yet DECIDE match queries" were STALE — `congruent_selector_reductions` (#330, see above) already closed the gap, so `x=succ zero ∧ (match … succ n⇒n≠zero)` is now end-to-end **`unsat`** (`tests/solve.rs::match_reaches_a_sound_unsat_through_selector_congruence` + a sat control). Only the comments were behind; no code fix needed for #325's Match chokepoint. (2) **#338 was a real one-line bug:** `Lowerer::positivity` emitted the guard as `(<= lo v)` (literal-on-the-LEFT), but the bare engine's LinArith claims a comparison only in the canonical `(op var literal)` orientation (literal-on-left is re-oriented only by the lu-smt CLI preprocessing — see the Test-boundary note), so a free `Nat`/`WNat` const's positivity reached the solver as an UNINTERPRETED atom → the whole query was `Unknown`, leaving the positivity un-decided. Fixed to emit `(>= v lo)` (`v≥lo` ≡ `lo≤v`, §3c polarity unchanged): `c:Nat ∧ c<1`/`d:WNat ∧ d<0` now `unsat` (LIA bound-infeasible), `c:Nat ∧ c<2` stays sat. **z3-differential 7/7 engine==z3** ([[feedback_z3_differential_for_unsat_trust]] satisfied). +5 verdict tests (refinement_collapse 6→9, solve 13→15); existing shape assertions flipped `<=`→`>=`. So the lu-kb type relation is NOW a decision input. See [[nat_wnat_refinement_collapse]], [[lukb_type_relation_utilization]].

**2026-06-29 (#317 — the M3-8 closing 후검증 gate finally BUILT + the one lowering-level fold it forced out):** built `examples/lower_solve.rs` (SMT-LIB stdin → face→lower→`Solver::new()` → sat/unsat/unknown) + `z3_differential.py`, a **THREE-WAY** randomized differential (16k trials/8 seeds). **KEY DESIGN INSIGHT (the reason z3-alone is the WRONG gate here):** the lowering's job is to hand the engine the SAME terms the native parser would, so a wrong verdict z3 catches could be a *lowering* bug OR a shared *engine* bug — z3 can't tell them apart. Decide each script by THREE engines: **LOWERING** (subject), **native `lu-smt`** (same engine, NO lowering = REFERENCE), **z3** (oracle). `lowering==native≠z3` ⇒ the engine got it wrong too ⇒ pre-existing **ENGINE** bug (cancels, tracked); `native==z3≠lowering` ⇒ genuine **LOWERING** mistranslation = THE GATE. **Result: 0 lowering-attributable wrong verdicts** (9354 definite, 5 improvements-over-native from the fold); 17 engine false-sats correctly isolated → filed **#347** (quantifier opacity: `(and(forall x q)(not(forall x q)))`→sat, `(forall x r)∧¬r`→sat; the engine returns a CONFIDENT `sat` so the Unknown-triggered OxiZ fallback never fires — verified survives `--features oxiz`) + **#348** (linear var-cancellation: `(= 4 (- j j))`→sat, `(= (+(- 0 j) j) 4)`→sat; `LinArith::assert` `Ignored`s any eq outside `(var,lit)`/`(var,var±k)` → UF merges the compound term; SUPERSET of #336/#337/#340/#341). **THE ONE GENUINE LOWERING FIX:** a **ground constant-fold in `lower.rs`** (`as_int_lit`+`fold_int_binop`+`bool_lit`+`int_ty`): the bare engine merges two distinct integer-literal `Const`s in UF (no built-in `4≠3` — `LinArith::assert` `Ignored`s a lit-vs-lit `=`), so `(= 4 3)`→spurious **sat**. Fold ground `+`/`-`/`*` over Int literals → a literal + `=`/`<`/`<=`/`>`/`>=` → `true`/`false` (folded at the `=` prelude + `try_arith` binary + `arith_neg`; checked arith — div/mod/Real/overflow abstain to the plain term). **Soundness-monotone** (only ever replaces an under-determined atom with its true value); replicates the native CLI's text preprocessing the lowering path BYPASSES (lowered terms → bare engine, never through the CLI parser). +6 verdict tests (solve 15→21; lower 14 + refinement 9 unchanged). **TWO process lessons ([[feedback_z3_differential_for_unsat_trust]]):** (1) a fixed boundary-probe (`(= 4 3)`) found only the literal-merge — only the RANDOMIZED multi-seed campaign surfaced #347/#348 (deeper formulas); (2) the FIRST three-way run reported 6 FALSE "lowering bugs" — the classifier treated `native=other` (the native parser ERRORS on `ite`, which it has no operator for) as "native disagrees ⇒ lowering bug". FIX = `native` non-definite ⇒ *inconclusive* (a `lowering_bug` requires `native==z3`) + **desugar Bool-`ite`→`(c→a)∧(¬c→b)` for the native run** (semantics-preserving; z3 stays the independent oracle on the ORIGINAL) so native is a usable reference on the whole fragment; all 6 reclassified to engine bugs (native-desugared also says sat). Local commit, NO version bump, NO push. See #347/#348 for the engine follow-ups.

See [[oxiz_relationship]] (the engine), [[verus_emits_lukb_surface]] (#325 is the Phase-1c verdict-diff gate), [[asp_face_design]] (the ASP face also lowers via #325).

**2026-07-02 (#386 — term-level `ite` LIFTED by fresh-var-free atom duplication, AD1 `9a85d30`):** a term-`ite` `(ite A c a b)` over a non-Bool sort (Int/Real/uninterpreted — `adsmt-core` has NO term-level conditional) previously abstained the WHOLE obligation; verus emits `if`-exprs pervasively so this was a big loss. NOW lowered by **atom duplication**: `hoist_term_ite` (top of `lower_term`) rewrites the smallest enclosing atom `F[ite]` → `(¬c ∨ F[a]) ∧ (c ∨ F[b])` (classically the `(c→F[a])∧(¬c→F[b])` the 선검증 proves; built from the non-binding `and`/`or`/`not` consts — NO kernel `imp`/`Pi` so NO de-Bruijn shift) and re-lowers. It fires ONLY for an atom (comparison/`=`/predicate app — never a connective/quantifier), so the ite is lifted at its SMALLEST enclosing atom, keeping a bound-var condition inside the `∀` scope ⇒ **capture-free, no fresh var, polarity-free** (a semantic equivalence ⟹ valid asserted OR negated). This SIDESTEPS both hazards that ruled out fresh-var lifting: binder capture under `∀` + the goal/hyp polarity of the flat `extra_hyps` channel (which the Nat/WNat positivity uses). `find_hoistable_ite` picks an INNERMOST ite (branches ite-free ⇒ measure `Σ2^#ite−1` decreases ⇒ terminates); `subst_kernel` replaces it in the binder-free skeleton only (never inside an ite CONDITION or binder — leaving a condition-copy is sound, it collapses under the c-case-split + recursion hoists it later). **선검증 `~/term-ite-lifting-verification`** (Verus, 8 verified/0): `atom_lift_equiv` (per-env equiv) ∘ `replace_form_cong` (preservation THROUGH quantifier binders — this is what licenses applying the local rewrite under `∀`/`∃` with the `∀env` hypothesis, no shifting) ⇒ `lift_preserves_sat` (equi-satisfiable both directions). **Validation:** 600-case randomized z3-differential over ground ite+LIA+Bool through the real `elaborate→lower→engine` pipeline → **0 false-unsat, 0 false-sat** (`term_ite_z3_differential`, `#[ignore]`d, needs z3); exact-`alpha_eq` `non_bool_ite_lifts` + nested/under-∀ lowering tests + z3-confirmed verdict tests (`term_ite_unsat`/`_sat`/`nested_term_ite_unsat`). adsmt-ir-lower green (lower 16 + refinement 9 + solve 24); old `non_bool_ite_abstains` retired. Design `docs/design/TERM_ITE_LIFTING.md`. (STALE note fixed 2026-07-04: the lukb surface DOES have `if` since #387 — term-ites arrive via BOTH faces and verus AIR.) See [[oxiz_relationship]] (the "lowering 확장 + OxiZ 갭 닫기" session), [[feedback_z3_differential_for_unsat_trust]], [[feedback_empirical_adversarial_review]].

**2026-07-04 (#403 — lukb field-SELECTOR applications + the `let`-blocked ite lift; corpus stage-bail 33/33 CLEARED):** the verus corpus's single stage-bail root cause was AIR selector applies (`` `<Ind>./<Ctor>/?N`(x) ``) with no surface→engine connection (the pre-#391 tester gap, selector edition). TWO-PART FIX: (1) **elab (adsmt-ir-lukb)** — `Item::Data` now registers each NAMED field in `selector_fields` (surface name → `(dt, canonical)`; cross-ctor duplicate = poisoned `None`, refuses to guess) and POSTULATES the canonical positional selector `{ctor}!sel{i} : D → fieldTy` (opaque `Open` const so kernel `infer` types it — still NO kernel selector primitive); `elab_call`'s unknown-symbol arm (after the `is-` tester hook) rewrites `field(x)` → `K::apps(K::cnst("{ctor}!sel{i}"), [x])` with tester-mirrored arity-1/sort validations; a DECLARED same-name symbol wins (env lookup precedes the hook). (2) **lower (adsmt-ir-lower)** — `try_selector` in `lower_app` (BEFORE the generic env-lookup dispatch — the postulate would otherwise Var-leaf it and the render would emit a duplicate `declare-fun` beside `declare-datatypes`): parse `{ctor}!sel{i}` (EXACT canonical spelling — `sel01`≠`sel1` guard), validate ctor `Modality::Constructor` + non-parametric + `i` in field range (else fall through), emit the `Const`-leaf application (the `lower_match` idiom; engine selector-reduction #330 keys on it). (3) **the residual 9 rows (datatypes-match-2) = a SECOND wall behind the first**: fuel-def axioms bind selector reads with `let` INSIDE data-valued ite branches (`let p$=sel(x) in ite(p$<10,…)`); `find_hoistable_ite` walks only App-spines+ite-branches so the `Let` hid the ite, and head-position ζ (whnf) reveals it only after the atom is gone → "ite over a non-Bool sort" abstain. FIX = `inline_definitional_redex`: when an atom has no hoistable ite, ζ/β-inline ONE ite-carrying redex on the same skeleton walk (kernel `Let` = lukb face; β-redex `(λ.b)v` = smtlib face's `let`) via `subst_top` — the kernel's own definitional step, conversion-sound, SN-terminating; ite-free redexes skipped (they head-reduce normally). **Corpus: 33/33 stage-bails cleared → +19 verified (104→123), +14 solver-unknown (join #404's targets); 0 regressions, negative controls 4/4 pinned.** Tests: elaborate 94→99 (canonical-head hash-cons equality, verus-qualified names, ambiguity, arity/sort, shadowing), solve 24→30 (selector congruence unsat + sat control + near-miss fall-through + let-blocked ite unsat/sat, all z3-preverified), driver differential 6→9 rows. NOTE the engine's pre-existing abstain: LinArith claims only bare-Var-LHS comparisons, so post-lift COMPOUND atoms (`x+1<5`) still Unknown natively ("theory atom assigned without theory interpretation") — the solve.rs let-tests use bare-var let values deliberately; #348-adjacent, not a #403 gap.
