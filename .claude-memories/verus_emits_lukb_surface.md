---
name: verus-emits-lukb-surface
description: "Strategic decision (2026-06-26, user/verus-fork-owner proposal): have Verus emit the lu-kb-successor unified surface to adsmt instead of FLAT SMT-LIB (AIR→lu-kb is less lossy than AIR→SMT-LIB). Phase 0 (goal-negation tag) LANDED. CORRECTION (2026-06-26): lu-kb is NOT S-expression-based — the memory's 'Phase 1 = SMT-LIB-term-shaped item-structured (axiom)/(goal)' was MISLABELLED + a WIP impl was reverted; user chose 'design the successor LANGUAGE first'. Real design = docs/design/LUKB_SUCCESSOR_SURFACE.md (lu-kb + quantifiers + axiom/assume/goal items + `|-`)."
metadata:
  node_type: memory
  type: project
  originSessionId: 32a1dc0d-7730-4862-8df4-6958199ce84f
---

**PROPOSAL (user, verus-fork owner, 2026-06-26):** have Verus emit the **lu-kb-successor unified surface** (→ typed CIC kernel `adsmt-ir`) to adsmt instead of flattening through SMT-LIB. Grounded design workflow read the REAL verus-fork (`/home/ybi/verus-fork`, branch `backend-pluggable`) + the adsmt-ir face. Verdict = **do-it-PHASED** (scope = new-surface-primary + SMT-LIB-fallback); all 3 adversarial objections → "recommendation survives". See [[asp-linter-design]] (the linter goal-isolation was the concrete trigger), [[asp-face-design]], [[verus-build-and-run]] (build/run/inspect-emit).

**KEY GROUNDED FINDINGS (corrected my initial framing):**
- `source/rust_verify/src/adsmt_emit.rs` is **NOT** the emitter — it's a post-verification cert→ITP bridge (`adsmt-emit run`). The real seam is **`source/air/src/printer.rs`** (has an existing `print_as_smt` bool + `solver: SmtSolver{Z3,OxiZ,Cvc5,Adsmt}` discriminator — a 3rd `print_as_lukb` dialect is a natural extension, not greenfield) + `emitter.rs`.
- **The goal/assumption distinction SURVIVES into AIR** (`StmtX::Assume` vs `StmtX::Assert`, ast.rs:222) — flattened only at `smt_verify.rs:672-692` (forced to one `Assert` → `not_expr = Not(...)` → unmarked `(assert ¬G)`). So an AIR→lu-kb emitter mapping Assume→hypothesis / Assert→goal-item gets **goal-isolation for free**.
- **The trigger-soundness thesis is HALF-TRUE + the lossy step is in VIR, UPSTREAM of AIR.** Recursive/non-rec fn defs are flattened to `forall+:pattern+fuel` axioms in **`source/vir/src/sst_to_air_func.rs:340-411`** (datatypes' box/unbox/height axioms in `datatype_to_air.rs`); AIR's `DeclX` has only `Fun`-signature + `Axiom`, **no define-fun-rec node** — so re-targeting the AIR PRINTER alone provably does NOT recover faithful defs. Capturing the trigger-win needs a VIR branch (invasive, couples to Verus's hot fuel/polymorphism machinery) OR a fragile lu-kb-face recognizer that re-folds `(=> (fuel_bool f)(forall (= (f x) body) :pattern))`. AND genuine user ∀-lemmas (proof_exec_axioms/broadcast_forall/height) survive as quantifiers on EITHER surface → MBQI still needed; the win is smaller than the proposal assumed.
- **Hard gates against a full retarget now:** kernel #317 + CIC→HOL #325 + the faces are all in_progress and NOT AD1-workspace members; the lu-kb path is adsmt-only → **no external z3-differential oracle** on the path carrying real VCs (the net that caught the 119/600 false-unsat a 13/13 battery missed); the closed-world `def`/`open` modality is a NEW unsoundness mode (classical Verus axiom mis-routed to least-model; conditional defining-axiom guard dropped); theory cliff — Real/BV/Float/div-mod ABSENT from the surface (Int-linear only), so a non-toy VC is unrepresentable; never route VCs at the ASP face's finite-grounding solver (it sound-abstains on the infinite domains every VC has).

**PHASED PLAN** (keep-differential: SMT-LIB stays DEFAULT/canonical + IS the differential oracle during bring-up; drop-portability OFF the table; lu-kb→SMT-LIB downgrade-projection REJECTED; coverage gaps fall back to SMT-LIB, never silent-drop):
- **Phase 0 (HIGH / ≈0 risk) — LANDED + VERIFIED (verus-fork `0e9a9e3dd`, branch `backend-pluggable`, local/unpushed):** tag the negated goal `(! (not G) :goal-negation)` on the `SmtSolver::Adsmt` path only (new `Emitter::log_assert_goal` + a branch at `smt_verify.rs` ~704, ~12 lines; z3/cvc5 keep byte-identical `(assert (not G))`). **Verified end-to-end:** rebuilt verus (vargo) + lu-smt, `verus --log-all -V adsmt` emits `(assert (! (not (=> %%location_label%%0 …)) :goal-negation))`, obligation `1 verified, 0 errors` (tag inert), and the **z3 path emits 0 `:goal-negation`** (A/B byte-identical → differential intact). **This RETIRES the linter LINT-VAC goal-isolation heuristic** — adsmt-cli reads `:goal-negation` from the assert SExpr (a later adsmt-side linter-wire slice). NOTE: a pre-existing STAGED `tools/common/consts.rs` in the fork is the user's WIP — left untouched (committed only my 2 files).
- **Phase 1 — REFRAMED (see the ⚠️ CORRECTION bullet below).** Original sketch was a `print_as_lukb` S-expr dialect; that was NOT lu-kb. Real Phase 1 = design the successor LANGUAGE first (`docs/design/LUKB_SUCCESSOR_SURFACE.md`), then 1b parser/elaborator (`adsmt-ir-lukb`), then 1c the AIR→successor printer with the SMT-LIB path AS the differential oracle (verdicts asserted equal on the 54 vstd + corpus + randomized z3-differential before any successor verdict is trusted). The successor is the unified surface's OWN non-S-expr face, spanning theory terms AND Horn rules (NOT the SMT-LIB face, NOT only the ASP `fact`/`rule` face).
- **⚠️ CORRECTION (2026-06-26) — the earlier "Phase 1 = SMT-LIB-term-shaped item-structured surface" was WRONG and is RETRACTED.** The user caught it: **lu-kb is NOT S-expression-based.** Confirmed by the real `.kb` files (`enum X:` / `fact X:` / `a <- b`, `#` comments) + the `adsmt-parser-lu-kb` lexer (`Colon`/`Arrow(<-)`/`FatArrow(=>)`/`Indent`/`Newline` tokens) — it's an **indentation + keyword layout language**. The retracted design emitted `(axiom φ)`/`(goal G)` **S-expressions** ("item-structured SMT-LIB"), which is NOT the lu-kb successor. A WIP impl of it (verus-fork emitter/context/smt_verify/config/verifier `-V emit-lukb` + adsmt-cli `normalize_lukb_surface`, all uncommitted) was **REVERTED**. **User decision: design the successor LANGUAGE first** (option 1 of 3). **Real Phase 1 = the language design, now written: `docs/design/LUKB_SUCCESSOR_SURFACE.md`.** KEY FINDING — lu-kb is already ~70% there (layout blocks + infix theory operators `+ - * / < > <= >= and or` + typed args `x: Int` + calls `f(a,b)` + enums/data + HKT kinds); the successor is an **EXTENSION = lu-kb + 3 additions**: (a) **quantifiers** `forall x y: Int. body` / `exists`; (b) the **obligation-item layer** `axiom name:` / `assume name:` / `goal name:` + the **sequent turnstile** `H1, H2 |- G` (mirrors AIR `DeclX::Axiom`/`StmtX::Assume`/`StmtX::Assert`; goal un-negated, structural — the real native form of the Phase-0 tag); (c) **theory ops/types Verus needs**, **STAGED** (Tier 0 now = Int/Bool/EUF/=/∀∃/`==>`/`<==>`/ite/let-in/enums; Tier 1 Real+div/mod+nonlinear; Tier 2 BV/Array/recursive datatypes — over the cliff → **SMT-LIB fallback**, never silent-drop). It's the **3rd concrete face of `adsmt-ir`** (beside smtlib/asp; new crate `adsmt-ir-lukb`); SMT-LIB stays the canonical wire + z3-differential oracle through bring-up. **SETTLED syntax (user, 2026-06-26):** canonical equality **`=`** (lu-kb `==` kept as a **legacy alias** — non-breaking), `!=`; space-shared comma-grouped binders `forall x y: Int, k: Bool.`; dot `.` body delimiter; sequent `|-`; implication/iff `==>`/`<==>` (ASCII, no `=>`-lambda collision). **ALL DECISIONS CLOSED (user, 2026-06-26):** NAME stays **"lu-kb"** (just the next version; crate `adsmt-ir-lukb`, flag `-V emit-lukb`); first-cut scope = **Tier 0 + Tier 1** (Int/Bool/EUF/∀∃/enums PLUS Real + real `/` + Int `div`/`mod`; Tier 2 BV/Array/recursive-datatypes → SMT-LIB fallback); item name **optional** (auto-numbered `goal#0`). **Strict `Nat`/`WNat` directive:** `Nat = {1,2,…}` (0 EXCLUDED), `WNat` (범자연수) `= {0,1,…}` (0 included) — distinct kernel types `Nat ⊂ WNat ⊂ Int` (this is what makes the user's Fermat `iff` sound: `x y z: Nat` ⇒ positive ⇒ no trivial `(0,k,k)` solution; `WNat` would falsify it). **Two user proposals integrated into the design doc (2026-06-26):** (1) **bounded/refinement-constrained quantifiers** §2a′ — `forall x in xs. body` (membership, elem type inferred), `forall x in 0..n.` (range, Tier-0/1), and refinement-constrained binders `forall (n: Nat) > 5. …` / `exists (a b c: Nat) >= 2, prime(a), prime(b), prime(c). …`; **CRITICAL distinction (user): a binder CONSTRAINT (refinement typing `{x:T|C}`, before `.`) ≠ a body ANTECEDENT (logic `==>`, after `.`)** — kept distinct in surface+kernel+printer, collapsed only in SMT lowering (∀→`==>`, ∃→`∧`); needs kernel subset/refinement types. (2) **theorem packages** §7 — build-time-proven axiom libraries via the adsmt-emit PM ([[adsmt_emit_system]]); `theorem name:` = machine-discharged (cert, build fails on Unknown/Sat) vs `postulate name: … cite "<ref>":` = externally-justified trust anchor (mandatory provenance citation; e.g. Fermat/Wiles, weak-Goldbach/Helfgott). **Soundness gate:** a mis-stated postulate = FALSE axiom → `⊥` → catastrophic; build gate runs a consistency check (`SAT(axioms)`, reuses the vacuity linter [[asp-linter-design]]) + pins each statement to its citation. Built-ins `pow`/`mod`/`odd`/`prime` (perf). The two examples illustrate the gate: Goldbach `<==>` is unsound (8=2+3+3 even → converse false) → use `==>`. **Adversarial design-review workflow (4 lenses) DONE — found real issues, all reflected in the doc (§9 ledger):** GRAMMAR — the term parser is net-new precedence-climbing (lu-kb's is a flat single-binop fold; `a+b+c` doesn't even parse today), new keywords MUST be CONTEXTUAL (`exists`/`in` are identifiers in lu-kb's own tests — reserving breaks them, so "non-breaking" only holds contextually), need lexer tokens `..`/`|-`, drop `&&`/`||` (lu-kb has only `and`/`or`), disambiguate quantifier-`.` vs field-`.`. SOUNDNESS — `SAT(axioms)` gate is ONE-SIDED (reject-only; Unknown≠consistent; doesn't auto-catch the WNat-Fermat/Goldbach-`<==>` errors — `pow(x,n)` symbolic-n is undecidable, gate abstains); citation-review + machine ground spot-checks (discharge Goldbach@n=7,9,11) are load-bearing. **KERNEL-FIT = THE GATING REALITY:** adsmt-ir kernel has **NO Int/Real theory** ("a later slice"), **no subtyping/coercion**, **no refinement/subset `{x:T|C}` constructor**. ⇒ (a) constraint/antecedent split is **surface-AST+printer ONLY** (kernel lowers to `C ==> φ`; Nat/WNat are postulated sorts + explicit injections, NOT kernel subtypes); (b) **ALL arithmetic (Tier-0 `+−×<>` over Int AND Tier-1 Real/div/mod) is BLOCKED on the adsmt-ir Int slice #317 — so the user's "Tier 0 + Tier 1" scope is NOT buildable at Phase 1b yet.** Triggers carry out-of-band (not on kernel Π); `exists` lowers asymmetrically (postulated `Exists T (λ)`, T:Type0); need top-level `const x: T` decl for free VC vars. **SCOPING DECISION RESOLVED (user, §9): option (c) — build the adsmt-ir Int/Real theory slice FIRST** (couples to kernel #317), THEN Phase 1b with real Tier-0/1 (no arith-fallback crutch). **Phase 1a′ = the prerequisite, NOW SUBSTANTIVELY LANDED:** (1) `adsmt-ir/src/theory.rs` (commit `34e0c8d`+`4240471`, master-track) — the arithmetic theory as a prelude of `open` postulates: `install_int_real` (SMT-LIB-standard Int/Real sorts+ops+int2real+literal helpers) vs `install_arith` (= that PLUS strict Nat/WNat sorts + inter-sort injections + pow/odd/prime; the full lu-kb prelude). **KEY DESIGN: the kernel only TYPE-CHECKS arithmetic; it never DECIDES it (solver's job, post CIC→HOL #325) — so ZERO new trusted kernel surface (no Lit TermKind, no new rule); literals = face-postulated opaque Consts; `Nat ⊂ WNat ⊂ Int ⊂ Real` via explicit injections, NOT kernel subtyping (kernel has none).** 6 kernel tests incl. a mistyped-term-REJECTED firewall check. (2) `adsmt-ir-smtlib` face (commit `0ec8d0c`) — wired Int/Real: `install_int_real` in the prelude (so `Nat`-datatype names don't collide — SMT-LIB face does NOT reserve Nat/WNat/pow/…), an `intern_numerals` `&mut self` pre-pass for literals, and operator dispatch `+ - * div mod abs / < <= > >= to_real` by operand sort (via `elab_same_sorted`; chained comparisons→Prop, n-ary left-fold, sort-inappropriate/mixed-sort REJECTED). 8 conformance tests. Both crate suites green + `--ignored` clean + clippy clean. **Phase 1b FIRST SLICE LANDED** — `~/adsmt-ir-lukb` (NEW crate, own git repo `d4b2992`, master-track): the **3rd face** of adsmt-ir (beside smtlib/asp). Self-contained: hand-written lexer + **precedence-climbing parser** (replaces lu-kb's flat single-binop fold per the review) + span-free surface AST + elaborator to kernel-checked Props + round-trip pretty-printer. Slice = Tier 0+1, NON-layout: items `sort`/`const x: T`/`axiom`/`assume`/`goal` (name optional) + sequent `H1,..,Hk |- G` → `(∧H)==>G`; terms = prop connectives + `=`/`!=`/cmp + Int/Real arith (`+−*/` + `div`/`mod`/`abs`/`pow`/`odd`/`prime`/`to_real` calls) + `forall`/`exists` (space-shared binders, hoisted into `unary` so `not exists` parses) + `let..in` + `f(args)`. **The numeric injection lattice `Nat⊂WNat⊂Int⊂Real` is REAL: the elaborator INSERTS the prelude injections where a narrower operand meets a wider one** (so `n>=3` with `n:Nat` works — the design's no-silent-subtyping mechanism). Firewall: every body kernel-checked to a closed Prop. 10 tests incl. Fermat/Goldbach number-theory shapes + rejections; clippy clean. **Phase 1b SLICE 2 LANDED** (`adsmt-ir-lukb` `0d18344`): the §2a′ **refinement-constrained quantifiers** — a parenthesised binder group carries an inline comparison constraint `(n: Nat) > 5` / `(a b c: Nat) >= 2` applying to each name; desugared at elaboration to per-name guards folded into the antecedent (∀→`==>`) / conjunct (∃→`∧`), **kept distinct from a body antecedent** (so Goldbach `forall (n:Nat)>5. odd(n)==>∃…` has `>5`=domain, `odd(n)`=antecedent — the constraint/antecedent split the user emphasised, realised). Plus the quick wins: **chained comparison** `0<x<10`→`(0<x) and (x<10)`, and the **`==` legacy equality alias** (non-breaking; printer emits canonical `=`). Printer renders the constraint → round-trips. 15 tests, clippy clean. **Phase 1b SLICE 3 LANDED** (`adsmt-ir-lukb` `7ac76c0`): **bounded-range quantifiers** `forall x in lo..hi. P` (sugar for `x: Int` + half-open guard `lo<=x and x<hi`, conjoined ∀→`==>`/∃→`∧`; collection-membership `x in C` needs Seq/Set theory = deferred) + **triggers** `… trigger f(x)` / `trigger { p1, p2 }` (repeatable; patterns parse at application level so a following op isn't swallowed; carried in the AST + round-tripped, but DROPPED at elaboration — kernel Π has no annotation slot, so they'll be threaded out-of-band as solver metadata once the CIC→HOL path lands; dropping is sound — triggers only guide instantiation). New lexer toks `trigger`/`{`/`}`. 18 tests, clippy clean. **Phase 1b SLICE 4 LANDED** (`adsmt-ir-lukb` `db40f83`): **multi-term item body** — an `axiom`/`assume`/`goal` body may be several terms (one per line / conjoined) that fold to a single `∧`-conjunction (`block_body`/`block_rest`/`starts_term`); the layout-free precursor to the indentation-block form. **Phase 1b SLICE 5 LANDED** (`adsmt-ir-lukb` `db1694e`): **function-signature declarations** `fn f(x: Int): Int` / `fn p(x y: Int): Bool` — `Item::Fn{name, params:Vec<(Vec<String>,Type)>, ret}` (space-shared comma-grouped param groups), elaborated to a curried kernel type `T1→…→U` (`Bool`→`Prop` via `elab_type`) postulated as an `open` const; printer renders `fn name(groups): ret`; misapplication is kernel-rejected. New `Tok::Fn` + `param_group()`. 24 tests, clippy clean, `--ignored` clean. **The lukb face is now PRODUCER-READY** (sorts + consts + fns + the full term/quantifier/sequent grammar) for the verus AIR→lukb printer. **Phase 1b SLICE 6 LANDED** (`adsmt-ir-lukb` `6d1e75c`): **backtick-quoted identifiers** `` `…` `` — a symbol a bare ident can't hold (special chars `%`/`~`/`@`/`!`/`.`, empty, or a keyword spelling) is taken verbatim as one `Ident` token (parser UNCHANGED — flows through anywhere a bare ident goes; only lexer scan + printer re-quote via `lexer::ident_needs_quote`). **Backticks NOT SMT-LIB `|…|`** (would clash with the `|-` turnstile: `a |- |b c|` ambiguous). The Phase 1c producer prerequisite — lets Verus/AIR mangled names render FAITHFULLY (not mangled = less lossy). 28 tests; design doc §3a. **PHASE 1c LANDED + VALIDATED END-TO-END** (verus-fork `748fc08fb`, branch `backend-pluggable`, local/unpushed — left my 4 modified + 1 new file committed; the user's STAGED `tools/common/consts.rs` UNTOUCHED). `air/src/lukb.rs` = self-contained AIR→lukb renderer (NO adsmt dep, replicates the keyword set + quoting rule): `DeclX::Sort/Const/Var/Fun/Axiom`→`sort/const/fn(synthetic params)/axiom`, `StmtX::Assume/Assert`→`assume/goal` (UN-negated goal), precedence-aware `ExprX/TypX`→lukb infix (`==>`/`=`/cmp/`+−*/`/`div`/`mod`/`to_real`/`forall`/`exists`/`let`; n-ary Multi folds; `distinct`→pairwise `!=`; triggers best-effort), **Tier-2+ → `#` fallback COMMENT carrying the reason (never silent drop)**. Wired: `context.rs` `lukb_log` sink + `set_lukb_log` + hooks in `global()`/`check_valid()`; `config.rs`/`verifier.rs` `-V emit-lukb` flag (`ArgsX::emit_lukb`) + `.lukb` log file. **TWO choices diverge from the review sketch (both empirically validated):** (1) **solver-INDEPENDENT** (the log is inert → no reason to gate on `-V adsmt`; emits under default z3); (2) hooked at the **post-`block_to_assert` final query** NOT the pre-fold block — `block_to_assert` folds body path-conds into the goal antecedent (`assume Q; assert P`→`goal Q==>P`, faithful), and the USER-meaningful structure still comes through clean because Verus encodes `requires` as query-local `DeclX::Axiom`s (→ `axiom:` hyps) + `ensures` as the `goal:`. **Validated:** `proof fn add_pos(x,y) requires x>0,y>0 ensures x+y>0` → `1 verified, 0 errors` (emit inert) + `root.lukb` obligation = `axiom: \`x!\`>0` / `axiom: \`y!\`>0` / `goal: Add(\`x!\`,\`y!\`)>0` (Verus int-add is `Apply("Add")` → a fn call, faithful); **full prelude = 301 well-formed lukb items, PARSES 100%** through `adsmt-ir-lukb`, 5 `# fallback` comments at the Tier-2 boundary (datatypes ×2, Fun-type ×2, Z3 `Relation(PartialOrder)` ×1). Structural differential harness = `adsmt-ir-lukb/examples/check_lukb.rs` (`90dbe02`) — parse(hard) + elaborate(soft, halts at the `fndef` Tier-2 sort, as expected). **The VERDICT-differential stays gated on CIC→HOL #325** (no solver decides lukb obligations yet → nothing to compare against the SMT-LIB oracle). Built via vargo (bash `tools/activate`, NOT fish). **SLICE 7 LANDED** (`AD1` `00a49e5`, lukb now an AD1 workspace member — see [[faces-in-workspace]]): **`data` datatypes + `fn=body` definitions** — the Phase-2 SURFACE. `data Peano = zero | succ(pred: Peano)` / `data Lst = nil | cons(head: Int, tail: Lst)` → kernel `declare_inductive` (non-param/non-indexed; recursive self-ref; fields named `head: T` or positional `T` — selector names = surface sugar, lowering synthesizes `{ctor}!sel{i}`); `fn f(x:T):U = body` → kernel `define` (`Modality::Def`, δ-unfolded) vs the signature-only `postulate` (`Open`). Recursive `fn` body REJECTED (kernel `fix` = later slice; self-ref is a sound "unknown symbol"). Lexer `data` kw + `|` Pipe; `Nat`/`Int`/`Real`/`WNat` stay reserved arith sorts (datatype uses a fresh name). 34 tests. The kernel inductive+def it produces are the SAME the smtlib face emits → `adsmt-ir-lower` already lowers them; it retires the verus AIR→lukb producer's `# fallback (datatypes)` once the PRODUCER is retargeted (Phase 2 proper = the VIR datatype_to_air retarget + the verdict gate). **STILL DEFERRED:** true indentation-block layout; collection-membership / predicate-guard quantifiers; RECURSIVE `fn`/datatype eliminators in goals (`match` — needs kernel `fix`/the #325 eliminator lowering + #331 engine completeness); Phase 2 proper (VIR producer retarget). The lu-kb-successor surface FACE is now feature-rich (Tier 0+1 complete sans layout); the remaining BIG pieces are **Phase 1c** (verus AIR→successor printer + differential) and **CIC→HOL #325** (so the SOLVER actually decides the elaborated obligations). **REMAINING for the bigger picture:** Phase 1c (verus AIR→successor printer + differential); CIC→HOL #325 recognizing arith terms/literals so the SOLVER actually decides them; to_int/is_int/divisible. **Third user proposal (2026-06-26) — proof-object integrity seal §7.3:** a packaged theorem ships a **512-bit hash of its ANONYMIZED proof object** (canonicalize the cert → strip non-semantic/identifying metadata + hash-cons ordering → digest depends only on math content → reproducible by re-derivation) as a 위변조-방지 seal in the manifest; verify = re-canonicalize+re-hash, mismatch=rejected; = content-address (dovetails content-addressed adsmt-emit store). Postulate seals the statement+citation record. Hash gives integrity NOT authenticity (sign the digest = future). **Hash = BLAKE3 (user pick); 512-bit via its XOF mode (`finalize_xof`→64 bytes, since BLAKE3 default is 256-bit).** **Build order:** 1a design **DONE** → 1b `adsmt-ir-lukb` parser+elaborator (Tier 0+1)+pretty-printer → 1c verus AIR→successor printer (Tier 0+1, dual-emit+differential, SMT-LIB stays oracle) → Phase 2 native datatypes/defs (the trigger win, gated on #317+#325+faces-in-workspace).
- **Phase 2 (HIGH / HIGH — the actual trigger win, HARD-GATED):** native datatypes (VIR `datatype_to_air.rs` → kernel Inductives, dropping the ~12 box/unbox/height/accessor `:pattern` axioms/datatype = worst matching-loop offenders; needs field selectors/discriminators — ASP `Ctor` is positional).
- **Phase 3:** native non-rec fn defs (needs a function-definition-WITH-BODY surface item — absent from adsmt-ir-asp, which has only predicates+Horn rules) + the modality contract (conditional defining-axiom → GUARDED def, never unconditional closed-world; closed-world OPT-IN per item).
- **Phase 4:** recursive defs (drop fuel+trigger → Inductive) + theory (Real/BV/Float) + non-Horn ∀ bodies.
- **GATE for Phase 2+:** (i) #317+#325 DONE + faces are AD1 members; (ii) lu-kb passes a full A/B z3-differential on the whole verus prelude.

## lukb 표면 `if`+`match` v1 FULL LANDED (2026-07-03, AD1 `4ae487d`→`3d903f8`→`91ecb02`)

verus-fork의 2026-07-03 제안(3-렌즈 설계+적대 리뷰, 9 asks)을 **당일 풀 v1**으로 landing (오너 승인: 풀 v1 지금, 슬라이스별 커밋; guards 포함 + strict hard-error = verus-fork 오너 사전 확정). 구현 전 5-에이전트 소스 검증(34 주장, load-bearing 전부 정확; 앵커 정정 4건 — adsmt-ir theory.rs:128, env.inductive :256-258, reduce.rs Match guard :176-178, Neg≠unify_sorts). 핵심:
- **① `ite : Π(T:Type). Prop→T→T→T` prelude postulate**(부재였음 — 유일한 load-bearing 다리) + 표면 `if c then a else b` → `(ite T c a b)`, Bool inductive 절대 금지(prelude에 없음; Bool=Prop). 조건 Prop-체크, 분기 sort는 elab_bin과 같은 numeric lattice(unify_sorts).
- **② flat `match` → 커널 `Match`**: `match`+`=>`(FatArrow, `==>`/`<==>` longest-first 뒤; `->` 미중복). **ctor-arity readback**(`env.inductive().ctors`+`peel_pis` — elaborator 최초). sort-directed 라우팅: Prop scrutinee → ite 빌더(`match p{true=>a,false=>b} ≡ if` **is_def_eq 테스트로 고정**); datatype → 선언순 1-ctor-1-total-minor, 비의존 motive, 필드당 λ. §6 계약 그대로: bucket→order→total, wildcard/binder catch-all 확장(binder는 scrutinee를 명명 — **subst_surface**(fn-인라인 관용구)로 표면 치환→field binder 아래 재-elaborate가 de Bruijn shift를 공짜로 처리), **guards는 bucket 내 nested-ite right-fold**(마지막 기여 arm이 unguarded여야 total — 구문적 backstop만; 의미론적 guard-완전성 시도 금지), 미커버 ctor = **ctor명을 담은 hard error**(fabricated branch 절대 금지; 커널 `minors.len()==ctors.len()` BadElim이 최종 방어). value-valued datatype match는 elaborate되고 lowering이 abstain(건전 Unknown).
- **③ Int/Real 리터럴 패턴** = 등식 guard desugar(`n => b` ⟶ `x if x=n => b`); **숫자 scrutinee match는 커널 Match를 안 거침** — 순수 ite 체인(`elab_scalar_match`) → 검증된 term-ite lowering이 **value-valued 스칼라 match를 판정**(e2e: `x=3 ⊢ (match x{3=>x,_=>0})>2` → 네이티브 DefiniteUnsat). 리터럴은 숫자 sort를 exhaust 못함 → unguarded `_`/binder backstop 필수(없으면 hard error). `n if g`는 and-결합.
- e2e 5건(lukb-driver): if 검증/반례, datatype match 네이티브 unsat(#331/#334 selector congruence), value-datatype Unknown, 스칼라 value match unsat. adsmt-ir-lukb 88/0. 문서 정정: LUKB_SUCCESSOR_SURFACE.md "#331 gated" stale 문구 + Tier-0/2 + §3a(`=>`는 이제 match-arm arrow; 예전 "lambda arrow" 사양 문구 폐기). 회신 `2026-07-03-lukb-if-match-LANDED-...md`(9 asks 전부 답변).
- **verus 쪽 후속**: `lukb.rs:225` `ExprX::IfElse` fallback 제거가 UNBLOCKED(verus는 if만 방출 — Phase-1로 모든 Rust match 커버); native match emit은 VIR retarget(Phase 2) 게이트. **연기**: nested/or/@ 패턴(명시적 내부 match로 우회), value-producing datatype match verdict(is-Variant lead), Elim/Fix, `:badmatch` opt-in, shadowed-arm warn(진단 채널 부재 — linter 후보).
