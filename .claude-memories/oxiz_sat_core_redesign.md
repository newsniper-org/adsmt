---
name: oxiz-sat-core-redesign
description: "OxiZ SAT/CDCL(T) core redesign — open-ended: audit found a pure-SAT 1-UIP soundness bug (fixed); design recommends an integration-layer clean-room, not a full rewrite"
metadata:
  node_type: memory
  type: project
  originSessionId: 32a1dc0d-7730-4862-8df4-6958199ce84f
---

**OPEN-ENDED autonomous effort (NO time cap — user clarified 2026-06-13).** Strategic question the user raised: "redesign OxiZ's SAT core clean-room like the oxiz-mbqi quantifier engine?" The user chose: (1) FIRST differential-audit the PURE SAT engine, then (2) produce a DESIGN DOC (not code) recommending the redesign SCOPE. Both delivered via a multi-agent Workflow. Carry the work Audit→Design→implement→validate→port; report at each milestone; confirm only on scope-changes or irreversible decisions; push/tag stays manual. Builds on the 7 soundness fixes in [[oxiz_mbqi_rewrite]] (this session) — those were the CDCL(T) glue; this is about the SAT core itself.

**AUDIT FINDING — the pure SAT engine HAD a real soundness bug, now FIXED (OxiZ `cc3872c`, parent bump `52ed941`).** `oxiz-sat/src/solver/conflict.rs::analyze()` (the 1-UIP conflict analysis) skipped reason-clause `lits[0]` (`start = if p.is_some(){1}else{0}`) on the MiniSat convention that a propagation reason keeps its implied literal at index 0 — **oxiz does not maintain that invariant**, so when `lits[0]` was a current-level antecedent it was dropped from `seen`/`counter` → counter undercounted → trail walk stopped at a non-dominating literal → learned an unsound over-strong unit → spurious UNSAT. Fix: process EVERY reason literal; the pivot `p` is already `seen` (selected off the trail), so the `!seen` guard skips it — never assume `lits[0]`. **CRITICAL PROCESS LESSON: my first differential harness was a FALSE NEGATIVE** — `oxiz --dimacs` converts CNF→QF_UF and runs the SEPARATE oxiz-solver SMT CDCL, never touching `oxiz-sat`; the audit built a `pure_sat_runner` example (`oxiz-sat/examples/`) driving `oxiz_sat::Solver` directly. The bug: **41/106,693 instances spurious-UNSAT (0.038%), all oxiz=unsat vs cadical+z3+cryptominisat5=sat, ZERO fake-SAT, config-gated on `random_polarity_prob>0`** (shipped Default + 9/10 presets; minisat preset clean). Verified fix: `oxiz-sat/tests/soundness_repro.rs` (minimal 6-var/7-clause, now un-`#[ignore]`'d) passes; pure-SAT campaign 37.5k instances / 4 presets / 0 unsound / 0 model-invalid; SMT path unaffected (solve_with_theory shares analyze): theories 1172 + solver 526 + differential fuzzer fatal=0. Tools on PATH: cadical (gold, DRAT), z3, cryptominisat5, minisat, drat-trim. Canonical harness `oxiz-sat/tests/sat_diff_fuzz_pure.py` (drives `pure_sat_runner`, self-validates models; the false-negative `sat_diff_fuzz.py` was removed).

**DESIGN — `docs/design/SAT_CORE_REDESIGN_ASSESSMENT.md` (382 lines, in OxiZ repo). Bottom line: INTEGRATION-LAYER CLEAN-ROOM + the analyze() fix — NOT a full 38k rewrite, NOT keep-patching.** Architecture (verified): oxiz-sat ~38k lines is "mono-struct core + standalone toolbox"; the `Solver` struct embeds only the core CDCL; XOR/Gauss, cube, GPU, ML-branching, preprocessing/inprocessing are SEPARATE structs **never wired into solve()** (dead code w.r.t. the solve path; grep `self.{xor,cube,gpu,...}` over solver/*.rs is empty). All 7 session soundness bugs + the bug CLASS live in the advisory theory glue, not the SAT nucleus; the engine is DRAT-self-certifying. So the leverage is to rewrite the unsound LAYER and reuse the sound core (the oxiz-mbqi shape: there the quantifier engine was unsound and got rewritten standalone while its host was reused). **The design (MBQI "make the bug unrepresentable" move, transposed to the trail):** (§4.1) register the theory ON the `Trail`; fire `push_frame`/`pop_frame` from INSIDE `Trail::new_decision_level` + `backtrack_to_with_callback` (the sole level-mutators) → `|theory frames| == trail.decision_level()+1` always → restart/vivify/incremental desync structurally impossible; DELETE the parallel `level_stack`. (§4.2) replace advisory `TheoryCallback` (4 callbacks, `on_new_level` default-empty) with a MANDATORY trail-driven `TheoryHooks` (5 methods; `assign_hook`/`unassign_hook` driven by the trail → DELETE the `theory_processed` re-scan hack; stale-bound impossible → DELETE the 3 `last_conflict_is_stale_bound` suppression guards). (§4.3) typed `Reason::TheoryLemma` + `TheoryReason{asserting: Lit, explanation}` → sentinel-0 leak (`conflict.rs:386` `Lit::from_code(0)`) unrepresentable. (§4.4) all 6 glue bugs → "impossible by construction" table. (§4.5) debug-assert `frame_depth == level+1`. **Phased plan (oxiz-mbqi playbook):** Phase 0 = trusted-core gate (analyze fix ✅ + corrected fuzzer CI gate ✅ + 10k DRAT-verified fuzz + theory-callback fuzz of `add_theory_reason_clause`/`analyze_theory_conflict` with random SOUND lemmas + the `conflict.rs:386` defensive fix) — **if clean, engine reusable**; Phase 1 = standalone `TheoryHooks`+typed Reason+trail hooks + toy theory (z3/cvc5 fuzz); Phase 2 = port real EUF/arith/simplex, DELETE parallel stack + suppression guards + re-scan; Phase 3 = both fuzzers as permanent CI gates. **Effort ~1/10 of a 38k rewrite** (~6.8k glue + 2 surgical engine edits + analyze fix). **Escalate only if** Phase-0 theory-callback fuzz surfaces a real `analyze_theory_conflict` defect (→ rewrite just that + reason-clause machinery) or post-fix pure-SAT fuzz keeps finding DISTINCT core repros. **Validation gates:** `sat_diff_fuzz_pure.py` (pure SAT vs cadical, +DRAT mode TODO) + `ground_soundness_fuzz.rs` (CDCL(T) vs z3). STATUS (2026-06-13): **Phase 0 SUBSTANTIALLY COMPLETE — engine confirmed reusable.** analyze() fix (`cc3872c`) + the sibling `analyze_theory_conflict` `[1..]` defensive fix (`4419298`, same lits[0] class, SMT-fuzzer-validated non-breaking) committed; parent bumps `52ed941`/`fe3e879`. Evidence the engine is reusable: pure-SAT 40k / 4 presets / 0 unsound + 0 model-invalid; SMT path (which exercises analyze_theory_conflict) fatal=0; both analyze bugs fixed; no new distinct repros. DEFERRED Phase-0 polish (NOT quick — and lower marginal value given the above): (a) DRAT-verified pure-SAT fuzz needs WIRING DratWriter into the core solve loop (proof.rs is currently UNWIRED standalone code; oxiz-sat's solve() emits no DRAT — the CLI `--proof-log` is the SEPARATE oxiz-solver SMT proof path); (b) theory-callback fuzz needs a synthetic SOUND theory (the existing `ground_soundness_fuzz.rs` already exercises the real EUF/arith theory surfaces, clean); (c) the `conflict.rs` `Lit::from_code(0)` placeholder leak when `p==None` (defer to a theory-callback fuzz to confirm it triggers; the typed-Reason redesign removes it structurally). **NEXT DECISION POINT: Phase 1 (integration-layer clean-room) — a major scope-change AWAITING USER CONFIRMATION before embarking.**
