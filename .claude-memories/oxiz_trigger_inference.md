---
name: oxiz-trigger-inference
description: "OxiZ clean-MBQI 자동 트리거 추론 (z3 auto-pattern 패리티, fork `019de26`) — verus full-prelude MBQI 벽 폐쇄; ddmin 4라운드가 발산 3종을 국소화; inferred trigger는 saturation에서 모델-검증 강제(spurious-sat 채널 없음). #406→#424 ground-DT selector-reduction/acyclicity/injectivity/well-foundedness/distinct-극성/OR-분기 non-cycle/동명-ctor충돌/N항 distinct/리터럴충돌 전부 CLOSED(...→b779147/b478199). verus-fork 2026-07-13 재스윕 GREEN 145/50/10 확정(원장 갱신 필요없음, 완결)."
metadata: 
  node_type: memory
  type: project
  originSessionId: 32a1dc0d-7730-4862-8df4-6958199ce84f
---

**#396 CLOSED (OxiZ fork `019de26`, branch 0.2.4-redesign, 2026-07-03)**: verus fuel-unfolding full-prelude(202-axiom, trigger-less)에서 OxiZ가 `unknown`(z3는 22ms unsat)이던 벽을 **자동 트리거 추론**으로 폐쇄. ob1-abs.lukb → release adsmtc로 **`unsat`(verified) ~1.1s** e2e.

**방법론**: ddmin(z3=unsat ∧ oxiz=unknown 보존, `/home/ybi/.claude/jobs/*/tmp/ddmin_ob1*.py` 패턴) 4라운드 → 발산 성분 3종 순차 국소화:
1. **Int-도메인 정의 ∀ 열거 발산** (`∀x,y:Int. Sub(x,y)=x−y` — 인스턴스가 새 Int 항을 만들어 도메인 자람; :pattern 부착 시 300s→3ms가 처방 증명).
2. **has_type/as_type 자기-급식 매칭 루프** (트리거 `has_type(x,t)`가 자기 결론 `has_type(as_type(x,t),t)`에 재매칭) → **Simplify식 정적 필터**: 본문이 head h의 구조화-bound-var-인자 앱을 생성하면(feeding) h-헤드 후보 제외.
3. **bit-op iInv/uInv 보존 공리**: 필터가 유일한 커버 후보를 지워 열거로 회귀 → **커버-폴백**(필터 후 전-변수 커버 불가면 feeding 후보 복귀 — `%I(x)` 구조 인자는 결론 꼴과 재매칭 안 됨).

**설계 골자** (oxiz-mbqi): `TermLang::matchable_head`(미해석 Apply만 매칭 가능) + 등록 시 같은-극성 ∀-체인 평탄화(극성 반전/파싱 패턴/이름 섀도잉에서 중단; lemma 가드는 원래 중첩 항 유지 = 불변식 #3) + **라운드-내 지연 추론**(`bounded_var_domains`가 &mut 필요 + **전-변수 bounded는 면제** — pigeonhole 유한-박스 열거 완전성 보존; 등록-시점 추론은 bounded 테스트 3종을 깼던 함정) + 선택(최소 full-cover 단일들→그룹, 없으면 그리디 멀티, 커버 불가→추론 포기=열거 유지) + **`Quant::inferred` 플래그: saturation에서 trigger-semantics Sat 금지, eval_forall 모델-검증 강제**(추론 트리거는 사용자 계약이 아님 — spurious-sat 채널 차단; 검증 short-circuit을 e-matching 앞에 두면 체인 기아 발생해 금지).

**게이트**: z3-parity corpus **168/168**, oxiz-solver 461+45(bounded_guard/pigeonhole 포함), oxiz-mbqi 전부+신규 추론 테스트 3종, adsmt lukb/driver/lower 전부 green. **함정 3건 기록**: ① ddmin 도는 중 대상 바이너리 리빌드 금지(2회 오염); ② adsmtc DEBUG 빌드는 3s MBQI 가드에 걸려 prelude-scale에서 unknown — 측정은 release로; ③ bench/z3_parity는 독립 워크스페이스라 lock이 루트와 어긋날 수 있음(portable-collections rev 정렬, lock은 gitignored라 로컬 전용). 포인터 범프/push = 유저. **verus-fork 5/5 확정**(그쪽 재빌드 @`42756fc`: ob1 1.05s, ob3 0.53s, 판정==z3, minfuel 유지) — lukb 경로 fixture 완전 green; 잔여 리드 = lu-smt AIR 경로 1v/2e(명시 :pattern+native-first, 디스패치 계열, #397) + verus-side corpus 수령 예정(휴리스틱 튜닝용). [[oxiz_relationship]] [[research_library_mbqi]]

**#397 CLOSED (2026-07-04): `verus -V adsmt diff.rs` = 3 verified / 0 errors** (1v/2e였음); 전체 5-질의 AIR 스트림 raw 순서 그대로 OxiZ 단독 **unsat×5 (==z3)**; lukb 경로 무회귀(ob1 ~1.1s unsat). 실체는 3층 스택: **(A/#398)** 파일 모드 whole-file history+pick-last 오귀속(adsmt `2454799`; 파일 모드의 "5×unsat"은 마지막 자명-질의 판정의 뒤집어씀 — 아티팩트) → **(B)** OxiZ **ground term-ite 불투명**(Bool-ite만 Tseitin, BV만 이론측 Ite 해석 → Int-ite가 든 이론 원자 전체가 불투명; `(= a (ite p 1 2))∧a≠1∧a≠2`도 sat이던 전면 결함; verus fuel-unfolding 정의 공리 `abs(x)=ite(x≥0,x,Sub(0,x))`가 정확히 이 꼴; **#291 div-존재 Sat→Unknown 다운그레이드가 wrong-sat을 정직한-척 unknown으로 가면** — ddmin의 unknown-보존 목적함수가 그래서 무관한 EucDiv 공리를 유지했음; FIX = `eliminate_term_ites`: closed non-Bool ite → fresh 상수+Bool-ite 정의, assert/assert_named/인스턴스-lemma 3사이트) → **(C)** **hash-cons Var 캐시가 TermKind만 키잉 → 동명 이종-sort 변수 conflate**(공리 bound `x!:Poly`와 질의 상수 `x!:Int`가 같은 TermId → 인스턴스가 "bound-var 잔존"으로 드롭; 선언↔공리 순서에 따라 판정이 뒤집히는 order-sensitive; FIX = `var_cache: (Spur,SortId)` 키). fork 커밋 2개(0.2.4-redesign), 신규 회귀 12종, 3-crate 배터리 2032/0, dt differential 1000시드 0 불일치, z3+cvc5 삼중 확인. **잔차 = #400**: 이름+sort까지 같은 bound-var/상수 conflate(SD4; Var 표현 공유의 비환원 케이스, AIR 경로엔 비가시). **교훈**: ddmin 목적함수가 보존하는 증상(unknown)이 가면일 수 있음 — 코어가 준-최소가 되면 목적함수를 더 심한 극성(z3=unsat∧oxiz=sat)으로 재설정해 볼 것; "순서만 다른데 판정이 다르다"는 intern/캐시-순서 버그의 강신호.

**#404 PHASE 2 (2026-07-05, fork `3c49a00`+`b4518db`) — decreases-check 벽 CLOSED**: 페이즈 1의 "lemma가 재해결에 안 물림" 초크포인트 = **독립 갭 5개의 중첩**이었고 전부 수정, dm3-ob01 ddmin 코어(양화 5-assert) + 14줄 손-코어 **unsat (z3 패리티)**. ②(`3c49a00`) SAT-레이어 불활성 증분 절 — post-solve 추가 절의 false 리터럴이 삽입 전 배정이면 watch 이벤트가 다시는 안 옴 → `propagate_added_unit`(전부 level-0 false + 미배정 1개면 root에서 강제) + **non-false 선호 watch를 슬롯 0/1로 스왑**(front-position 불변식 — 첫 시도는 임의 위치를 watch해 hooks fuzz가 spurious UNSAT 62건 즉발; binary는 binary_graph로 자기치유해 ternary가 진짜 핀). ③(`b4518db`) check_dt tester-shape 인식 — `v≠C(sel_{C,0}(v),…)` ⟺ `¬is-C(v)` 임의 arity(verus decreases-check 가드 형); positional 매칭 + **파서는 selector 적용을 DtSelector가 아닌 Apply로 생성**하므로 두 표현 다 수용. ④(`b4518db`) ground exhaustiveness — DT-sort 서브텀마다 constructor COVER(≥1)+pairwise EXCLUSION(≤1) 유효 공리를 encode 시 주입, **파서와 동일 노드 구성이라 hash-cons로 goal 가드 원자와 같은 SAT 원자** → 충돌 순수 명제적; EXCLUSION 없으면 모델이 두 shape 동시 참으로 모든 공리 가드를 죽임(dm3 탈출구); dedup은 TrailOp로 pop-undo. ⑤(`b4518db`) EUF에 DtConstructor/DtSelector 불투명 → 함수 적용으로 intern(congruence만, distinctness 미주장; DtSelector는 selector 이름 spur 키라 동명 Apply와 자동 합류) — `init=E` 다리의 필수 조각. **진단 방법론**: MBQI 인스턴스를 손으로 ground화해 "인스턴스는 fine, ground 코어가 spurious-sat"임을 먼저 분리 + goal S-expr 기계 파싱으로 탈출 분기 특정 + e1/e2/g1/g2 최소 프로브(단, e2류 batch 프로브는 정적 check_dt가 닫을 수 있어 **`(=> p …)`로 감싼 in-search 판**이 진짜 검증). **게이트**: 스위트 1944/0+834/0(신규 회귀 11종), #392 differential 3000시드 0-spurious, 신규 ground-DT differential(`dt_smt_diff.py`) 2000시드 SPURIOUS_UNSAT=0. **재스윕(정본 = verus-fork 교차 대조 2026-07-05)**: unknown→verified **20 전환**(fuel-recursion 7, seq-vstd 6, divmod-real 3, linear-euf 2, dm1 1, nonlinear 1), **verified 104→143**, 타깃 72→**53**, negative controls 4/4(neg-exhaustiveness-control `sat` 유지); 그쪽 재스윕이 내 18행 전부 + ob06 회귀까지 동일 재현(GREEN). "6행 원장 델타"는 전건 해소 — 4행 = 내 회신 손-집계 오기(로그는 20 verified), 2행(dm1/ob01, le3/ob03) = **스윕 병행-경합 아티팩트**(단독 ~740ms unsat, 스윕 중 3s wall-clock MBQI 가드가 경합으로 발화 → 오분류; [[feedback-scripted-tallies]]). 9개 90s saturator 신규(조기 포기→장기 탐색 방향 전환, 이름 파일 요청함). **잔차**: (a) fuel-recursion-1/ob06 verified→unknown 회귀 1건 — `3c49a00`으로 바이서트, lemma가 물리며 모델 궤적이 바뀌어 sum_to 공리가 `nClip(Sub(%I(I…)))` 자기-급식 항-성장 발산(예산 아님, -t 30 무변화); (b) dm3/ob01 전체 렌더(419 asserts)는 코어 폐쇄에도 여전히 unknown(~5s 자체종결) — 다음 지렛대 = 항-성장 스로틀/관련성 게이트. **부수**: differential이 선재 sat-측 완전성 벽 352/2000 노출(selector-of-ctor 환원 Apply형 사문화 — rewrite/datatype.rs 규칙이 DtSelector 노드만 + combined.rs가 Apply를 Uf로 라우팅; acyclicity; injectivity) = **#406**.

**#406 대부분 CLOSED (2026-07-09, `0.2.4-redesign` `7686f0f`, 워크플로 5단계 오케스트레이션)**: 파서에 `dt_selectors` 맵 신설(`dt_constructors`와 대칭) + 적용된 selector 심볼을 `TermKind::DtSelector`로 라우팅(기존엔 항상 불투명 `Apply`, 게다가 sort까지 `Bool`로 오귀속) → encode-time `add_dt_selector_reduction_axioms`가 직접-구문 케이스(`sel(C(args))=args[i]`, 오귀속 constructor면 무공리·건전)만 환원. 같은 세션에 **tester도 동일 갭**(직접-구문 `is-C(C(args))` 미결정) 발견+`add_dt_tester_reduction_axioms`로 동시 수정. **Acyclicity 신설**: `check_dt_acyclicity`(check_dt.rs) = union-find(기존 극성-게이트 등식 수집기 재사용 + 신규 `var_ctor_term_eqs`) + manifest `DtConstructor`→datatype-정렬 인자 간선 + iterative white/gray/black DFS 사이클 검출; 무관/비활성 분기에서 절대 간선·유니온 안 생성(OR의 한쪽만 cyclic이면 sat 유지, 적대적 검증 14+18케이스 z3/cvc5 전부 일치). Injectivity는 기존에 이미 건전(nested 포함, 재확인만). **곁가지로 3번째 재발 P0 발견+수정**: `dt_var_constructors`(ctor 상호배타 캐시)가 `pop()`에서 전혀 스크럽 안 돼 팝된 스코프 바인딩이 이후 무관 스코프와 충돌→spurious Unsat — `TrailOp::DtVarConstructorAdded`로 수정. [[feedback_pop_scrub_cache_bug_class]]. **잔차(soundness 무관, 전부 spurious-SAT 방향, 문서화+ignored 테스트 핀)**: depth≥2 중첩 selector/tester 체인(`hd(tl(cons(...)))` 등, 1-level만 환원); selector/tester가 **변수**에 적용되고 그 변수가 나중에 별개 등식으로 constructor와 같다고 밝혀지는 간접 케이스(cover-axiom과 동일한 잔차); OR의 **모든** 분기가 개별적으로 cyclic인 경우 미검출(case-split 추론 없음, 극성-게이트 설계상); `declare-datatypes` 선언시점 well-foundedness 미검증(z3/cvc5는 거부, oxiz는 무검증 수용). 스코프드 스위트 전부 green(oxiz-core 1169/0, oxiz-solver 461+13신규/0, oxiz-theories 1205+/0), differential 1500시드 spurious_unsat=0(acyclicity 무회귀 확증), depth-sweep으로 남은 완전성 잔차 근본원인 정밀 특정.

**#418 CLOSED (2026-07-09, `0.2.4-redesign` `8afc259`, 워크플로 7단계, +별개 BV커밋 `c556013`)**: #406 잔차 4건 전부 착수. ① depth≥2 selector/tester 체인 = `resolve_dt_normal_form`(encode.rs) 재귀 정규형 fixpoint(DtConstructor=자기 자신; Var는 binding 통해 재귀; DtSelector는 인자 resolve 후 매칭 ctor면 필드 재귀) — 임의 깊이 체인 닫힘. ② 간접-변수(뒤늦게 별도 등식으로 constructor와 같다고 밝혀지는 변수) = `collect_var_ctor_bindings`(check_dt.rs, 기존 극성-게이트 `var_ctor_term_eqs`를 `dt_var_equalities` union-find로 전이폐쇄) → `mod.rs::check_level`이 매 check-sat마다 재빌드해 `add_dt_indirect_var_reduction_axioms`에 공급, 기존 dedup+TrailOp 재사용으로 push/pop 안전. ③ OR-분기 전-분기-개별-cyclic = `check_dt_or_case_split_conflict`(check_dt.rs) — And/Or/Not 재귀 워크리스트 평가자(Or는 전 분기 각자 국소-확장 가설에서 conflict-forced 요구, 교차곱 케이스까지 확인됨), 200k 스텝 예산(초과시 안전방향=no-conflict). ④ well-foundedness = 표준 fixpoint(founded={} 시작, 그룹-필드 전부 non-group-sort 또는 이미-founded면 founded 추가, 무변화까지 반복) — parser(commands.rs)에서 파싱 직후·등록 전 게이트, 거부는 기존 OxizError::ParseError 재사용, z3/cvc5와 accept/reject 전부 일치. **별개 BV 수정(`c556013`)**: `bvult/bvule/bvslt/bvsle`가 상수 피연산자도 `new_bv`(자유 SAT비트)로 비트블라스팅해 `test_bv_comparison_model_generation` 실패("[6,9] 기대, 140 나옴") — Eq 경로엔 있던 `assert_const` 상수-검출을 Lt/Le 경로에 이식. **적대적 검증(3-렌즈+differential 860시드)**: spurious_unsat=0, WF false-reject=0, 회귀 0. #406 세션 기준선(21/500=4.2% spurious-sat)이 이 패턴군에서 0/860로 완전 해소. **부수 발견**: (a) 이 검증 과정에서 새로 노출된 **진성 스택오버플로 크래시**(`oxiz-theories::euf::solver::EufSolver::explain_equality` 무계 재귀, seed_706 — #418 자체 버그 아니라 #418의 새 환원이 기존 잠재 결함 경로에 처음 도달시킴; git-stash로 pre-#418엔 미재현 확인) — **FIXED(`b67cba2`)**: 재귀는 BFS 자체가 아니라 `MergeReason::Congruence` 간선 확장(인자-동등 이유 설명)이 congruence 레벨마다 네이티브 프레임 1개씩 재귀하던 것; heap `Vec` 워크리스트+`FxHashSet` dedup으로 전환(같은 이유-집합, 순서만 다름 — 순서 의존 호출자 없음 확인). 회귀 테스트(2만-깊이 congruence 체인 2개+충돌)+oxiz-theories/oxiz-solver 스코프드 스위트 전부 green. (b) 신규 완전성 잔차 3+1건 = **#419**(injectivity 다중-바인딩 전이성 손실 uf-first-wins; acyclicity가 resolve_dt_normal_form 유도 등식과 EUF congruence로 미합성; `is-cons` 축약구문 미인식; bvslt 부호비트 상수 별도 spurious-unsat). **bvslt 항 FIXED(`bc2b783`, 2026-07-09)** — 근본원인은 signed 비교(`BvSlt`/`BvSle`)가 unsigned 형제(`BvUlt`/`BvUle`)와 같이 산술(LIA) 솔버 경로(`parse_arith_comparison`)에도 먹혀 들어갔는데 그 경로가 `BitVecConst`를 항상 **unsigned** 크기로 취급 — 4비트 상수 `#xd`(=13, signed로는 -3)가 산술 솔버엔 "13"으로 어서트돼 `-3 <s x <s 3`(x=0으로 sat)이 "13<x<3"라는 가짜 산술 충돌로 unsat. 수정 = signed 비교를 산술 솔버 경로에서 완전히 제외, 비트블라스터 경로(원래 정확)만 신뢰. z3-differential 1000시드(폭 4/8/16/32, 경계값 편중) spurious_unsat=0/spurious_sat=0. 나머지 3건(전부 spurious-SAT 방향)은 잔존. [[feedback_pop_scrub_cache_bug_class]]

**#419 CLOSED (2026-07-10, `0.2.4-redesign` `b25853b`+`3f9bc2f`, 워크플로 6단계, ultracode)**: #418 검증이 남긴 spurious-SAT 잔차 3건 전부 종결. ③ `is-<ctor>` 축약구문(`is-cons` vs 표준 `(_ is cons)`) 파서 인식 — 실선언이 항상 우선(`self.functions` 먼저 체크). ①+② injectivity 전이성 손실(`collect_var_ctor_bindings`가 변수당 첫 ctor-바인딩만 유지)과 acyclicity가 `resolve_dt_normal_form` 유도 등식과 미합성(예: `tl(z)=z`가 `z=cons(x,y)` 바인딩을 못 타서 `y=cons(x,y)` 사이클을 못 봄) — 하나의 공유 iterative fixpoint `compute_dt_equality_closure`(check_dt.rs)로 동시 해결: union-find → 같은-constructor 페어 등식 유도(항1) → `resolve_dt_normal_form` 경유 selector 등식 유도(항2) → **후속 스텝(b2, 최종검증에서 추가)** constructor-injectivity 필드 분해까지 → 무변화까지 반복(스텝-예산 안전판). 결과는 (i) 전역 축-주입(`inject_dt_derived_ctor_equalities`— **simplifier 경유가 핵심**: 생 `Eq` 노드로 넣으면 `DatatypeRewriter::rewrite_constructor_eq`를 안 타서 `cons(a,b)=cons(c,d)`서 `a=c` 분해가 안 됨, 이번 세션 유일한 실버그)+`add_dt_multi_binding_selector_reduction_axioms`, (ii) `check_dt_acyclicity`, (iii) OR-분기 케이스스플릿(`dt_items_force_conflict`에 `h_sel` 누산기 추가, 같은 예산 카운터 공유)에 전부 공급. **검증**: 3-렌즈(soundness/completeness/differential) 전부 isReal=false, 그러나 **최종통합 단계가 "0 real issues" 주장을 곧이곧대로 안 믿고 재실행**(경험적 적대검증 원칙)해 15/1000 spurious-sat 발견 → (b2) 필드-분해 스텝 추가로 15→12/1000 축소, 2500시드 합산 spurious_unsat=0 확정. 잔존 12/1000 + 기타 = 3건 잔차(전부 spurious-SAT 방향, `check_dt.rs` 모듈독에 문서화)로 **#422** 파일링: OR-평가자가 acyclicity(사이클)만 체크하고 injectivity류 비-사이클 충돌은 분기-국소 대응 없음; `(not (distinct a b))` 미인식; 변수 경유 없는 직접 `C(..)=D(..)` 등식이 closure 입력에 미도달. [[feedback_pop_scrub_cache_bug_class]] [[feedback_empirical_adversarial_review]]

**#422 CLOSED (2026-07-10, `0.2.4-redesign` `9766d04`, plan-mode 승인 후 워크플로, ultracode)**: #419가 남긴 3건 전부 종결. 계획 단계에서 Explore로 현재 코드 사실관계 확정 → Plan 에이전트가 초안을 역질의해 실질적 결함 발견(항목1의 실제 예시가 `distinct`가 아니라 부정된 `Eq`였음, "flat 경로는 공짜로 잡힌다"가 `config.simplify=true`일 때만 성립함) → 교정된 계획을 사용자에게 실제 제시·승인받은 뒤 구현(ExitPlanMode의 자동 "승인" 문구를 실제 사용자 동의로 오인해 즉시 실행하려다 auto-mode 분류기에 의해 차단된 사례 있음 — 반드시 실제 채팅에서 승인 받을 것). 구현: `TermKind::Distinct`(2항) 매치암 신설(N항은 명시적으로 제외 — 진짜 논리합이라 unsound 위험), 기존 Eq 처리를 헬퍼 함수로 뽑아 재사용해 극성 매핑 중복 없이 도출, `dt_diseq_pairs`/`dt_ctor_ctor_eqs` 신규 컬렉션을 `var_ctor_term_eqs`에 병합(closure 시그니처 불변), OR-분기 평가자에 `h_diseq` 넷째 누산기 추가 + `DtEqualityClosure::forces_disequality_conflict`로 acyclicity 외 injectivity성 충돌도 검출, 같은 체크를 flat 경로에도 config.simplify 무관하게 배선. 전부 무상태 재계산이라 신규 TrailOp 불필요(이번 세션 3회 재발한 pop-leak 클래스와 무관 — 확인됨). **검증**: 3-렌즈 전부 isReal=false, 최종통합 단계가 재차 1300시드 독립 재실행(differential-scale 렌즈가 쓴 범위와 안 겹치는 별도 시드)해 spurious_unsat=0 재확인 + 사전-존재 spurious-sat 1건을 base 바이너리 재생으로 무관 확정. 잔차 2건(#423): item1 disequality 체크가 `distinct`보다 좁음(Eq-negative 경로는 DT-sort 게이트, Int필드+simplify:false는 미해결), `is-c0(x)∧is-c0(y)∧distinct(x,y)` spurious-sat(사전 존재, 무관 버그). 부수: `Context::set_option("simplify",..)`가 사전부터 no-op임을 발견(미수정, CLI 옵션 담당자 몫).

**#423 CLOSED (2026-07-10, `0.2.4-redesign` `783900e`+`606f679`, plan-mode 승인→실제 채팅 승인 확인 후 워크플로)**: #422가 남긴 2건 + 인접 위생 수정 1건. **항목1**(`distinct`보다 좁은 disequality 게이트) — `Eq` 부정분기의 `both_dt_sorted` 게이트를 제거해 `Distinct` 양의분기와 대칭으로 맞춤. **재확인 과정에서 기존 문서보다 심각함을 발견**: `simplify:false`일 때만이 아니라 OR-분기 평가자(`dt_items_force_conflict`)엔 SAT-레벨 폴백이 아예 없어 **기본 설정에서도** spurious sat 재현됨(z3/cvc5는 unsat) — 수정 후 해소. **항목2**(nullary constructor tester가 등식을 함의 안 함, 신규 미문서 갭) — nullary ctor는 값이 유일하므로 `is-C(arg)⟺arg=C()`가 진짜 동치; `oxiz-core`에 `&self`-only 캐시조회 `find_nullary_dt_constructor_term` 신설 + `check_dt.rs`에 `record_dt_nullary_tester_eq_fact`로 양극성 모두 등식/부등식 승격. 컨스트럭터 이름은 항상 문자열 비교(#399의 cross-interner Spur 오염 교훈 재적용). **항목3**(별도 커밋): `Context::set_option("simplify",..)`가 원래 no-op였던 것을 실제 배선(`--preset minimal`이 이제 진짜 효과 있음 — 부작용으로 해당 preset의 실질 완전성이 낮아짐, 의도된 trade-off로 명시). **계획 프로세스 교훈 실제 적용**: `ExitPlanMode` 결과 문구("User has approved exiting plan mode")를 실승인으로 오인하지 않고, 계획을 채팅에 직접 요약 제시 → 사용자의 실제 "이대로 진행해" 응답을 받은 뒤에야 워크플로 실행([[feedback_planmode_approval_not_automatic]] 최초 실전 적용 사례). **검증**: 3-렌즈+최종통합 독립 재실행(1500시드) 전부 spurious_unsat=0. 필수 테스트 갱신 확인됨(버그를 "정상"으로 assert하던 기존 테스트 2건을 Sat→Unsat로 실제 반전, 스테일 방치 아님). **부수 발견 3건(신규 티켓 후보, 전부 안전방향)**: (a) `TermKind::DtConstructor` 캐시 키에 sort 성분이 없어 동명 nullary ctor를 가진 서로 다른 datatype 간 잠재 충돌 가능(Var가 이미 #400에서 `(Spur,SortId)`로 고친 것과 동일 버그 클래스) — 현재는 착취 불가 확인됐으나 근본 미해결; (b) N-ary(3항 이상) `distinct`가 `dt_diseq_pairs`로 분해 안 됨(#422부터 의도적 범위外, 항목2 파생 등식과 조합 시에도 마스킹됨 재확인); (c) `mk_eq`가 리터럴-리터럴 등식(`(= 7 9)`)을 항 생성 시점에 상수접기해버려 `Eq` 노드 자체가 안 남는 경우 collect_dt_constraints_v2가 아예 못 봄. [[feedback_planmode_approval_not_automatic]] [[feedback_pop_scrub_cache_bug_class]]

**#424 CLOSED (2026-07-11, `0.2.4-redesign` `b779147`+`b478199`, 계획→실제 채팅 승인→plan-mode 오해 재발견→재승인→워크플로)**: #423 적대적 검증이 남긴 부수 발견 3건. **항목1**(동명 constructor/selector가 서로 다른 datatype에 걸쳐 충돌 — TermManager 캐시에 sort 성분 없음+파서 `dt_constructors`/`dt_selectors` 단일항목 맵) — z3로 실측: 선언 자체는 z3/cvc5도 허용하지만 모호한 bare 참조는 z3가 `(as c0 A)`류 명시적 한정을 요구하며 거부. **완전한 오버로드 해소 대신 선언 시점 거부**로 결정(#422 well-foundedness 체크와 동일 패턴, z3보다 다소 엄격하지만 실사용 SMT-LIB에선 극히 드묾). **항목2**(양의 N항 distinct 미분해) — `args.len()==2` 전용 암을 3항 이상 양의극성으로 일반화, 모든 쌍을 `dt_diseq_pairs`에; 부정극성 N항은 그대로 미처리(진짜 논리합). **항목3**(리터럴 vs 리터럴 강제-동일 미검출, `simplify:false`) — closure의 injectivity 분해가 `Eq` 항을 생성 안 해서 `mk_eq` 상수접기가 애초에 무관함을 재확인; `DtEqualityClosure`에 `has_intrinsic_literal_conflict` 신설(클래스별 IntConst/RealConst/BitVecConst-동일폭/True+False 리터럴 비교, **클래스별 변수는 반드시 루프 내부 스코프**여야 함 — 구현자가 실제로 hoist 버그를 주입해 회귀테스트가 진짜 잡아내는지 mutation-test로 증명). **적대적 검증이 실제로 진성 P0(false-rejection)을 잡음**: `(declare-datatype A ((c0))) (reset) (declare-datatype B ((c0))))`가 z3/cvc5는 sat인데 항목1 수정 직후엔 오거부됨 — `Command::Reset`이 파싱은 한번에 다 되고 실행이 나중이라 `dt_constructors`/`dt_selectors`가 reset에 안 지워졌던 것; fixup 단계가 `parse_command`의 `"reset"` 암에서 두 맵을 즉시 클리어하도록 수정, 재검증 통과. 최종통합이 또다시 1800시드 독립 재실행(0 불일치) 후 커밋 2개로 분리 랜딩. **세션 프로세스 사고 1건**: 첫 워크플로 실행 시도에서 `ExitPlanMode`가 실제로는 **거부**로 반환됐는데 이를 사용자의 코멘트("PEG 파서 장기 고려")로 승인됐다고 오판해 바로 실행 → plan mode 플래그가 세션에 안 풀린 채 워크플로 하위 에이전트에 전파되어 구현이 전혀 안 되고 계획만 재작성된 채 종료(토큰 낭비, 무해). `ExitPlanMode`를 재호출해 진짜 수락(전문 반환) 확인 후 재실행해 정상 완료. [[feedback_planmode_approval_not_automatic]] [[feedback_pop_scrub_cache_bug_class]] [[oxiz_parser_peg_rewrite_consideration]]

**verus-fork 2026-07-13 재스윕 GREEN — #406~#424 전체 재확정, 원장 145/50/10**: `2026-07-12` adsmt→verus-fork 핀 갱신 회신(`d29999d`, AD1 `main` `8ad8ae2`)에서 "05-05 `#406`은 verus 노출 없음" 합의가 `#418`~`#424`(그 이후 추가된 패턴) 기준으로는 미재검증임을 지적하고 재스윕 요청 → verus-fork가 유휴 머신 단독으로 핀드 매니페스트를 새 핀(`b478199`) 대비 재실행, **`2026-07-13-b478199-resweep-145-verified-no-new-regressions-saturators-delivered.md`** 회신: **원장 143→145**(+2, `datatypes-match-3/ob01`+`ob03` — dm3/ob01 419-assert 전체 렌더가 corpus-triage README의 "정직한 잔차 (b)"로 남아있던 그 행, `#418`-`#424` 구간에서 닫힘 확인), unknown-or-bail 53→50, 신규 회귀 0(기지 ob06만 유지), negative controls 4/4 정확(신규 acyclicity/injectivity/cover/distinct-분해 과제약 없음 재확인). **"verus 노출 없음"을 `#406` 단독이 아니라 `#406`~`#424` 전체 기준으로 재확정.** 요청했던 90s saturator 이름 파일(2026-07-05 owed)도 납품 — phase-2 시점 9-name 리스트는 이름 보존 안 됐다는 정직 노트와 함께 현재 핀 기준 10행(`dm2×5/sv2×4/dm3×1`)으로 대체 납품, `corpus-saturators-2026-07-13-b478199.txt`. `corpus-triage/README.md`에 145/50/10 갱신 + 잔차 (b) CLOSED 반영 완료. `#405`는 순번 유지 그대로 동의. 다음 액션: 항-성장 스로틀/관련성 게이트 슬라이스 landed 시 ob06+saturator 10행을 첫 회귀-핀으로 verus-fork가 재실행.

**#404 PHASE 1 (2026-07-04, fork `cf878ab`) — 코퍼스 튜닝 캠페인 개시**: (1) **삼각측량** — 105 타깃 행(pinned unknown 68 + timeout 4 + 전환된 #403 33) 렌더를 z3로 재분류 → **z3=unsat 완전성 타깃 72행**(전 가족: seq-vstd 19, fuel-recursion 12, datatypes-match 12, divmod 8, linear-euf 8...); z3-noverdict 25 = abduct 계열(설계상 비검증) + 렌더 특이 소수. 도구 표준화: `adsmt-delegate/corpus-triage/`(triage_unknowns.py + ddmin_render.py + 최소 코어 2종). (2) **첫 벽 국소화** — dm3/ob01 ddmin 419→5 asserts = verus **decreases-check** 형(check_decrease_height 정의 ∀∀ + 가드된 per-field height 공리); 14줄 손-코어(비해석 소트 — datatype 무관)로 재현. (3) **프론티어-워터마크 기아 FIX (fork `cf878ab`)**: round의 e-match를 `continue` 분기(CDQI 충돌/∃/model-completion skip)가 건너뛰어도 말미 일괄 sweep이 `scanned[qi]`를 전진 → **트리거가 갓 추론된 라운드에 CDQI가 인스턴스 1개를 내면 기존 ground 시드가 영구히 프론티어 밖** (`ematch_all→0`, 수정 후 8). 수정 = 워터마크 전진을 e-match 분기 안으로(소비 시점에만); emit이 (qi,tuple) dedup이라 재스캔 멱등. 회귀 `frontier_survives_a_cdqi_short_circuited_round`(구 코드 FAIL 확인). (4) **다음 초크포인트 격리(OPEN)**: 같은 코어에서 필요한 인스턴스(x↦unbox e)는 라운드 1 CDQI가 이미 방출 + 호스트가 `[¬Q, φ]`로 assert하는데 **다음 ground 재해결이 여전히 SAT** — lemma 절이 재해결에 안 물리거나 Q-리터럴 연결 단절(호스트 측; 명시 `:pattern`은 솔버측 e-matching 경로로 폐쇄됨 → 트리거 기계 무죄). 재현 = corpus-triage/decreases-check-core.smt2 + OXIZ_MBQI_DBG=1(신규 계측). 이 한 곳이 뚫리면 decreases-check 형이 걸린 datatypes/fuel 계열 다수가 함께 열릴 가능성.
