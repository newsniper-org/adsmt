---
name: oxiz-mbqi-rewrite
description: "Clean-room MBQI/quantifier engine rewrite (~/oxiz-mbqi) to replace OxiZ's unsound-by-patching one"
metadata: 
  node_type: memory
  type: project
  originSessionId: 32a1dc0d-7730-4862-8df4-6958199ce84f
---

**2026-06-19 (later) — completeness frontier batch 2 on branch `0.2.4-redesign`: deadline hard-bound + ∀∃-anchorless + dense-real range completion; +1 latent soundness hole found & fixed. Corpus 168/168 terminate, agree 157→159, SP-SAT/UNSAT 0.** Four commits: `4f2d312`(array extensionality/read-over-write recognizers) `5afa4eb`(deadline) `2a03f32`(∀∃ skolem-witness) `e6adb88`(dense-real range completion + design doc + the soundness fix). User handles push + parent submodule bump.
- **Deadline hard-bound (`5afa4eb`):** the MBQI loop checked `mbqi_deadline` only BETWEEN rounds; a single non-terminating ground `solve_with_theory`/`solve_with_hooks_inner` (a dense-real f-tower instance set) overshot the cap unbounded. Added `Solver::set_deadline(Option<Instant>)` to oxiz-sat + a throttled (`%1024`) poll in both solve loops → sound `Unknown` on expiry. oxiz-solver MBQI loop pushes `mbqi_deadline` into the SAT core. This made the corpus gate DETERMINISTIC (was hanging at 166/168 → real_lipschitz/real_interp now bail to Unknown at the 1s cap; freed CPU also raised agree).
- **∀∃ anchorless = fresh-Skolem equality-witness recognizer (#8, `2a03f32`):** after skolemization `∀x.∃y.g(x)=f(y)` → `∀x. g(x)=f(sk(x))` (`skolem_test`→Sat). `try_skolem_witness_eq`: body `∀x̄.(= L R)` where a side is `f(…sk(x̄)…)` with `f` single-quantifier, `sk` a FRESH skolem (name `sk!…`, single-quantifier) covering ALL bound vars, and `L` is `f`-free → set `f(sk(x̄)):=L(x̄)` at fresh witness points = conservative extension over `f`,`sk`. The `quant_count[f]==1` gate is the firewall (a universally-bounded `f` could forbid the witness → decline).
- **Dense-real constant range completion (#4b, `try_range_completion`, `e6adb88`):** body `∀x̄.⋀ cmp(f(x̄),cᵢ)` bounding one `f` against ground constants → interval `[lo,hi]`; dense order ⇒ nonempty interval has an interior point → `else`-value `δ≡k∈[lo,hi]` (z3's `as-array`/`else` model rep). `real_bounds`→Sat. The Real-rational two-sided generalization of the Int single-comparison recognizer 4 (`term_to_rational`/new `resolve_eq_rational`).
- **SOUNDNESS LESSON (reusable) — the symbolic-point accounting gap.** The FIRST cut of range completion (and a PRE-EXISTING latent hole in `try_function_completion`) was UNSOUND: a constant-completion recognizer verifies only LITERAL ground points (`ground_apps`), but `f(c)` for a FREE CONSTANT `c` (`real_unsat`: `∀x.f(x)≤1` ∧ `f(c)>1`) is NOT recorded as a ground point (its arg is a Var) → its constraint MISSED → spurious Sat. FIX = the **accounting gate** (same as `try_monotone_extension`'s): `global_occ[f] == (body f-occ via count_syms) + |ground_apps[f]|`; any shortfall = an unaccounted symbolic application → decline → engine instantiates & refutes (real_unsat recovers z3's `Unsat`). Added to BOTH `try_range_completion` and `try_function_completion`. **Whenever a recognizer completes `f` to a default it MUST account for EVERY `f`-application, not just literal-ground ones — `quant_count==1` alone is insufficient (it gates other QUANTIFIERS, not symbolic ground apps).** Caught by the corpus gate; int analog is a soundness control test.
- **Design roadmap: `docs/design/REAL_MODEL_CONSTRUCTION.md`** — surveys z3 (MBQI over default-extended `as-array`/`else` UF model) + cvc5 (CEGQI + `--fmf-bound` guard reasoning), distills the "default-value model + symbolic verification" frame, maps the 5 UFLRA reals to recognizers: §3.1 range completion DONE; §3.2–§3.5 DESIGNED (then DONE — see next entry).

**2026-06-19 (later·2) — design-doc §3.2–§3.5 ALL IMPLEMENTED on `0.2.4-redesign`. Corpus agree 159→163, 0 spurious throughout.** Five more commits: `3e12459`(§3.2) `913b239`(§3.3) `c4b4e59`(§3.4) `fc442f9`(§3.5) `de8afdb`(doc). Each = one corpus real Unknown→Sat + a positive test + a deliberate soundness control (a near-miss that must stay Unknown/Unsat). User handles push + parent submodule bump.
- **§3.2 bounded oscillation (`try_bounded_oscillation`, 4c) → `real_lipschitz`.** `∀x,y∈G. |f(x)−f(y)|≤B` (two-sided `f(x)−f(y)≤B ∧ f(y)−f(x)≤B`). Constant `else`-value `δ=min`; sat iff pinned `f`-values span `≤B` (max−min≤B). `B≥0` required (diagonal `x=y` forces `0≤B`). Single-quantifier + accounting gates.
- **§3.3 variable-relative bound (`try_var_relative_bound`, 4d) → `real_archimedean`.** `∀r. f(r) ⋛ r` (e.g. `ceil(r)≥r`). KEY: for NON-STRICT `≥`/`≤` the IDENTITY default `f(r):=r` makes the body reflexively true (`r≥r`) — guard-IRRELEVANT, no constant/guard-parse needed; verify each pinned `f(p)=v` against `rel(v,p)`. Strict `>`/`<` (needs guard sup/inf) deferred.
- **§3.4 `f≡g` identity collapse (`try_commuting_functions`, 4e) → `real_composition`.** `∀x. f(g(x))=g(f(x))` over two DISTINCT uninterpreted unary fns. RELATIONAL model: merge `g≡f` → `f(g(x))=f(f(x))=g(f(x))` structurally (guard-irrelevant). Sound when both single-quantifier + fully accounted AND agree on every shared pinned point (else the merge breaks a ground fact).
- **§3.5 layered bounds (`collect_layered_bounds` Pass 7 + `layered_ok` set, 2b) → `real_interp`.** THE multi-axiom capstone + the ONLY recognizer that RELAXES the single-quantifier firewall: `f` is constrained by TWO universals (`∀x≥0.f(x)≥0` + `∀x∈[0,10].f(x)≤g(x)`) plus `g`'s guarded affine def `∀x∈[0,10].g(x)=2x+1`. Can't fire per-quantifier, so the GROUP is classified + its canonical model `f≡L`(max const lower), `g≡affine` VERIFIED ONCE in Pass 7 (like `definitional_quants`), marking every member; `eval_forall` certifies by membership. Soundness: unique affine def covering the `f≤g` region (`region_subset`), `L≤min(affine)` over the region (`affine_min_over` — a dipping affine like `g=2x−15` → infeasible → decline), ground `g`=affine + ground `f` in-band, and `f`,`g` LOCAL to the group (`group_accounted`). New `parse_affine`/`guard_bounds` linear-real parsers.
- **Whole §3.2–§3.5 batch: 0 spurious sat/unsat, all 8 new soundness-suite tests green (positive + control each), adjacent ground-soundness suites green.**
- **`nested_quantifiers` ALSO closed (`636d4d9`, agree 164→165; doc `b8c1104`) — uncovered + fixed a CORE substitute bug.** `∀x.∃y.∀z.(z≥y⇒f(x,z)≥0)` (triple ∀∃∀). CLI-localized (eval_forall debug showed only the OUTER ∀x is visited, body=nested ∀z; threshold debug showed the guard was `z≥y` with **y a LEAKED free Var**, not `sk(x)`). ROOT = **`oxiz-core` `substitute_cached` left `Forall`/`Exists` UNCHANGED** (catch-all `Some(_)=>id`) → skolemizing the inner `∃y` (`substitute(∀z.body, {y↦sk(x)})`) was a NO-OP on the nested `∀z` → y leaked free; ALSO means nested-quantifier INSTANTIATION kept its bound var (latent). FIX = capture-avoiding `Forall`/`Exists` arms (`subst_under_binder`: drop shadowed keys, BAIL body-unchanged on capture, preserve patterns) — `crate::interner::Spur`+`crate::sort::SortId` imports added. Broad-validated (substitute drives instantiation): oxiz-core 1168, oxiz-sat 619, oxiz-theories 1174, oxiz-solver 435, z3-differential `ground_soundness_fuzz` all green. THEN the recognizer: with the skolemized `∀x,z.(z≥sk(x)⇒f(x,z)≥0)` (sk FRESH), `try_threshold_guard` (2c) — push the fresh threshold above all finitely-many ground points ⇒ pinned points fall in the EXCLUDED region `z<sk(x)` (guard false, vacuous), constrained region `z≥sk(x)` holds only fresh points where f is free ⇒ conservative extension over f,sk, sound `Sat` WITHOUT touching ground facts. Flattens the nested ∀ chain (`flatten_foralls`); requires a fresh-skolem lower-threshold on a bound var z (z∉sk's args), a feasible single-`f` range consequent with z among f's args, f single-quantifier. Infeasible consequent ⇒ `∀z.z<sk` unsat ⇒ declines (control). soundness suite 68/68. **PROCESS: the "fix substitute properly in core" path beat papering over the leak — it also fixed latent nested-instantiation.**
- **`array_sorted` ALSO closed (`5624118`, agree 165→166; doc `a8f073f`) — Z3-PARITY CORPUS COMPLETENESS FRONTIER NOW CLOSED.** `∀i,j.(0≤i ∧ i≤j ∧ j<n)⇒a[i]≤a[j]` (n=4), cross-var TRIANGULAR guard. `bounded_var_domains` boxed only from `var⋈const`, so the cross-var `i≤j` pinned nothing (i no const upper, j no const lower) → un-instantiated → Unknown. FIX = `collect_int_bounds` also records `var⋈var` relations (`rels: Vec<(Spur,Spur,Cmp)>`, `Cmp` made Copy); `bounded_var_domains` propagates them TRANSITIVELY to a fixpoint (`i≤j`+`j≤3`⇒`i≤3`, `0≤i`+`i≤j`⇒`j≥0`) → box `[0,3]²`. Each derived bound is IMPLIED by the guard ⇒ box is a SUPERSET of the guard region ⇒ bounded enumeration is EXACT (out-of-region tuples vacuous via guard) ⇒ #280 re-solve confirms Sat. SOUNDNESS = box must COVER: unsorted control (`a[0]=5>a[1]=3`) → box includes (0,1) → engine refutes → Unsat (never spurious sat). soundness suite 70/70; differential fuzz + adjacent green. **CORPUS: 168/168, 166 agree, 0 spurious — EVERY z3=Sat case agrees; only `forall_exists_unsat` (z3=Unsat) stays the sound Unknown.** Engine-only changes (no core touch this commit).
- **SESSION ARC TOTAL (clean-MBQI completeness, this whole continuation): corpus agree 157→166, 0 spurious throughout.** Closed: ∀∃ skolem-witness eq, dense-real ×5 (bounds/lipschitz/archimedean/composition/interp), real_fixed_point, nested_quantifiers (+ core substitute-into-quantifier fix), array_sorted. Commits on `0.2.4-redesign`: deadline `5afa4eb`, skolem-witness `2a03f32`, range `e6adb88`, §3.2 `3e12459`, §3.3 `913b239`, §3.4 `c4b4e59`, §3.5 `fc442f9`, real_fixed_point `486abe2`, nested_quantifiers `636d4d9` (+oxiz-core), array_sorted `5624118` + 6 doc/design commits. **All UNPUSHED + oxiz-core changed (nested_quantifiers commit) → user push + parent submodule pointer bump includes core.** Frontier remainder = OFF-corpus (general unbounded-∃, richer real/array reasoning) — the z3-parity corpus itself is complete.
- **`real_fixed_point` ALSO closed (`486abe2`, agree 163→164; doc `3a4d472`).** `∀x∈[0,1].0≤f(x)≤1` (guarded range) + `∃x∈[0,1].f(x)=x` + f(0.5)=0.5. TWO blockers, empirically localized via CLI (rfp with/without ∃): (1) `try_range_completion` only matched UNGUARDED bodies → now PEELS an optional `(=> guard …)` (out-of-guard args vacuous; guard has no f-app so accounting unchanged) → guarded range fires. (2) the ∃ skolemizes to `f(sk)=sk` (sk∈[0,1]) — a SYMBOLIC f-app that the accounting gate rejected (like real_unsat's `f(c)`), BUT here IN-RANGE (`f(sk)=sk`, sk bounded ⇒ value stays in range). Pass 8 `collect_skolem_fixedpoints` records each `(= (f sk) sk)` (sk a fresh `sk!…` const) + sk's sibling bounds; range completion accounts for them + folds sk's bounds into a feasible interval for the constant `k` (since `f(sk)=k` forces `sk=k`). Out-of-range fixed-point (`∃x≥2.f(x)=x` under `f≤1`) folds to empty → declines → engine refutes (control test); non-eq-pinned `f(c)>1` is not a self-fixed-point → still trips plain accounting → real_unsat stays Unsat. soundness suite 66/66. REMAINING `z3=Sat` Unknowns (design-doc §5): `nested_quantifiers` (∀∃∀), `array_sorted` (cross-var triangular `i≤j` guard, not an axis box).

**2026-06-19 — z3-parity corpus sweep exposed TWO session-introduced spurious-SAT (both FIXED) + a process lesson. The 168-case `clean_mbqi_corpus` gate had been ABORTING before its panic-classification on an OOM (`UFLIA/congruence_closure.smt2`, unguarded congruence ∀ → unbounded-Vec matching loop, 2.68GB@4GB-cap / 20GB@30GB-cap — task #276); so the spurious verdicts AFTER it (alphabetically) were never seen → "0 spurious" was a partial-run undercount (cf. rc.38 "partial validation hid it a full cycle"). Skipping that one case, the real tally was 4 spurious-UNSAT + 2 spurious-SAT.**
- **#260 regression FIXED — `∀x.f(x)≥0 ∧ f(5)<0` → spurious Sat** (the case line ~46 below mis-filed as "benign MBQI completeness"; it is a genuine UNSOUND spurious-sat, z3=Unsat). BISECT: sound `unsat` at 38019b0 → `sat` at e516a55 (#260 arith function-completion). ROOT: `try_function_completion`'s VERIFY scanned the MODEL's `assign` for pinned `f`-points, but the model POISONS an inequality-constrained UF application to a DEFAULT `0` (`int_of(f(5))=0` while `f(5)<0`) — so `0≥0` "verified" and certified, hiding the refuting instance. Equality-pinned points are faithful; inequality/unconstrained are not. FIX (`clean_mbqi.rs`): verify against TOP-LEVEL equality pins + IntConst literals ONLY, never the model — `resolve_eq_concrete` follows `(= (f t̄) k)` chains (and `(= (f 7) c)`,`(= c 5)`) to a literal; `CompletionFacts.ground_apps` (per-fn ground applications) + `.eq_pins` (top-level `(= a b)`). A point not eq-pinned to a concrete ⇒ NOT certified → enumerate → theory refutes. The pin RHS is also resolved eq-pin-only (a const `d` with only `d<0` is poisoned too). Tests: `function_completion_respects_an_inequality_constrained_ground_point` / `…_a_chained_inequality_ground_point` / `…_certifies_through_an_equality_chain` (uf_sort 21→24).
- **#277 regression FIXED — `UFLIA/pigeonhole_3` (`∀i,j∈[0,n). i≠j ⇒ hole(i)≠hole(j)`, n+1 holes in [1,n]) → spurious Sat.** BISECT: sound `unknown` at 38019b0 → `sat` at 85c39ba (bounded-guard finite-domain). ROOT: `engine::round_with` saturation's **finite-exhaustion shortcut** (bounded ∀ with all-Some var_domains ⇒ auto-"satisfied") trusted "Saturated ⇒ ground solve consistent" — but the INCREMENTAL CDCL(T) MISSES a GLOBAL conflict across the emitted instances (4 distinct in 3 holes; the DUAL of the documented incremental spurious-UNSAT — a FRESH single-shot of the same `{ranges ∪ 6 implication-instances}` is correctly Unsat). FIX (`engine.rs`, USER chose option A): REMOVE the finite-exhaustion verdict claim → bounded ∀ defers to `eval_forall` → sound `Unknown`. The anti-tower bounded enumeration (`var_domains`/`enumerate`) is KEPT (no hang). **Trade-off (user-approved): ackermann + bounded-grid sat→Unknown** (85c39ba's decisive-sat feature lost; no-hang retained). `bounded_guard_instantiation` tests updated to expect the sound Unknown. NB: `verify-on-Saturated` (re-solve via `Solver::with_config` sharing the manager, à la `verify_clean_unsat`) was tried and FAILED — the shared-manager re-solve ALSO returns Sat on the provably-unsat instance set (only a truly-fresh `Context` is reliable); so `verify_clean_unsat` itself likely silently downgrades real unsats to Unknown. Completeness recovery (ackermann sat) needs a truly-fresh render+reparse verify OR fixing the incremental conflict-miss — deferred.
- Corpus after #260+#277 (skip congruence OOM): **SPURIOUS SAT 0** (was 2), agree 139→135 / unknown 22→28 (all sound, the #277 trade-off). The **4 spurious-UNSAT** (AUFLIA/array_max·array_permutation·array_search + QF_NIRA/qf_nira_basic_sat) are PRE-EXISTING (unsat at 38019b0 too) → **task #278**, NOT this session.
- **#278 — the 4 pre-existing spurious-UNSAT are TWO independent bugs, both FIXED:** (Bug Y, qf_nira) `dispatch_nl_solver` routed `QF_NIRA` through the INTEGER nlsat (`is_nia = NIA || (NIRA && !NRA)`), INTEGERIZING the real var — `(* x x)=4 ∧ 1.0<y<2.0` (each SAT alone, no shared var) made `y` an int → `1<y<2` no int solution → spurious unsat. FIX = real-sort gate (`term_mentions_real_sort`): a mixed NIRA problem declines the NIA dispatch → CDCL(T) → **correct sat** (pure-int NIRA still dispatches, `x*x=3`→unsat preserved). `check_nlsat.rs 2b4740e`. (Bug X, 3 array) the clean engine registered `∃` as an instantiable `Quant` and ground-instantiated it like a `∀` (`Q ⇒ φ[i↦t]`); at `i:=42` the guard `0≤42≤1` is false → `Q ⇒ false` = `¬Q` refuted the asserted `∃` → spurious unsat. An `∃` only needs SOME witness; sound discharge = host-side skolemization, which the no-fabrication firewall forbids INSIDE the engine. FIX = `round_with` SKIPS existentials in all strategies + the saturation check returns the sound `Unknown` for any active `∃` (never `eval_forall`, a `∀`-only recognizer). 3 array → **sound Unknown**; `∃`-completeness (→ sat via bounded-`∃` disjunction `Q⇒⋁φ[t]` or skolemization) deferred to **#279**. `engine.rs bfc727c`. Corpus (both #278 fixes, STABLE build): **SP-UNSAT 4→0, SP-SAT 0**, agree 135→136 (qf_nira→sat), unknown 28→31 (3 array). collateral 0 — an earlier "18-case regression" was a sweep run CONCURRENT with a rebuild (race artifact), not real.
- **#279 — bounded-`∃` completeness recovered (`b5d67ef`).** #278's `∃`-skip was sound but made a bounded `∃` the sound `Unknown`. A bounded `∃x̄.(guard ∧ φ)` over an integer-box guard is EXACTLY the finite disjunction `Q ⇒ ⋁_{t̄∈D} φ[t̄]`, so the engine emits that ONE lemma and the ground solver decides. New `TermLang::mk_or`; `bounded_var_domains` reads the `∃`'s `(and guard… φ)` body too (And path is `∃`-only — restricting a non-guarded `∀` would be unsound); `Engine::emit_existential_disjunction` substitutes the FULL body per box tuple and ORs them (out-of-range tuple's guard is false → self-excludes). At saturation a bounded `∃` is satisfied (disjunction satisfiability is the EASY direction, immune to the incremental conflict-miss that keeps `∀`/`(C)` at `Unknown`); an UNBOUNDED `∃` stays sound `Unknown`. EXACT ⇒ complete BOTH ways (refuted witness disjunction → genuine Unsat). Corpus: array_search+array_permutation → sat (agree 136→138, unknown 31→29), SP 0. array_max keeps a bounded `∀` → still Unknown (the `∀` verdict = the deferred **②/(C)**: recovering a bounded-`∀` Sat soundly needs a RELIABLE ground verdict — neither the incremental solver nor the shared-manager re-solve provides one, both miss the pigeonhole conflict via EUF↔LIA combination incompleteness — so it needs (A) render+reparse or a combination fix, NOT the engine-internal model check I'd wrongly suggested).
- **#280 — bounded-`∀` Sat recovered, SOUNDLY, via a logic-carrying re-solve (`c6a7f74`); #277's `Unknown` was over-conservative.** A bounded-finite `∀` is fully captured by its box conjunction of emitted instances, so the engine reaches `Saturated` again and the SOLVER confirms it with a single-shot ground re-solve of `{ground assertions ∪ instances}` (fresh `sat`→`Sat`, fresh `unsat`→`Unsat`, else `Unknown`). **ROOT CAUSE of the earlier failed verify: `logic` is a `Solver` FIELD, NOT in `SolverConfig`** — so `Solver::with_config(self.config.clone())` left the verifier's logic `None`, and a logic-LESS solve does not wire up the EUF↔LIA combination, so it MISSED the pigeonhole conflict and returned `sat` (useless verify; this also silently downgraded some real unsats in `verify_clean_unsat`). FIX = the shared `fresh_ground_resolve` calls `verifier.set_logic(self.logic…)`; `verify_clean_unsat` routes through it (latent bug fixed) + new `verify_clean_saturated` is its `sat`-direction dual, gated on `Saturated` with emitted instances. **No render/reparse needed — one `set_logic` line, the most efficient path (the user asked for render+reparse OR an equivalent; this is equivalent-and-cheaper).** Corpus: agree 138→143, unknown 29→23, SP-UNSAT/SAT 0. ackermann + bounded grids → `sat`; **pigeonhole_3 IMPROVES from `Unknown` to the correct `unsat`** (the re-solve refutes the box — better than #277's `Unknown`); 1 case OxiZ now decides where z3 was `unknown`. Residual (→ **#281**): array_max keeps a symbolic-bound guard `0≤i<n` (n=5 a const, not a literal) so `bounded_var_domains` doesn't see it as finite; + ~21 genuine completeness Unknowns (general quantifier / UFLRA real / array reasoning the clean engine doesn't do, sound by design).
- **#276 — congruence_closure OOM FIXED by recognizing the axiom as a tautology (`2231176`).** OOM site = `EufSolver::propagate` (euf/solver.rs:626, a single 2.68 GB Vec): the EXPLICIT congruence axiom `∀x,y.(= x y) ⇒ (= (f x)(f y))` is unbounded (equality guard, no int box) → enumerated over the whole ground index → every instance adds `f(s)`,`f(t)` → an `f`-of-`f` tower that blows the EUF propagate worklist (`set_timeout_ms` only polls between rounds). The axiom is REDUNDANT — a function maps equal inputs to equal outputs, so `(⋀ aᵢ=bᵢ) ⇒ (= (f ā)(f b̄))` is VALID and UF's built-in congruence closure already enforces it. `body_is_valid` now recognizes it (`is_congruence_axiom`: consequent `(= (f ā)(f b̄))` over the SAME uninterpreted `f`, every arg pair covered by the guard's equalities) → `eval_forall` `Some(true)` → engine SKIPS instantiating → no tower, no OOM, `sat` in ms. SOUND: built-in EUF congruence still fires (`a=b ∧ f(a)=1 ∧ f(b)=2` → unsat). **This is the in-engine generalization of the user's "use the solver's built-ins" idea — no need to rewrite the corpus file, the pattern is recognized generally.** Full corpus (OOM-skip removed): **168/168 run, agree 144, SP-UNSAT 0, SP-SAT 0, 1 stronger-than-z3.**
- **#281 — completeness frontier, batch 1 (`7903a4d`): +5 recovered, all sound.** (a) ORDER-TRANSITIVITY recognizer (`is_order_transitivity`): `(A≤B ∧ B≤C) ⇒ A≤C` is valid (≤ transitive) → `body_is_valid` tautology → skip instantiating → `trans_simple` sat. (b) SYMBOLIC-BOUND resolution: `collect_int_consts` gathers the top-level `(= const literal)` map (`(= n 5)`⇒`n↦5`), the host (`OxizHost::with_int_consts`) substitutes it into the guard before `bounded_var_domains` reads the range, so `∀i.(0≤i<n ⇒ …)` becomes a bounded `[0,n-1]` ∀ → instantiated + #280-verified → array_max, sum_bounds, array_forall_init, array_partition sat. Corpus agree 144→149, unknown 23→18, SP 0. Still Unknown (deeper): array_sorted (cross-var `i≤j` triangular guard ≠ box), 8 UFLRA reals (need a REAL bounded-domain machine; the box is int-only), and the genuine quantifier-reasoning / UNBOUNDED-∃ cases (surjective, forall_exists, skolem, nested). **DUALITY NOTE (user-observed):** `∃x.∃y.P ≡ ¬(∀x.∀y.¬P)` — the bounded-∃ disjunction (#279) IS this on a FINITE domain (`∃x∈D.φ ≡ ⋁_{t∈D}φ[t]` via de Morgan). For UNBOUNDED ∃ the `¬∀¬` rewrite does NOT bypass the engine's no-fabrication firewall (a negated/false `∀` still needs the witness where `¬φ` fails = the `∃` witness), so unbounded ∃ needs host-side SKOLEMIZATION (mint a fresh witness const) — top-level positive ∃ (surjective) is recoverable that way; `∀∃` nested ones become `∀`-over-skolem-functions (still hard).
- **SESSION SUMMARY (the whole rc.39+ MBQI completeness/soundness arc): corpus went from 4 spurious-UNSAT + 2 spurious-SAT (masked by the OOM) → 168/168 clean, ALL spurious 0, no OOM, agree 144→149.** 8 OxiZ commits on `0.2.4-experiment/iteration-recognizer`: `b3d982c`(#260 fc eq-pin) `edfd749`(#277 finite-exhaustion drop) `bfc727c`(#278 X ∃-skip) `2b4740e`(#278 Y NIRA real-sort) `b5d67ef`(#279 bounded-∃ disjunction) `c6a7f74`(#280 bounded-∀ logic-carrying re-solve) `2231176`(#276 congruence recognizer) `7903a4d`(#281 transitivity + symbolic-bound) — user handles merge→redesign + parent submodule bump. Open follow-up = **#281** remaining frontier (cross-var bound, UFLRA-real bounded machine, unbounded-∃ skolemization). [[feedback_long_test_runs]] — corpus is a user `!` gate; validated via a throwaway `examples/repro_clean` + skip-the-OOM sweep loop (NB: never run a corpus sweep while editing/rebuilding — concurrent `cargo run` reads an inconsistent binary).

**2026-06-18 — clean engine is now the DEFAULT + SOLE quantifier engine; M3 model completion landed; both measured Unknowns → Sat. All on OxiZ branch `0.2.4-redesign` (committed locally, NOT yet pushed — user handles remotes; full `clean_mbqi_corpus --ignored` + `cargo test --workspace` are the user's `!` gates).**
- **#262 — legacy `oxiz-solver/src/mbqi/` DELETED** (~12k lines) + its tests + the dead support (`ematch_engine`/`ematch_fixpoint_step`, `register_declared_const` (0 callers), `collect_skolem_candidates`, `solver/candidates.rs` + `solver/pigeonhole.rs`, `encode::add_arith_eq_trichotomy`). `clean_mbqi` is the only path. The `clean_mbqi` config flag (and `:oxiz.clean-mbqi`) now means **instantiate-quantifiers (default true) vs ground-only**; ground-only is what `verify_clean_unsat` uses. Quantifiers are encoded as boolean proxies; the clean `Engine<OxizSig>` rebuilds its index from `self.assertions` each solve. oxiz `28932a5`.
- **#264 + #260 — M3 model-completion recognizers in `SolverModel::eval_forall`** (clean_mbqi.rs), guarded by global `CompletionFacts` over the whole asserted formula so per-quantifier `Some(true)`s compose into ONE model (witnesses built host-side, never cross into the engine):
  - **A definitional**: `∀x̄.(= (f x̄) rhs)`, `f` uninterpreted/applied to the distinct bound vars/occurring NOWHERE else ⇒ conservative extension.
  - **B pure-polarity predicate**: single-literal `(P …)` whose uninterpreted predicate occurs ONLY positively (resp. negatively) across the non-definitional formula ⇒ `P ≡ true` (resp. false). (Definitional axioms are EXCLUDED when scoring other symbols' polarity.)
  - **C single-function range completion** (#260): `(cmp (f x̄) g)`, `g` ground, `f` the sole universal constraint ⇒ complete `f` to a constant — but ONLY after VERIFYING every existing pinned `f`-application satisfies `cmp(·,g)` in the model (arith-folded). Needed (1) **arith-aware `eval_bool`** (`int_of`: fold `</<=/>/>=/=` over concrete IntConst model values; was `None` on every comparison) and (2) the **pre-enumeration skip restored** in `oxiz-mbqi` `engine::round_with` (`eval_forall==Some(true)` ⇒ skip), because `∀a.f(a)>0` otherwise diverges on an `f`-tower (`a:=f(t)⇒f(f(t))`) that never saturates. Sound: a model-falsified pinned point (f(7)=-3) is NOT certified → falls to enumeration → arith refutes.
  - Both measured Unknowns now **Sat**: trigger-free partial-order (A+B) and `∀a.f(a)>0` with f(7)=5 (C). Guards: f(7)=-3 ⇒ Unsat, f(8)=-1 ⇒ Unsat, definitional-masking-ground-contradiction ⇒ Unsat, pure-polarity-vs-negative-occurrence ⇒ Unsat. oxiz `9e5a0a2` (#264) + `e516a55` (#260). uf_sort_and_quant_soundness 21/21; full oxiz-solver + oxiz-sat green.
- **#274 §4 four-writer anti-drift spine** (oxiz-sat `trail.rs`): the three trail-SHRINKING level-writers (`backtrack_to_with_callback`/`backtrack_to_size`/`clear`) now route through ONE `retract_trail_to` primitive (the only frame-popper; `new_decision_level` the only frame-pusher) so the §4.1 lock-step invariant `|theory frames| == decision_level+1` cannot DRIFT across writers — the native realisation of the algebraic-deegen anti-drift idea on the gap behind 38019b0 / trigger-F. `backtrack_with_phase_saving` (hence the solve_with_hooks level-0 re-base) funnels through it too. New `test_lockstep_frames_equal_level_plus_one` catches under-pop. oxiz `915df71`.
- **#261 `extract_linear_terms` Ratio<i128> widening** (encode.rs): accumulate coefficients in `Ratio<i128>`, narrow to `Rational64` at the boundary (`narrow_r128`; downstream LRA is i64). An intermediate i64 overflow that CANCELS/reduces to an i64-fitting final value is recovered instead of dropped opaque; byte-identical parity on non-overflow inputs. Done on a FORK branch **`0.2.4-redesign+wider-rational`** (oxiz `520f218`), compat/parity checked, then **backported to `0.2.4-redesign`** (oxiz `c3adad7`). 60-case z3-parity adversarial + lia/nlsat/ground + full oxiz-solver green; new `i128_widening_recovers_a_cancelling_overflow_intermediate`.
- **algebraic-deegen→OxiZ fit (analysis only):** the portable-algebraic-aotjit `algebraic-deegen` FEATURE is a poor fit (OxiZ has no trace/digest/AOT stack; prelude reuse already solved by persistent push/pop). The anti-drift PHILOSOPHY is a real fit for the §4 four-writer gap → realised natively as #274 (no external crate).

---

**Clean-room quantifier-instantiation engine at `~/oxiz-mbqi`** (separate local git repo, started 2026-06-12), to be ported into OxiZ replacing the patched-but-still-unsound `oxiz-solver/src/mbqi/`.

**Why:** OxiZ's MBQI produces spurious `unsat` on the real Verus `-V adsmt` prelude via MULTIPLE independent defects, and they can't be reliably delta-debugged because the bisection oracle (OxiZ's own subset verdict) is itself unsound (a superset reports `sat` while a subset reports `unsat`). Patching is whack-a-mole. The verus-fork P0 ("prelude false-unsat") A/B/C round was followed by a second round (`.local-replies-from/verus-fork/2026-06-12-P0-ABC-fixed-but-full-prelude-still-unsat.md`): A/B/C confirmed fixed in isolation BUT the FULL prelude still unsat via triggers D (trigger-free Height partial-order, distinct-bearing) and E (11-axiom distinct-free int-width/has_type) — and behind those, more (the fuel machinery). z3 times out goal-free on the full prelude, so the gate is "not unsat" (unknown OK).

**THE invariant that makes the rewrite tractable:** the engine NEVER concludes `Unsat`. It only emits sound, guarded, ground instantiation lemmas `Q ⇒ φ[x̄↦t̄]` where each tᵢ is a real ground term from a **ground-term index** (never a fabricated witness, never a bound variable), capture-free, guarded by the quantifier's boolean literal `Q` (so a `∀` under `(=> g …)` inherits the guard). `Unsat` is then ONLY the host ground SAT+theory core's → it's real → spurious unsat is structurally impossible. Every OxiZ defect reduces to "emitted an invalid lemma": B (self-match `{i↦i}`), E (sibling-capture `{i↦x_sibling}`), D (fabricated universe `u!N`), fuel (guard dropped). [[jit_aot_replay_section_3_5]]-style: the firewall is a single central check in `instantiate`.

**Architecture:** solver-agnostic `TermLang` trait → develop/z3-cross-check standalone (toy host), then port by impl'ing the trait for `oxiz_core::ast::TermManager`. Modules: term/ground/instantiate/engine/toy. Candidate-selection STRATEGIES (all feed the same sound `instantiate`, so all inherit the invariant): enumerate (M1 baseline), trigger/e-match (M2), **CDQI** (M2 — conflict-driven, picks ground tuples whose body is false under the model; re-derives OxiZ's `generate_ground_conflicts` inside the invariant), model_based (M3 — Ge–de Moura, witnesses used ONLY to evaluate, NEVER turned into lemmas = the D-bug firewall). Corpus: (1) soundness triggers (B/E/D/fuel/partial-order + full prelude), (2) **import OxiZ's z3-parity regression** (bench/regression + lia_/uf_/mbqi_/quant_ tests) so completeness is guarded. Oracle is z3, never OxiZ.

**Milestones (dev repo `~/oxiz-mbqi`, then in-tree `external/oxiz/oxiz-mbqi`):** M1 sound core + never-unsat (`4c28ade`). M2 e-match+CDQI (`ccd65ca`). M3 model-based verification, witnesses firewalled = D-bug-impossible (`c8980bf`). M3.5 relevance gating (respects the fuel guard) + frontier mod-time e-matching (`8b23b63`). Caching comparative analysis (`032cbbd`, `CACHING-ANALYSIS.md`): adopt lemma-bank(3)+progress-aware(1)+inverse-reuse(4), defer model-state-cache(5)/lookaheads(2). **M4a** children() accessor refactor (host-agnostic). **M4b** crate brought into oxiz workspace as a member. **M4c** `OxizHost` — `TermLang` impl for OxiZ terms in `oxiz-solver/src/clean_mbqi.rs` (local newtype, orphan-safe; TermKind→view/children/patterns, substitute delegates to `TermManager::substitute`); KEY fix: a Var REACHED by the ground index is a declared constant (index never crosses a Quant) → treat as ground so OxiZ's declared-const-as-Var becomes a candidate. Validated end-to-end on real OxiZ terms (oxiz submodule `4816e36`, oxiz-solver `tests/clean_mbqi_host.rs` 2 green; oxiz-mbqi 10 green, 0 warnings). **M4d** (`6db5e57`): `SolverModel` oracle (eval_bool structural fold / eval_forall=None / is_active=quant-lit-not-false) over the SAT+theory model. **M4e-1** (`527ebc6`) **Sig/Host split**: `TermLang` split into a lifetime-free identity trait `Sig` (Term/Sort/VarName/Sym) + behavior `TermLang { type Sig: Sig }`; `Engine`/`GroundIndex`/`Quant`/`TermView`/`Binding` re-parameterized by `S: Sig`, methods take `&L where L: TermLang<Sig=S>`. Unblocks the blocker: `Engine<OxizHost<'a>>` was `'a`-typed though it only stores TermIds; `Engine<OxizSig>` is lifetime-free → lives across `check()` rounds. Toy gains `ToySig`, OxizHost gains `OxizSig`. **M4e-2** (`9f587ad`) **wired into `Solver::check`** behind `SolverConfig.clean_mbqi` (default OFF, opt-in via new pub `Context::set_clean_mbqi`): a persistent `Engine<OxizSig>` local runs the model-based phase across MBQI iterations; legacy Phase-1 e-matching short-circuited off when on; `Saturated`→Sat, `Inconclusive`/`Budget`→Unknown. **TWO REAL OxiZ BUGS found+fixed validating end-to-end:** (1) `TermManager::substitute` (`query.rs::substitute_cached`) silently NO-OP'd on `TermKind::Apply` (+Distinct/Neg/Xor/Div/Mod) via `Some(_)=>id` → `(P x)[x↦c]` stayed `(P x)`; added the FO/UF/LIA arms (1167 oxiz-core tests still green). (2) adding the guard as a unit on the `Implies` node's Tseitin proxy didn't propagate (solver escaped via result=false) → switched to the DIRECT guarded clause `[¬Q, φ]` (standard SMT form). Soundness backstop: instances whose body isn't var-free (residual from substitute's BV/string/nested-Q gap) are DROPPED (sound, incomplete). `tests/clean_mbqi_wiring.rs` 4/4: B/partial-order(D)/ground-match stay NOT-unsat (soundness), genuine ∀+¬ conflict → real Unsat (completeness). **M4e-3** corpus gate (`tests/clean_mbqi_corpus.rs`, vendored 168-case corpus, `#[ignore]`) CAUGHT A SPURIOUS UNSAT: `UFLIA/injective.smt2` (z3=Sat, clean=Unsat). Exhaustive trace concluded the CLEAN ENGINE'S LEMMAS ARE SOUND (the exact emitted instances solved fresh are Sat — `ground6`/`ground6+forall` Sat/Unknown); the bug is in OxiZ's CORE solving, exposed by the clean engine's ground-term instantiation. **TWO DISTINCT OxiZ core soundness bugs:** (1) **incremental CDCL(T)** — adding the (sound) instances across MBQI rounds and re-solving yields spurious unsat, while the SAME clause set single-shot is sound (no-bounds injectivity); (2) **ground EUF/arith** — single-shot `{facts ∪ deep bounded instances}` is ITSELF spurious-unsat (bounded corpus injective), a deeper ground bug like [[oxiz_relationship]]'s P0 A/B/C. Legacy MBQI hits the same wall on the no-bounds form (masked on the corpus by its bounded finite-domain heuristic). **Mitigation landed (`8a79f64`): `Solver::verify_clean_unsat`** — on a clean-engine `unsat`, re-solve `{non-quantifier assertions ∪ ground instances}` single-shot fresh; trust only a confirmed unsat (else sound `Unknown`). Fixes bug (1) (no-bounds → Unknown; `injective_no_bounds_is_not_unsat` regression; genuine_conflict still Unsat). Does NOT fix bug (2) (the single-shot ground solve is itself unsound there). **The clean engine's soundness fundamentally REQUIRES a sound host ground core, which OxiZ does not fully provide** → corpus-clean needs a dedicated OxiZ ground EUF/arith soundness audit (separate major effort). Also fixed during M4e-3: re-encode-avoidance (`70eaf60`, don't re-register the quantifier per instance); pub `Context::set_clean_mbqi` + `set_timeout_ms`. clean_mbqi stays OFF by default. USER chose **black-box ground audit** (2026-06-12). **`oxiz-solver/tests/ground_soundness_fuzz.rs`** (`#[ignore]`): differential fuzz of OxiZ quantifier-FREE EUF+LIA vs **live z3** (random ground QF_UFLIA, delta-debug minimizer, panics on spurious unsat/sat). FIRST RUN found ~7.5% unsound. **Bug FIXED (`5cad638`): `encode(false)` returned wrong-polarity literal** — the `TermKind::False` arm pinned its var false but returned `Lit::neg(var)` (evaluates TRUE) instead of `Lit::pos(var)`; a `false` SUBTERM read as TRUE. So `(distinct 5 3)` = `¬(5=3)` = `¬false` inverted → spurious unsat for any distinct with ≥2 equal-folding int constants (even `(distinct 5 3)` alone!). Top-level `(assert false)` masked it via `has_false_assertion`. This one fix dropped the fuzzer's unsound rate ~7.5%→~0.7% (526 solver-lib + wiring + host green). **Bug (a) ROOT-CAUSED + documented (`aad390a`, no behavior change):** the spurious-SAT (`g(10)=5 ∧ f(g(10))=20 ∧ f(g(10))≤10` → OxiZ Sat) is `extract_linear_terms` (encode.rs ~361) DROPPING a numeric nested app `f(g(k))` from arith once its arg `g(k)` becomes an arith term (the `has_conflicting_apply_arg` guard) → the DIRECT bounds on `f(g(k))` are never arith-checked. The obvious fix (keep nested apps in arith) was MEASURED with the fuzzer to REINTRODUCE spurious UNSAT (1-3 / 400 — the soundness-dangerous direction, no complete congruence propagation). So the guard correctly trades a COMPLETENESS loss (spurious sat) to avoid a SOUNDNESS loss (spurious unsat); for the clean engine that's the right call. **Bug (a) is therefore COMPLETENESS, not soundness** — real fix = complete Nelson-Oppen EUF↔arith equality propagation (deep; documented inline as the fix location). **NO investigation (user chose complete-N-O):** the `combination/` module (nelson_oppen, equality_propagation, interface_eq, convexity, nelson_oppen_advanced) is DEAD CODE — never used by `solver/`. Live combination = two ad-hoc fns in `theory_manager.rs`: `propagate_euf_equalities_to_arith` (one pass, only over `var_to_parsed_arith` terms — NOT a fixpoint, misses arith terms not in a constraint) + `model_based_combination` (hard-conflicts on EUF-equal/arith-different rather than asserting the interface equality). Diagnosed the spurious-unsat (nested apps in arith) fires from **the arith solver's own `check()`** (process_constraint path, before propagation runs) — NOT from either combination fn. So complete N-O for non-convex LIA+EUF needs interface-equality propagation (fixpoint, all arith terms) + case-splitting — a substantial SMT-core feature, high regression risk. DEFERRED: the conservative `has_conflicting_apply_arg` restriction stays (it protects the clean engine from the dangerous spurious-UNSAT direction; bug (a) completeness-loss is the acceptable trade). The clean engine's actual SOUNDNESS blocker remains bug (b) (injective spurious-unsat), a separate bug.

**GROUND AUDIT RESOLVED (2026-06-12) — three OxiZ-core soundness bugs fixed, fuzzer now 6000-problem / 4-seed / 100%-z3-agree / 0-unsound:**
- **EUF `use_list` OOB panic (robustness/soundness crash)** — interning a term DURING a theory check (after a SAT push) grows the EUF node space (`nodes`/`use_list`/`uf` via `intern`'s `uf.add()`), but EUF `pop` truncated `nodes`/`use_list` while `UnionFind::pop()` only undid unions, never shrank `parent`/`rank` → the NEXT `intern` desynced the node index from the uf slot and `find` returned a stale root past `use_list`. Fix (`oxiz-theories/src/euf`): drop path compression on the backtracked paths (`find_no_compress`/`same_no_compress` everywhere in `solver.rs`; union-by-rank alone keeps finds O(log n)) + new `UnionFind::truncate(num_nodes)` called in EUF `pop` to re-sync uf with nodes + bounds-safe `are_equal`/`find`/`*_immutable` (a popped/stale index = its own singleton, never panics).
- **Bug (b) injective deep-bounded spurious-UNSAT — ROOT CAUSE: the proof forest was not backtracked with the union trail.** EUF `pop` truncated `proof_forest` by node INDEX but left merge/congruence edges that had leaked onto SURVIVING nodes (< num_nodes) during the popped scope. `explain_equality`'s BFS then routed through a retracted-merge edge and returned reasons for equalities that no longer held → an INVALID (over-strong) learned conflict clause → spurious unsat. It only bites at scale + with backtracking (nested-app instances fire extra congruence → more leaked edges; 102 nested instances unsat, every small subset sat). Fix: `proof_trail`/`proof_trail_limits` (mirror `sig_trail`) record every proof-edge insertion via `push_proof_edge` and `pop` undoes them BEFORE `proof_forest.truncate`; also clear `expl_cache` on `pop` (it was only cleared in `merge`, comment lied). Found by **categorical bisection** (split the corpus by structural class → "≥1 arg is `(f n)`" = nested apps was the trigger) after realising **every subset of a SAT assertion set is SAT, so ddmin needs NO z3 oracle** (subset spurious-unsat ⟺ OxiZ says unsat).
- **Bug (a) nested-app spurious-SAT — RECLASSIFIED as SOUNDNESS and FIXED by REMOVING the guard.** `g(10)=5 ∧ f(g(10))=20 ∧ f(g(10))≤10` is UNSAT (20≤10 false) but OxiZ said Sat because the `has_conflicting_apply_arg` guard DROPPED the numeric nested app `f(g(10))` from arith (both `track_theory_vars` and `extract_linear_terms` in `encode.rs`), losing its direct bounds. The earlier conclusion that removing the guard "reintroduces spurious unsat (1-3/400), so keep it as a completeness-vs-soundness trade" was WRONG — that spurious unsat was a **symptom of bug (b)** (the proof-forest corruption broke the EUF↔arith congruence explanations). With bug (b) fixed, BOTH guard sites removed (track every numeric Apply, flat or nested) and the fuzzer confirms NO spurious unsat across 6000 problems. The congruence that keeps `f(g(a))` agreeing with `f(v)` once `g(a)=v` is supplied soundly by `propagate_euf_equalities_to_arith`, not by hiding the term.
- **Also fixed (defensive, latent):** `terms_to_conflict_clause` always emitted `Lit::neg(var)`; a conflict reason from a NEGATIVELY-assigned equality atom (an `(= x y)` the SAT solver set false → asserted as a disequality in EUF) needs `Lit::pos`. Added `assigned_phase: FxHashMap<Var,bool>` (recorded in `on_assignment`) → emit the polarity that FALSIFIES each literal. Didn't change the injective corpus (its diseq reasons are tautological IntConsts, dropped) but is a real malformed-clause bug for explicit disequalities.

Regressions added: `oxiz-theories` `test_pop_removes_stale_proof_edges` + `test_pop_keeps_outer_scope_proof_edges` (EUF unit, proof-edge backtracking); `oxiz-solver/tests/ground_soundness_regression.rs` (4 z3-free cases incl. `nested_app_consistent_is_sat` guarding the OPPOSITE direction). oxiz-theories lib 1172 + oxiz-solver lib 526 + injective_min (z3-diff) all green. The three fixes are committed: OxiZ `eb6e76f`, adsmt parent submodule bump `724aad4`. **This UNBLOCKS the clean engine: its soundness required a sound host ground core, which OxiZ now provides (re-validate `clean_mbqi_corpus.rs`).** OxiZ submodule on `0.2.4-feat/cdqi`.

**Broadened the fuzzer with `gen_problem_arith` (linear sums `+`/`-`/`*k`, `ite`, nested boolean) → `ground_arith_no_spurious_unsat` (gates the DANGEROUS spurious-UNSAT direction; tolerates+logs benign spurious-SAT). This RICH-arith fragment surfaced PRE-EXISTING dangerous spurious-UNSAT bugs the EUF/LIA `gen_problem` never generated (it lacks subtraction / OR-of-arith / ite). Fixing them one root cause at a time:**
- **GCD-reason bug — FIXED (committed `fb6e920`).** `assert_eq`'s integer-infeasibility shortcuts (GCD `2c=3`, non-int-const `2x=3`) poisoned the simplex with contradictory bounds `x≥1 ∧ x≤0` tagged with a HARDCODED reason id `0`, returning early WITHOUT `add_reason(reason)`. The genuine equality atom was never recorded, so the simplex conflict `Err([0,0])` resolved to a stale unrelated term and `terms_to_conflict_clause` built a learned clause OMITTING the disjunct literal → in `(or (= c (- 3 c)) (>= 10 3))` the SAT solver couldn't flip to the satisfiable disjunct → spurious UNSAT. Fix: `add_reason(reason)` in both branches, pass the id to set_lower/set_upper. **Root-caused via a 4-agent Workflow** (1 instrumenting in a worktree + 3 read-only); the "no retraction on backtrack" hypothesis was REFUTED (push/pop is correct). This same bug was the REAL cause of the earlier "ite spurious-unsat" red herring (`/tmp/ite_unsat.smt2` had a `(- 3 c)` GCD disjunct; the ite axioms merely added atoms that shifted decision order to expose it). 6 ground_soundness_regression cases green.
- **Stale-bound pseudo-conflict — FIXED with a SURGICAL GUARD (committed `72af09a`, parent bump `4374061`); honest trade-off.** Root-caused by a 2nd 4-agent Workflow (instrumenting agent in a worktree, decisive trace): a theory↔SAT FRAME DESYNC leaves one atom asserted into the simplex under BOTH polarities at the same level — `(>= (f 2) 5)` decided false at level 3 (→ `f(2)≤4`), then a learned clause re-forces it TRUE at level 3 WITHOUT the theory frame retracting the level-3 false bound (→ `f(2)≥5`); the simplex conflict's two distinct reason-ids BOTH resolve to the SAME atom term → `terms_to_conflict_clause` emits a malformed `[¬v,¬v]` unit → spurious UNSAT. The inequality assert paths (assert_lt/le/ge) are CLEAN (the "no retraction on backtrack" and "reason-loss in inequalities" hypotheses were REFUTED). Fix: `ArithSolver::check` records the conflict shape; `last_conflict_is_stale_bound()` = `distinct_reason_ids≥2 && distinct_atom_terms<2` (a genuine self-infeasible atom like `(< x x)` or the GCD branch reports under ONE reason-id); the theory manager SUPPRESSES the pseudo-conflict at all 4 arith conflict sites. **TRADE-OFF (the agent's "suppression cannot hide a real unsat" claim was EMPIRICALLY FALSIFIED):** suppressing rather than RETRACTING the stale bound lets ~0.65% of random rich-arith formulas return a benign spurious SAT (rich-arith tolerated_spurious_sat 108→121, consistent across 3 seeds) — but the DANGEROUS spurious-UNSAT direction goes to 0 (EUF/LIA 6000 + rich-arith 3×2000) and the z3-parity corpus is UNCHANGED. Per "spurious-unsat is the killer" this is the right prioritisation (verus: spurious-sat = reject valid obligation = benign; spurious-unsat = accept invalid code = catastrophic). **ROOT cause is a theory-frame↔SAT-trail desync. Two pieces:** (1) **restart desync — FIXED (`fe71e93`, parent `38bb65e`):** `solve_with_theory`'s `handle_clause_deletion_and_restart()→restart()` backtracked to level 0 without `theory.on_backtrack(0)`, so the theory frame stack kept the stale level-1.. frames (reused on the next descent). Every OTHER backtrack site notifies the theory; restart was the lone exception. A MULTI-atom restart desync would escape the single-atom `last_conflict_is_stale_bound` guard → a dangerous spurious-UNSAT hole on restart-heavy workloads (verus prelude restarts often). Fix: `handle_clause_deletion_and_restart()` returns whether it restarted (`#[must_use]`); all 4 sites call `theory.on_backtrack(0)` on restart. Validated non-breaking (oxiz-sat 606 + theories 1172 + solver 526; fuzzer dangerous=0; no measurable random-fuzzer delta — small formulas rarely restart-then-conflict — but restores a correctness invariant). (2) **same-LEVEL backtrack+re-decision desync — STILL DEFERRED (broad/risky):** an atom decided false at level L then re-forced true at level L without the theory frame L being reset; the stale-bound GUARD (`72af09a`) is the surgical interim for this (trades benign spurious-sat). A full same-level resync would clean both directions but is a deeper SAT-core change for the user to scope.
- **Numeric-ite spurious SAT (benign).** `(= (ite (= c 0) 10 20) 99)` → OxiZ Sat (z3 Unsat): a numeric ite is an unconstrained opaque arith value. A term-ITE-elimination fix (`emit_numeric_ite_axioms`) was attempted then REVERTED because it exposed the GCD spurious-UNSAT (now fixed) — worth RE-ATTEMPTING once the rich-arith spurious-unsats are all fixed, validating it adds no spurious unsat.
- **Nonlinear (QF_NIRA) spurious UNSAT — out of scope, pre-existing.** `(= (* x x) 4)` + an independent real-`y` bound flips OxiZ to Unsat. OxiZ is a linear solver; sound nonlinear behavior is `unknown`. Not a regression; verus prelude is linear.
- **Quantified spurious SAT (MBQI completeness).** `forall x. f(x)≥0 ∧ f(5)<0` → both MBQI paths return Sat. Spurious SAT = a verifier rejects a VALID obligation (annoying, not dangerous).
- **div/mod spurious SAT (loose axiomatisation, benign).** Spot-check: the dangerous direction is CLEAN for div/mod (no spurious unsat), but OxiZ does not enforce the Euclidean identity `x = d*(x div d) + (x mod d)` nor `0 ≤ (x mod d) < |d|` → `(not (= x (+ (* 3 (div x 3)) (mod x 3))))` and `(< (mod x 5) 0)` both return Sat (z3 Unsat). Benign direction; needs div/mod axioms.

EUF/LIA fragment: dangerous spurious-UNSAT CLEAN across 6000 problems / multi-seed. Rich-arith: GCD class fixed, more spurious-unsat under active fix. PROCESS WIN: the GCD bug is the same CLASS as the EUF conflict-clause polarity bug — wrong/lost REASON → malformed learned clause → spurious unsat; audit every theory `assert_*` for sentinel/hardcoded reasons.

**Interim OxiZ fixes (keep, on `0.2.4-feat/cdqi`):** B `c38ea58`, E `64cc32a`, D `09935dd` — real completeness gains for the mechanisms they cover; superseded by the clean engine at M4. See [[oxiz_relationship]].

**verus-fork prelude encoding finding** (`verus-fork/source/vir/src/prelude.rs:67-87`): emits backend-specific preludes — Z3/OxiZ use native `(_ partial-order 0)` + `:pattern`; **Cvc5/Adsmt hand-roll trigger-free reflexivity/antisymmetry/transitivity/strict-def axioms** (the trigger-free shape that trips MBQI), per a STALE comment that lu-smt can't parse `(_ partial-order N)` (it can since rc.30, `adsmt-parser-smtlib2/convert.rs:540`, as an uninterpreted predicate). OxiZ HAS a real `special_relations.rs` partial-order theory but it's UNWIRED dead code. BUT: removing the partial-order axioms does NOT close the full-prelude P0 (the fuel machinery is another trigger) → encoding/theory fixes alone insufficient → the clean MBQI rewrite is the principled fix.

**Assert-time quantifier preprocessing (2026-06-19, OxiZ `e87e24e`, branch `0.2.4-experiment/iteration-recognizer`, UNPUSHED).** Two equisatisfiable, polarity-safe rewrites in `clean_mbqi.rs`, composed in `Solver::assert` (encode.rs) gated on `config.clean_mbqi`: (1) **`fold_valid_quantifiers`** — fold any quantifier whose matrix is a recognized tautology to `true`, POLARITY-INDEPENDENTLY (recurses through not/and/or/=>/ite). Reuses the existing `SolverModel::body_is_valid` recognizer (reflexivity/congruence/transitivity/∧∨⇒ite¬), which previously fired ONLY on the positive `eval_forall` path. (2) **`skolemize_unbounded_existentials`** — top-level positive UNBOUNDED `∃` → fresh const `sk!…` (bounded `∃` left for #279's finite disjunction; `∃` under not/=>/ite left alone). **DISCOVERY — clean MBQI was POLARITY-BLIND → spurious SAT on negated quantifiers.** `collect_quants` (oxiz-mbqi/engine.rs) descends through `App` (incl. `Not`) and records `universal: forall` SYNTACTICALLY, ignoring polarity. Two subclasses: **(a) negated VALID `∀`** (`(not (forall x. (= x x)))`, `(not (forall x. (= (f x)(f x))))` → should be unsat) — was spurious `sat` because the validity recognizer never ran under the negation (quantifier asserted false → inactive → `eval_forall` skipped); **FIXED by the fold** (the user's `(= x x)` idea: rewrite valid-bodied quantifier → `true` up front, so `(not true)`→`false`→sound unsat). **(b) negated CONTINGENT quantifier** (`(not (exists y. (= (f y) 0)))` + `(= (f 5) 0)`, unsat) — STILL spurious `sat`: this is NOT a validity question; `∀y.f(y)≠0` is contingently unsat only via the ground fact, and the engine sees the negated `∃` as inactive → never instantiates the implied universal at `f(5)`. Needs polarity-aware instantiation / NNF (push negation inside so a syntactic `∃` under `not` is collected as a universal-to-instantiate). REMAINING frontier item. **surjective.smt2 Unknown→Sat** (skolemize, +1 agree vs prior 149). Corpus: 167 terminating cases, **0 spurious sat/unsat**, 150 agree / 16 unknown / 1 stronger; fold changes NO corpus verdict (+3 regression tests in uf_sort_and_quant_soundness). **Pre-existing (NOT this change): UFLRA/real_lipschitz.smt2 HANGS** — the 1000ms wall-clock cap is not honored on an unbounded real-domain `∀` f-tower (x:=f(t)→f(f(t))→…); reproduces identically on HEAD (`git stash` confirmed) → blocks a clean full 168 corpus-sweep completion (the gate now finishes 167/168 then a straggler hangs). Separate latent termination bug (deadline not checked inside the clean-MBQI enumeration round).

**NNF preprocessing closes the negated-CONTINGENT-quantifier spurious sat (2026-06-19, OxiZ `127605d`, UNPUSHED).** The polarity-blindness subclass (b) — `(not (exists y. (= (f y) 0)))` + `(= (f 5) 0)`, unsat only via the ground fact — is now FIXED by `nnf_push_negations` (clean_mbqi.rs), run FIRST in `Solver::assert` before fold + skolemize. It pushes negations to the quantifier boundaries (`¬∀≡∃¬`, `¬∃≡∀¬`) so `collect_quants` sees true polarity; `(not (exists …))` becomes the positive universal `(forall y. (not (= (f y) 0)))` which the engine ENUMERATES over the real ground index at 5 → `f(5)≠0` → conflict. **Empirically validated FIRST** (memory lesson again): feeding the hand-NNF'd form already returned unsat, proving the engine's trigger-free enumeration was always capable — the gap was purely the un-pushed negation (a multi-agent design agent had WRONGLY claimed NNF couldn't fix (b), conflating CDQI/triggers with enumeration; the empirical probe refuted it). Two safety constraints: SURGICAL (only the boolean spine reaching a quantifier is rewritten via a `has_quant` gate — ground structure/theory atoms untouched) + BODY-OPAQUE (a flipped quantifier wraps its body in a SINGLE `not`, never recursively NNF'd, so the `=>`-matching recognizers body_is_valid-congruence/transitivity + bounded_var_domains #279/#280 still match). Corpus: 0 spurious, distribution UNCHANGED (corpus has no negated/antecedent quantifiers; positive quantifiers kept opaque). +2 regression tests. **SUBSOLVER design verdict (workflow wf_3f27255a, 4 perspectives + adversarial):** a recursive subsolver (decide a quantified subformula by a nested solve of its dual; `verify_clean_unsat`/`fresh_ground_resolve` are the existing primitive) is the principled Tier-2 fallback for the genuinely-recursive frontier NNF can't reach (∀∃ nested where the inner witness needs a real solve, valid-but-unrecognized quantifiers). SOUND **only** behind 7 mandatory guardrails (the stated contract `fold only fresh_ground_resolve-confirmed verdict + inconclusive→Unknown` is necessary-not-sufficient): (1) explicit solver-RE-ENTRY depth bound (≤2; the existing body_is_valid `depth` only bounds term-DAG descent — forall/exists alternation over a shared appendable TermManager diverges into an f-tower otherwise); (2) independent strictly-decreasing budget, sub-Unknown→parent-Inconclusive NEVER→fold; (3) fold only on DEFINITE dual-unsat then re-confirm ground-only; (4) no subsolver-minted Skolem may enter `clean_instances` (scope-escape unsoundness); (5) don't invoke inside the `take_theory_manager` window (mod.rs:550 — the real TermManager is mem::take'd out → cross-manager TermId aliasing); (6) gate OFF the known-fragile ground fragments (QF_NIRA real-var integerization, array ∃, congruence towers) until the ground core is hardened; (7) saturation guard against spurious-SAT-by-instance-omission (verify_clean_saturated is sound only relative to the instances it was given). DEFERRED as a separate larger effort; NNF+fold+skolemize covers the negated-quantifier classes cheaply.

**Skolem functions + the deadline-overshoot hang exposed (2026-06-19, OxiZ `06b7fb9`, UNPUSHED).** Generalised assert-time existential skolemization from top-level CONSTANTS to nested Skolem FUNCTIONS: `skolemize_unbounded_existentials` now carries the scope of enclosing universal vars and descends into `∀` bodies / `and`/`or` / the `=>` consequent; a positive `∃ȳ` under universals `x̄` → `φ[ȳ↦sk(x̄)]` for a fresh `sk` (equisatisfiable; `mk_apply` for the function, nullary = the old constant case). Polarity-respecting (only positive ∃; under `not`/`=>`-antecedent left alone). A `∀` whose body has no `∃` is returned verbatim (same TermId) so recognizers/bounded_var_domains are untouched. **forall_exists_simple.smt2 Unknown→Sat (+1 agree)** — the ground-anchored `∀x.∃y.f(x,y)>0` becomes `∀x.f(x,sk(x))>0` the engine instantiates+verifies. The anchorless `forall_exists_unsat` stays sound Unknown (never-fabricate engine has NO ground term to seed enumeration — z3 introduces one; fundamental limitation). z3-parity: 0 spurious, +1 agree, +2 regression tests. **NEW FINDING — the dense-real `∀` hang (real_lipschitz, real_interp) is a DEADLINE-OVERSHOOT bug, PRE-EXISTING (git-stash confirmed on HEAD; NOT caused by Skolem — Skolem is a no-op on those quantifier-only-`∀` cases).** ROOT: the MBQI wall-clock deadline is checked ONLY between rounds (`solver/mod.rs:543`); a single round's ground solve `solve_with_theory`/`solve_with_hooks` (`oxiz-sat/src/solver/mod.rs:1084`/`1335`) takes NO deadline arg and the engine's `max_instances=100_000` lets one round emit a huge instance set, so a dense-real f-tower round overshoots the 1000ms cap by an unbounded amount → effective hang (flaky under parallel load: contention slows rounds so the between-round check fires earlier → Unknown; standalone it diverges). Blocks a clean full 168 corpus sweep (gate finishes 166-167/168 then a straggler hangs). FIX OPTIONS: (A) thread the deadline into the SAT solve loop (periodic check every N conflicts → bail to Unknown; robust, touches oxiz-sat); (B) tighten the per-round instance budget so no single ground solve overshoots (simpler, less robust). The verdict on expiry is always the sound Unknown. DEFERRED pending user direction. NNF+fold+skolemize(const+function) all landed on the negated-quantifier + surjective + ∀∃-with-anchor frontier; remaining 15 Unknowns need MODEL CONSTRUCTION (dense-real/array/idempotent/monotone) which fights the never-fabricate firewall (incremental recognizers, spurious-sat risk) — to be brainstormed case-by-case.

**Model-construction frontier — definitional recognizer fixed + monotonicity KB designed (2026-06-19).** The 15 SAT-Unknown corpus cases all need MODEL CONSTRUCTION (a satisfiable axiom z3 models but the never-fabricate engine leaves Unknown). Families: definitional `f(x)=expr`, monotone `x≤y⇒f(x)≤f(y)`, idempotent `f(f(x))=f(x)`, arrays, dense-real. SOUNDNESS PRINCIPLE for any new eval_forall recognizer (#260/#277): return `Some(true)` ONLY after VERIFYING a concrete model exists that satisfies the axiom AND is consistent with ALL existing ground facts — never fabricate, never finite-exhaust. **DEFINITIONAL FIXED (OxiZ `6094561`, UNPUSHED): real_identity.smt2 Unknown→Sat (+1, 102 agree).** Two pre-existing #260 gaps, both hit by `∀x.f(x)=x`: (1) `definitional_head` matched the application only as the `Eq` LHS, but `TermManager` CANONICALISES `Eq` operand order → for `(= (f x) x)` the bound var (interned before `(f x)`) becomes LHS and `(f x)` the RHS → recognizer bailed (worked for `f(x)=c` only by interning luck). Fixed via `definitional_match`/`definitional_side` trying BOTH operands. (2) Pass-1 "head occurs exactly once globally" rejected a definition whose `f` also appears in ground facts; new Pass 5 accepts it when `f` is in no other quantifier and every ground `f`-app is eq-pinned to the value the definition predicts (projection or constant rhs) — a non-pinned app / disagreeing pin → NOT accepted (engine then refutes via instantiation, never spurious sat). 0 spurious, +2 regression tests. **MONOTONICITY KB (user hints 1+2, designed not yet built):** model construction via a KB of monotonicity THEOREMS (sound, not guesses). TWO mechanisms: **(a) bare axiom** (uninterpreted f + `∀x,y.x≤y⇒f(x)≤f(y)` + ground points) → order-extension theorem: ground f-facts pairwise order-consistent ⟹ a monotone extension EXISTS ⟹ Sat (handles corpus monotone_basic + real_monotone; no KB needed); **(b) defined/composite f** → a `monotonicity(expr, var) → (Direction, strict?)` certifier, sign-algebra: Direction∈{↑,↓,•}; scalar ×c uses sign(c) (c=0→•); compose = sign-multiply (↑∘↑=↑, ↓∘↓=↑, mixed=↓); sum ↑+↑=↑/↓+↓=↓/mixed=⊤; nonneg-product=↑; exp base>1→↑/base<1→↓/base=1→•; INVERSE fires ONLY on STRICT (strict monotone ⟹ injective ⟹ inverse exists, on the image; weak monotone e.g. constant is NOT invertible) → track strictness alongside direction. (b) serves real verus obligations (cost = sum of monotone terms etc.); not in this corpus. Next: implement (a) order-extension recognizer (2 corpus wins) + the (b) KB certifier (unit-tested). Session commits this run: e87e24e fold+skolem-const, 127605d NNF, 06b7fb9 Skolem-fn, 6094561 definitional — 0 spurious throughout.

**Monotonicity KB certifier (b) implemented (2026-06-19, OxiZ — pending corpus validation/commit).** `clean_mbqi.rs` gained a SOUND structural monotonicity certifier: `monotonicity(m, expr, var, depth) → Option<(MonoDir, strict)>` (MonoDir∈{Inc,Dec,Const}) over arith — Var(hole)=Inc-strict, Const=indep, Neg=flip, Add=mono_sum(↑+↑=↑/↓+↓=↓/mixed=None), Sub=a+flip(b), Mul=one var-factor × literal-sign scaling (x·x→None), Div by nonzero literal=sign-scale. Wired into `body_is_valid`'s OP_IMPLIES arm via `is_monotone_implication(guard, conseq)`: recognises VALID `(x⊴y) ⇒ (E[x]⊵E[y])` — guard orders two bound vars, `anti_unify_hole` checks L=E[x]/R=E[y] for a common one-hole context E (commutative Add/Mul matched up to permutation), `monotonicity(E)` certifies the matching direction; strict consequent needs strict guard + strict E. Valid in every interpretation ⇒ sound `Sat`. Probes: `2x+1`/`-3x`(≥-flip)/identity/`x+x` increasing → sat; `-x`/`x*x`/wrong-direction → Unknown (declined). **CAUGHT+FIXED a self-introduced soundness hole in `anti_unify_hole`**: two DISTINCT childless leaves (`5` vs `7`, or unrelated vars) vacuously "matched" (empty children → greedy bijection trivially true), so `x≤y ⇒ x+7 ≤ y+5` (INVALID, 7≰5 at x=y) was certified → spurious sat; fixed by `if lc.is_empty() { return false }` (distinct non-hole leaves are not a common context). Post-fix: invalid→Unknown, valid-`2x+1`→sat. (b) gives 0 CORPUS wins — the corpus monotone cases (monotone_basic/real_monotone) have UNINTERPRETED f so `monotonicity(f(x))`=None; (b) serves DEFINED/composite monotone obligations (real verus), unit-tested. Still TODO: (a) order-extension recognizer for the bare-axiom uninterpreted-f corpus cases (interval-feasibility over ground points). +3 monotone regression tests (affine-valid → sat; anti-monotone + x² → not sat). Session commits: e87e24e/127605d/06b7fb9/6094561 + this (b) pending.

**Monotonicity KB certifier (b) COMMITTED + derivative-engine design (2026-06-19, OxiZ `38691a9`, UNPUSHED).** Built `monotonicity(m, expr, var, depth) → Option<(MonoDir, strict)>` sign-algebra (Var=↑strict, Const, Neg=flip, Add/Sub=same-dir combine, Mul=one-var-factor × literal-sign, Div by literal=sign-scale) + `is_monotone_implication(guard, conseq)` in `body_is_valid`'s OP_IMPLIES arm: certifies VALID `(x⊴y)⇒(E[x]⊵E[y])` via `anti_unify_hole` (L=E[x]/R=E[y] common one-hole context, commutative Add/Mul matched up to permutation) + `monotonicity(E)` matching direction (strict consequent needs strict guard+E). SELF-CAUGHT+FIXED soundness hole: distinct childless leaves (5 vs 7) matched vacuously → certified INVALID `x≤y⇒x+7≤y+5` → spurious sat; fixed `if lc.is_empty() {return false}`. Corpus UNCHANGED (uninterpreted-f monotone cases decline → None), 0 spurious, 102 agree; +3 regression tests. Serves DEFINED/composite monotone obligations; corpus monotone_basic/real_monotone (uninterpreted f) still need **(a) order-extension** (interval-feasibility over ground f-points — STILL TODO, the user-requested "둘 다 구현" other half). **DERIVATIVE-ENGINE design (user hint 3, the GENERALIZATION of the sign-algebra, NOT YET BUILT):** monotonicity ⟺ derivative sign (f′≥0 on a CONNECTED interval ⟹ non-decreasing, MVT). Two sub-problems: (1) symbolic differentiation `derivative(e,var)→e′` — mechanical/decidable/sound (linearity, power `d(x^a)=a·x^(a-1)`, product, CHAIN `d(g(f(x)))/dx=g′(f(x))·f′(x)`, exp `d(a^x)=a^x·ln a`, trig, `d(ln x)=1/x`); (2) derivative-SIGN determination `f′≥0 over domain?` — the HARD part, nonlinear-arith validity, UNDECIDABLE in general + OxiZ is LINEAR, so handle the decidable fragment (constant-sign=affine, same-sign sums, known-sign products) + recursively reuse the KB. TRANSCENDENTALS (sin/cos/exp/ln/tan) are NOT native OxiZ theory → uninterpreted funcs + KB-asserted derivative/monotonicity/range facts (exp>0 & strict-inc; ln strict-inc on ℝ⁺; **sin/cos bounded & monotone ONLY on intervals — must NOT certify global monotonicity**). CARATHÉODORY's lemma = rigorous differentiability foundation (∃ continuous φ: f(x)−f(a)=φ(x)(x−a)), cleanest for chain rule; used as meta-justification (proof certs), not computed at solve time. SOUNDNESS GATES: domain CONNECTEDNESS (1/x decreasing on each of (−∞,0),(0,∞) but NOT across 0) + differentiability on the domain. Scale = a small CAS; sign-determination is the spurious-sat landmine. Session run: 6 commits e87e24e/127605d/06b7fb9/6094561/38691a9 (+7903a4d prior), 0 spurious throughout.

**Order-extension (a) COMMITTED — monotonicity work (a)+(b) DONE (2026-06-19, OxiZ `9236092`, UNPUSHED).** `try_monotone_extension` (eval_forall recognizer) decides a bare monotone axiom `∀x,y.(x⊴y)⇒(f(x)⊵f(y))` over UNINTERPRETED unary f by checking the ground f-points admit a monotone extension (order-extension theorem). CompletionFacts Pass 6 `collect_mono_facts` records per-app `[lo,hi]` from WEAK shapes `(= app c)`/`(>= app c)`/`(<= app c)` + relations `(<= app1 app2)` (exact `term_to_rational` BigRational, both Eq orders); recognizer groups points by arg, greedy interval-feasibility (inc: v=max(lo,prev)≤hi; dec mirror), f-f relations must be direction-IMPLIED. SOUNDNESS = ACCOUNTING GUARD: fires only if `global_occ[f] == 2(axiom) + mono_handled_occ[f]` — any f-app in an unmodeled context (strict bound, arith term, other quantifier) isn't tallied → mismatch → DECLINE. **monotone_basic + real_monotone Unknown→Sat (+2, 104 agree), 0 spurious**; probes: violated/infeasible→unsat-via-engine, `(* 2 (f 5))=60`→declined Unknown. +3 regression tests. **SESSION TOTAL = 7 commits** (e87e24e fold+skolem-const / 127605d NNF / 06b7fb9 Skolem-fn / 6094561 definitional / 38691a9 monotone-KB-certifier(b) / 9236092 order-extension(a) + 7903a4d prior), corpus 104 agree / 10 unknown / 1 stronger, **0 spurious throughout**. **DERIVATIVE-ENGINE KB — full spec accumulated from user hints 1-3+ (NOT YET BUILT, the next big phase = a small CAS):** monotonicity ⟺ derivative sign (1st-derivative test ONLY per user: f′≥0 on connected interval ⟹ non-decreasing). (1) symbolic `derivative(e,var)` mechanical (linearity, power `d(x^a)=a·x^(a-1)`, product `d(fg)=f′g+fg′`, CHAIN `d(g(f(x)))/dx=g′(f(x))·f′(x)`, `d(a^x)=a^x·ln a`, trig `d sin=cos, d cos=−sin, d tan=sec²`, `d(ln x)=1/x` **with domain x>0 per ln's definition**); (2) derivative-SIGN over domain = the HARD/undecidable part (OxiZ linear) → decidable fragment + recursive KB. TRANSCENDENTALS = uninterpreted + KB facts (exp>0 strict-inc; ln strict-inc on ℝ⁺; **sin/cos monotone only on INTERVALS — never global**). A CALCULUS ATTRIBUTE LATTICE propagated through diff + arith: **direction** (sign-algebra ↑↓•), **strict** (strict⟹injective⟹invertible; inverse rule fires only on strict), **parity** {Odd,Even,Both(=0),Neither} (d/dx: Odd↔Even; const=Even; 0=Both; product Odd·Odd=Even/Even·Even=Even/Odd·Even=Odd), **periodicity** (f periodic(r)=f(x)=f(x+r) ⟹ EVERY f⁽ⁿ⁾ periodic(r) — diff preserves period). CARATHÉODORY's lemma = rigorous differentiability/chain-rule foundation (∃ continuous φ: f(x)−f(a)=φ(x)(x−a)), meta-justification for proof certs. SOUNDNESS GATES: domain CONNECTEDNESS (1/x dec on each of (−∞,0),(0,∞) NOT across 0) + differentiability on domain; sign-determination is the spurious-sat landmine (build via workflow + adversarial verify).

**Derivative engine increment 1 — self-verifying, leveled KB (2026-06-19, OxiZ `025f36a`, UNPUSHED).** New `oxiz-solver/src/calculus.rs` module (registered `pub mod calculus` in lib.rs): a one-variable calculus `Expr` (X/Const/Add/Mul/Neg/Pow(b,k)/ExpBase(a,g)/Ln/Sin/Cos/Tan), mechanical `diff` (product/power/chain/`a^g`/`ln`=g'/g/trig), conservative `Sign` lattice + `Expr::sign_on(&Domain)` (connected interval), and `Expr::monotonicity` = the FIRST-DERIVATIVE SIGN TEST (f′≥0⇒↑, f′≤0⇒↓, MVT). LEVELED KB (non-monolithic, lookups newest-level-first): L0 = primitive facts (diff rules + primitive signs `a^g>0` for a>0, `ln` domain x>0 + the primitive catalogue) → L1 = facts DERIVED by the derivative test + VERIFIED vs L0 declarations. `Kb::build_and_verify` runs the test on every primitive first → **0 mismatches** (self-consistent before consultation); `derive_next_level` stub holds the leveled-growth API. DOMAIN-AWARE soundness: ln increasing ONLY on x>0 (all-reals 1/x no constant sign — not connected through 0 → inconclusive); x² inc on ≥0/dec on ≤0/inconclusive across 0; sin/cos oscillate → never certified. 6 unit tests. **Built per user's plan**: 1st-derivative sign test on the KB's OWN functions first (incremental self-verification); user wants trivial derived facts accumulated into higher KB levels (L0→L1→…) to avoid a monolithic KB's lookup-perf problems. NEXT increments: compositional derivation into higher levels (sum/product/composition of certified fns) + wire into clean_mbqi `monotonicity()` (replace affine-only sign-algebra for transcendental/composite fns). Attribute lattice still to add (parity Odd↔Even under diff, periodicity preserved under diff, strictness→inverse). **SESSION TOTAL = 8 commits**, 0 spurious throughout; monotonicity completeness (a)+(b) DONE + derivative engine started.

**Derivative engine increments 2-4 (2026-06-19, OxiZ `3da3564`/`48591e7`/`983df5c`, UNPUSHED).** **#2 KB expansion (`3da3564`):** added hyperbolics (sinh/cosh/tanh) + atan as DERIVATIVE-VERIFIED primitives (diff+sign rules: cosh≥1>0, sinh/tanh/atan odd→sign tracks arg) + abs/floor/ceil/round as AXIOMATIC non-differentiable monotones (`Expr::Undefined` = formal derivative of non-smooth nodes, sign Unknown so the test never certifies THROUGH them; `Primitive.differentiable` flag, test runs only on differentiable, still 0 mismatches). atan2 deferred (needs multi-arg Expr). **#3 compositional derivation (`48591e7`):** `Kb::derive_next_level` now COMPOSES — for every total fact f in the newest level × every total base g, certifies `f∘g` by `compose_dir` sign-multiply (↑∘↑=↑, ↓∘↓=↑, mixed=↓); KB `Fact{dir, total}` gains a `total` flag (domain=ℝ, range⊆ℝ) and ONLY total fns compose (sound — ln/x²/cosh domain-restricted excluded until a range analysis); floor∘tanh=↑ etc. **#4 parity lattice (`983df5c`):** `Parity{Odd,Even,Both(=0),Neither}` + `Expr::parity()` (x odd, const even, 0 both, −f keeps, sum preserves shared / Even+Odd→Neither, product Odd·Odd=Even/Even·Even=Even/Odd·Even=Odd, b^k, odd-outer sin/tan/sinh/tanh/atan inherit inner, even-outer cos/cosh/abs→Even for symmetric inner, a^g/ln g→Even ONLY for even inner). Load-bearing: the user's rule **diff flips Odd↔Even**, SELF-VERIFIED on every parity-bearing primitive (sin odd→cos even, x² even→2x odd; Neither/asymmetric-domain ln excluded). 11 calculus unit tests total. **SESSION = 11 commits**, 0 spurious throughout (8 clean-mbqi/calculus + 3 prior context). REMAINING derivative-engine increments per user plan: strictness (⇒invertibility) + periodicity (preserved under diff, needs symbolic 2π for trig) attributes → then WIRE into clean_mbqi `monotonicity()` (caveat: transcendentals aren't OxiZ theory symbols, so wiring helps the arith-composite fragment (already covered by the committed sign-algebra) + speculative uninterpreted-fn-name recognition; the calculus engine is primarily a self-verifying STANDALONE foundation).

**Derivative engine COMPLETE — increments 5-6 (2026-06-19, OxiZ `76854be`/`c1e2be8`, UNPUSHED).** **#5 strictness+periodicity (`76854be`):** `Expr::monotonicity_strict` reports strict (`f′>0`, sign Pos) vs weak (`f′≥0`, NonNeg) — e.g. tanh STRICTLY inc (1/cosh²>0, positive-base power), x² on x≥0 only weakly inc; `inverse_dir` = strict⟹injective⟹invertible (the user's rule). `Period{Pi(k)|Rat|Constant|Aperiodic}` keeps trig periods symbolic (sin/cos 2π, tan π; π irrational→Pi(k) marker), `Expr::period` propagates (trig-of-periodic-inner keeps inner period, sum/product agreeing periods, Constant identity), SELF-VERIFIED: **diff PRESERVES period**. Attribute lattice complete = {direction, strictness, parity, periodicity}. **#6 clean_mbqi WIRING (`c1e2be8`):** `clean_mbqi::monotonicity` = structural sign-algebra THEN calculus fallback; `term_to_calculus` translates the OxiZ arith fragment (bound var→X, literals→Const, Add/Mul/Neg/Sub) to `calculus::Expr`, `monotonicity_via_calculus` runs the derivative test over ℝ + maps the verdict (`monotonicity_structural` recursion unchanged, fallback top-level only). On OxiZ's LINEAR term language both agree → corpus UNCHANGED (0 spurious, 104 agree), no regression; value = the connection (calculus engine = the principled authority, ready for powers + the transcendental KB once those symbols enter the term language). **DERIVATIVE-ENGINE PLAN COMPLETE**: engine→KB-expansion→compositional-derivation→parity→strictness/periodicity→wiring (6 increments). 13 calculus unit tests + 45 monotone-soundness tests, 0 spurious. **SESSION TOTAL = 12 commits this arc** (fold+skolem-const, NNF, Skolem-fn, definitional, monotone-KB-certifier(b), order-extension(a), derivative engine ×6), all UNPUSHED, all 0 spurious. User handles push + submodule pointer bump. Remaining frontier (separate): deadline-overshoot hang (real_lipschitz/interp), array/dense-real model construction, atan2/multi-arg calculus, calculus `derive_next_level` sum/product (not just composition), range analysis to compose domain-restricted (ln) functions.

**Idempotent recognizer (2026-06-19, OxiZ `93e99b8`, UNPUSHED).** `try_idempotent` eval_forall recognizer: `∀x. f(f(x))=f(x)` over uninterpreted f is sat with its ground facts iff IDEMPOTENCY-CONSISTENT — instantiating at a pinned `f(t)=v` forces `f(v)=v`, so every pinned value must be a fixed point; an idempotent total model then exists (identity off the pinned points). `idempotent_sides` matches `(f (f x))`/`(f x)` (both Eq orders), f in no other quantifier, builds arg→pinned-value map over ground_apps, checks every pinned value that is itself an arg maps to itself. Unpinned ground app (f in inequality) → declines (engine instantiates + refutes; `f(0)=5`+`f(5)=7`→unsat). **idempotent.smt2 Unknown→Sat (+1, 105 agree), 0 spurious**, +2 regression tests. **SESSION = 13 commits this arc**, all UNPUSHED (user merged the experiment branch into `0.2.4-redesign` + pushed those commits to origin mid-session; super-repo pointer 27e94cc still 23 commits behind c1e2be8/93e99b8 = USER's pointer-bump task, deferred per user "adsmt 버전 bump 아직 하지 말고"). REMAINING frontier (9 Sat→Unknown + 1 Unsat→Unknown): array (3: extensionality/sorted/update — array theory + quantifiers), dense-real UFLRA (real_archimedean/bounds/composition/fixed_point + the 2 deadline-HANGS interp/lipschitz), ∀∃ nested without ground anchor (forall_exists_unsat[Unsat], nested_quantifiers, skolem_test). The DEADLINE-OVERSHOOT hang (real_lipschitz/interp, pre-existing, single round overshoots between-round deadline check) still blocks a clean full 168 sweep (gate finishes 166/168 then a straggler hangs) — the gating robustness item.
