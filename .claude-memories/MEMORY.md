- [Project layout](project_layout.md) — adsmt SMT solver workspace + logicutils submodule
- [MBQI research library](research_library_mbqi.md) — `.claude-research-library/` holds 13 open-access MBQI/quantifier-instantiation papers (2007→2025) + README index, for improving OxiZ's clean-MBQI engine; not committed (3rd-party PDFs)
- [CCFV unified instantiation design](ccfv_unified_instantiation_design.md) — sketch: adopt CCFV/E-ground-(dis)unification as ONE instantiation core behind clean-MBQI's Sig/TermLang firewall (trigger/conflict/MBQI = parameterizations); resolves (a)+(b). GroundLedger funnel, AOT/JIT prelude=method reuse, upstream-verus pre-verification. Doc `external/oxiz/docs/design/CCFV_UNIFIED_INSTANTIATION.md`; P0→P2-flip LANDED (OxiZ `9a84198`, congruence-aware e-matching ON by default — also closes a latent nested-congruence spurious-sat); `trigger::match_term` deletion + P3/P4/P5 deferred
- [adsmt ⇔ logicutils relationship](logicutils_version_rule.md) — +2 offset (adsmt 0.21 ⇔ logicutils 0.23) until v1.0; 21E.2 option 2-A' = absorption + adsmt-meta metacrate + CLI preservation + chained auto-migration at v1.0
- [adsmt ⇔ OxiZ relationship](oxiz_relationship.md) — Path A+B P1-P4 landed; P5 decided 2026-05-30 as option 5 bidirectional embed (adsmt stays separate, abductive trait surface flows upstream as Apache-2). rc.30: realised the "solving on top of OxiZ" design as a runtime **delegation** — lu-smt's `oxiz_fallback` (`adsmt-cli/src/main.rs`) replays buffered SMT-LIB through OxiZ when native returns `Unknown`/degraded, giving `-V adsmt` OxiZ's full completeness (Y4 vstd obligations: 54 verified = Z3). TWO backends: (a) **in-process** (cargo feature `oxiz` → `oxiz_solver::Context::execute_script`, NO subprocess/env — the recommended Verus-backend build); (b) subprocess oracle via `ADSMT_OXIZ_PATH` (feature off). In-process feeds commands ONE-at-a-time (OxiZ batch parse_script mis-parses big multi-command inputs); override syncs `last_result` to synthetic Unsat (else Verus `discovered_error`). Stabilising in-process for the LIVE driver needed TWO OxiZ submodule fixes the subprocess isolation had masked: simplex bound-vector OOB (`.get()` → unbounded) + **`parse_declare_datatypes` multi-datatype form** (parsed only the 1st group → "expected ')', found LParen" on Verus's field-bearing Range/Layout record datatypes). OxiZ vendored at external/oxiz (submodule, 100%-Z3-parity, MBQI). **rc.36: vendored OxiZ was UNSOUND on quantified `:pattern` axioms (z3-cross-checked); fixed (sort persistence / IntConst EUF diseqs / pattern-guided MBQI) + CDQI; submodule → branch `0.2.4-feat/cdqi`. rc.36 P0 follow-up (verus-fork prelude false-unsat A/B/C): native is SOUND (delegation was the unsound path); A=uninterp-sort→2-Bool (Part 3a), C=trigger-free forall eagerly e-matched (Part 3b), B=ematch IDENTITY self-match `{i↦i}` left bound vars free → captured across axioms (Part 4, NOT overflow; per-command vs batch grouping symptom); oxiz `c38ea58`.**
- [WASM emitter package manager + runtime](adsmt_emit_system.md) — rc.31: prover-emit backends are *packages* (PKGBUILD-style frontmatter+build-script) run by a dedicated wasmi runtime, not system binaries. `adsmt-emit/` group (contract/pm/runtime/env/cli/lean); path A (core-wasm+WASI-stdio+wasmi, NOT Component Model); makepkg model; project-local `.adsmt-emitters/`; cert wire CBOR(default)/JSON
- [Design conversation pointer](design_conversation.md) — full design decisions live in .claude-conversations/
- [Ask before writing copyright notices](feedback_copyright_holder.md) — never invent / guess copyright holder names; ask first
- [A fallback that drops constraints must never report sat/unsat](feedback_soundness_opaque_fallback.md) — rc.27 P0: opaque `flatten→None` arm dropped the whole clause accumulator (empty clause from `(assert false)` included) + bailed to a theory path that can't see propositional false → unsound `sat`. Soundness asymmetry: dropping constraints preserves Unsat, destroys Sat. A path that ignored any assertion may return Unsat or Unknown but NEVER Sat. Fix = keep flattenable subset + had_opaque flag downgrades Sat→Unknown. rc.28 (S.1-AOT): the SAME bug recurred on the `--aot-load` path — grep every OTHER path (cache/AOT/JIT/restore) that re-implements the accumulate-and-verdict shape; `restore_cdcl_state_into` dropped genuine empty clauses via blanket `if !lits.is_empty()` (keep them via explicit `ok` flag — the empty clause IS the contradiction) + `had_opaque` must be threaded across the serialization boundary as a wire field. rc.29 (S.2) Tseitin completeness fix LANDED — two soundness pitfalls: aux atoms must be content-named not per-call-counter'd (counter aliases distinct sub-formulas onto one hash-consed Term → unsound), and the empty clause must stay sacred through the aux path
- [Streaming: no exit on a per-command error](feedback_streaming_no_exit_on_command_error.md) — in the persistent-solver streaming model (one lu-smt per air context, pipe + `<<DONE>>` sentinel) a mid-stream `process::exit` kills the whole verus run (reader RecvError). A read-only query command (`(abduce)`/`(get-abduct)`/`(declare-abducible)`) parse error must report (stderr) + CONTINUE, like cvc5/z3 `-in`; `--strict-commands` may still hard-fail. `(assert)` is different (dropping a constraint is soundness-sensitive). Recurred twice (2026-06-08 fast-unknown crash; rc.35 `(abduce)` exit).
- [Round-trip replay/serialize tests through the REAL producer](feedback_roundtrip_through_real_producer.md) — a hand-built payload can pass while the actual produce→encode→decode→consume path is fully broken (rc.34 §3.5 replay shipped green-tested but never fired: tests used pool indices, the real recorder writes content hashes + omits the terminal conflict). Run the real producer in the regression test; a CLI/wire end-to-end smoke is strongest.
- [Take the Arc::ptr_eq short-circuit on hash-consed types in hot paths](feedback_hashcons_hot_paths.md) — HashMap keys + structural-eq fast paths + container-shape Vec→(Index)Set + algorithmic O(N²)→near-linear. Seven measured incidents (rc.21 CdclState … rc.25 UF close() signature-hashed … rc.26 ematch extend_match/substitute_in + Combination::check NO-dedup). After rc.26 the SMT-solving hot path is FULLY de-quadratified (clean workspace grep; only cold abduction remains). THREE PROCESS LESSONS: (1) grep WORKSPACE-WIDE every cycle (rc.23 narrow grep held the wall flat); (2) after removing an O(N²) bottleneck ALWAYS re-profile — rc.24's correct ematch fix made the wall go UP 7× by unmasking UF::close() (wall-up-after-correct-fix = unblocked downstream, bisect+re-profile don't revert); (3) the throttle-unmask chain marches phase-by-phase — terminating condition is a CLEAN GREP not a flat wall
- [dynasm-rs minimum version](feedback_dynasm_min_version.md) — `dynasm` / `dynasmrt` must always pin to v5.0.0 or newer in the workspace deps; earlier majors ship a different macro grammar + `ExecutableBuffer` shape
- [Split oxiz bindings by surface](feedback_oxiz_bindings_split.md) — language bindings split into core (oxiz proper) vs contrib-* (our contribution crates)
- [portable-collections ScopedRollback kills multi-store desyncs](portable_collections_scoped_rollback.md) — ~/portable-collections git-dep; used twice on OxiZ (FlatRadixBimap term↔var `fa946ad`; VecScopedStack clause-ledger pop `3e69e15`/adsmt `2f84de6`, fixing verus-fork check-sat-poisons-abduce). Data structure makes rollback atomic, single funnel makes the forgetting impossible
- [adsmt cycle vs Cargo version](project_cycle_versioning.md) — code-comment "vX.Y" = Cargo minor version; current **1.0.0-rc.40** (2026-06-22). rc.40 = verus-fork-requested FIRST clean cut of the CCFV-redesigned engine (rc.39.3 nominally captured CCFV but shipped with the eqvars `(abduce)` hang + a g2 native spurious-`sat`; `90659cc` is the first point it's sound+complete on the verus workload, A2 **11/11**) — adsmt code = `main.rs` ONLY (goal-relevance abduce search order `abduct_goal_relevance` + batch-first `oxiz_inproc` `26ab129`; g2 empty-universe fresh-witness `d6b0d80` + abduce hang guards `fa0d2d4` landed in the .3→.40 window) + OxiZ submodule pointer `1b0d274`→`3d72b96` (the `Rational64 → Ratio<i128>` LRA/LIA migration); 1112→**1115** (+3 tests); NO `lu-smt` build/CLI change; push (main + OxiZ `0.2.4-redesign`) = user's; see [[oxiz_relationship]]. rc.39.3 = point bump (`7d9b87a`) capturing the OxiZ submodule advance through the **CCFV unified-instantiation** program (P2-flip→match_term-del→P3→P4-scaffolding, OxiZ `0.2.4-redesign`, UNPUSHED); adsmt crates unchanged (version+pins+docs; books CCFV callout ×4 langs `15ec9a3`); submodule pointer bumps left for user; see [[ccfv_unified_instantiation_design]]. rc.39.2 = verus-fork-requested clean pin cut + the **native Bool-predicate-trigger e-matching** soundness fix (latent P0): Bug 1 (`quant.rs` `collect_trigger_patterns`) now recurses through `dest_eq` so a `:pattern`-annotated `∀x.(= (P x) <body>)` yields `(P x)` as a trigger (was the whole `=`, head-mismatched against ground `(P c)` → no instantiation); Bug 2 (`cnf.rs` `rewrite_bool_iff`) encodes every Bool-sorted `=` as the iff `(and (=> a b)(=> b a))` (was one opaque atom → `(= p q)∧p∧¬q` spurious `sat`); `dest_iff` keeps EUF/arith `=` a theory atom. Small predicate-def `(check-sat)` now sound `unsat`; abduce deductive path fires the def directly (`[(ensL xc)]`). **Full-prelude UNCHANGED** (~75s `[]` — native unknown@scale already delegated, OxiZ MBQI wall = #264/#279/#280/#281; NOT shared with this fix). +7 native regs, 1105→1112; OxiZ-side corpus UNAFFECTED (no OxiZ change). rc.39.1 = adsmt point bump (`d749c8b`, local/unpushed) capturing the OxiZ submodule advance — the **clean-MBQI z3-parity completeness frontier is CLOSED** (corpus 166/168 agree, every z3=Sat decided Sat, 0 spurious; recognizers for ∀∃ skolem-witness / dense-real ×5 / real fixed-point / nested ∀∃∀ / triangular array guards + a capture-avoiding substitute-into-quantifier oxiz-core fix); adsmt crates unchanged, version+pins+docs only; see [[oxiz-mbqi-rewrite]]. rc.39 = **verus-fork abductive-residual pair CLOSED + entry into the cross-platform/docs polish phase** — (1) consistency-gate **theory-frame leak** (deep-level `(check-sat)` left per-decision EUF/arith frames un-re-based → next `(pop)` stranded an inner-scope `x~0` EUF merge → spurious `unsat`; UF⇄proof-forest desync; FIX = OxiZ `solve_with_hooks` re-bases the trail to level 0 before detaching the theory; `8ce7ed2`→`38019b0`, z3-parity, fatal=0, +2 OxiZ regs) (2) entailment-gate **streaming-vs-batch divergence** (`decide_fh`'s delegated query rebuilt F from session history + replayed the prior in-scope `(check-sat)` inside an open `(push)` → leaked → `(>= x!0)` wrongly entailed `(= x!0)`; FIX = `strip_abductive_commands` also strips interactive cmds → feed-independent; adsmt-cli `028bb85`, verus CONFIRMED A2 9/9). Both verus-non-blocking completeness/abduct-quality. PROCESS: empirical localization beat the multi-agent structural guess BOTH times. PHASE: rc.39→stable = cross-platform + docs/comments/book. rc.38 = **clean-MBQI default soundness RESTORED + a SAT-core dangling-watch fix** (the clean_mbqi default flip exposed an UNMEASURED spurious-`sat` — the instance-groundness guard dropped ground instances over declared-constant `Var` terms → fixed to test BOUND-var survival; + `eval_forall` structural-tautology fragment recovers `Sat` on `∀x.f(x)=f(x)`; + `forget_learned_since` now scrubs dangling watchers before recycling clause ids, fixing `bv_mul…_8bit` spurious-`unsat`; 2 satisfiable trigger-free cases now report the sound `Unknown`, full Sat = M3 model-completion = M4e #260; OxiZ `369a3a8`→`0d08274`, z3-verified, ZERO spurious sat/unsat, adsmt count UNCHANGED 1105; PROCESS: a partial validation set hid it a full cycle). rc.37 = vendored OxiZ **§4 redesign Phase 2 = in-process-backend default** (lock-step `TheoryHooks` driver) + **EUF↔arith spurious-`sat` fixed** (arith-fixed→EUF congruence; `Neg(IntConst)→IntConst` sanitizer); z3-verified, zero spurious UNSAT; clears verus-fork prelude trigger E/D, OxiZ submodule `0.2.4-redesign`. rc.36 = `:abduct-theory` OxiZ delegation + vendored-OxiZ fixes + CDQI (1101→1105). See [[oxiz_redesign_verification_pipeline]], [[oxiz_relationship]]. Full per-cycle history (rc.7→rc.35.1) in the topic file.
- [Abductive SMT-LIB surface (rc.35)](abductive_smtlib_surface.md) — `(declare-abducible)`/`(abduce)`/cvc5 `(get-abduct)`+`(get-abduct-next)` expose the `Abductive` verdict so a verifier can ASK what hypothesis discharges a goal; the abductive-deductive Verus integration surface (abduct = advisory, user must justify). `term_to_smtlib` flattens the curried HOL spine for re-parseable output.
- [OxiLean ↔ Lean4 syntax investigation](oxilean_syntax_investigation.md) — 2026-05-28T06:33:48Z snapshot; single `prover_emit::lean` covers both ITPs
- [prover_emit output policy](prover_emit_policy.md) — Lean = reference; Rocq + Isabelle mirror exactly; Ltac1 fully excluded for Rocq (Ltac2 only); on-demand classical-axiom imports with offline-first hard check
- [adsmt-contrib out-of-tree workspace](adsmt_contrib_repo.md) — ~/adsmt-contrib hosts adsmt-emit-rocq + adsmt-emit-isabelle; initial commit b8c80ef on 2026-05-29. rc.28 sync (f5dfe50): silently rotted against adsmt-core's rc.10 Term enum→struct reshape (gitignored Cargo.lock → no forced rebuild); migrated render_term to `t.kind()`/`TermInner::*` mirroring reference lean_emit; version 1.0.0→1.0.0-rc.28; 15/15 + 11/11 green. Re-build + re-sync whenever adsmt lands a breaking core/cert change.
- [LSP roadmap + vscode-extension split](lsp_roadmap.md) — 21F.3 = option 4: stabilize → LSP+split → v1.0 RC bump (three-phase sequence)
- [Stable sign-off requires explicit user approval](feedback_stable_signoff_user_approval.md) — never autonomously bump N.0.0-rc.M → N.0.0; ask first
- [Pre-stable feature freeze](pre_stable_feature_freeze.md) — AWAITING SIGNAL: when the user backports main→testing wholesale + signals, freeze testing at rc.40 and defer ALL new adsmt features until v1.0.0-stable (stabilization-only: bug/soundness/docs OK). Make the policy explicit in repo docs, not just memory.
- [OxiZ nonlinear-solver clean-room redesign](oxiz_nlsat_redesign.md) — DECIDED 2026-06-23: redesign NlsatSolver+NiaSolver (NIA+NRA unified) in a separate repo (z3-diff proved both broadly unsound on nonlinear unsat); same playbook as clean-MBQI/SAT-core; kickoff staged (architecture discussion next)
- [Nonlinear-solver research library](research_library_nonlinear_solvers.md) — `~/research-library/nonlinear-solvers/` = 13 papers (CDCAC, nlsat/MCSAT, incremental linearization, NIA, local search, CAD theory) for the redesign; not committed
- [z3-differential for any trust-an-unsat change](feedback_z3_differential_for_unsat_trust.md) — randomized z3/cvc5 differential, NOT a unit battery; a 13/13 battery hid a 119/600 false-unsat
- [nlsat algebraic-reduction KB](nlsat_algebraic_reduction_kb.md) — EXPERIMENT branch `0.2.4-redesign+fix-algebraic-solution` in external/oxiz (3 commits, NOT merged/pushed): a leveled SAT-only-additive reduction KB making oxiz-nlsat solve coupled equality systems with irrational/algebraic roots (fixed a pre-existing ignored-test spurious-unsat, NOT a redesign regression). Rule catalog REDUCTION_KB_RULES.md; transcendental rules = documented frontier.
- [Don't run long full-workspace tests yourself](feedback_long_test_runs.md) — scoped suites only; hand the full `cargo test --workspace` to the user via `!`
- [Always add a `-- --ignored` test pass](feedback_test_ignored_pass.md) — re-run any full/crate test suite with `-- --ignored`; ignored tests hide real bugs (the spurious-unsat that started [[nlsat_algebraic_reduction_kb]] was `#[ignore]`d). Pre-existing unrelated ignored failure: oxiz-solver `test_bv_comparison_model_generation`
- [Clean-room MBQI rewrite](oxiz_mbqi_rewrite.md) — never-conclude-unsat quantifier engine (dev'd at ~/oxiz-mbqi, M4-ported into `external/oxiz/oxiz-mbqi`). **2026-06-18: DEFAULT + SOLE engine** — legacy `oxiz-solver/src/mbqi/` deleted (#262); **M3 model completion** in `eval_forall` (definitional / pure-polarity / arith-function-completion w/ arith-aware `eval_bool` + verify-then-skip + the engine pre-enumeration skip; #264+#260 → BOTH measured trigger-free Unknowns now Sat, soundness guards Unsat); §4 four-writer anti-drift spine `retract_trail_to` (#274); `extract_linear_terms` Ratio<i128> widening (#261, fork `0.2.4-redesign+wider-rational` → backport). All on OxiZ `0.2.4-redesign`, committed locally / UNPUSHED; corpus + `cargo test --workspace` are user `!` gates. **2026-06-19: 168-case corpus sweep (the `clean_mbqi_corpus` gate had been ABORTING on `congruence_closure.smt2` OOM before its panic-classification → masked verdicts) exposed + FIXED 2 session-introduced spurious-SAT — #260 `∀x.f(x)≥0∧f(5)<0` (model poisons inequality-constrained UF-apps to default 0 → verify via top-level eq-pin chains ONLY, `resolve_eq_concrete`) + #277 pigeonhole (finite-exhaustion trusted incremental model that MISSES a global conflict → REMOVED the verdict claim, bounded ∀→sound Unknown; ackermann/grid sat→Unknown trade-off, user-approved). SPURIOUS SAT 2→0. Then **#278 fixed the 4 pre-existing spurious-UNSAT** = 2 independent bugs: Bug Y (qf_nira) QF_NIRA routed through INTEGER nlsat integerized the real var → real-sort gate `2b4740e` → correct sat; Bug X (3 array) `∃` instantiated like `∀` (`Q⇒φ[i↦42]`=`¬Q`) → engine skips existentials + active-`∃`→Unknown `bfc727c` → sound Unknown (`∃`-completeness=#279). Whole-corpus SP-UNSAT+SP-SAT now 0 (skip congruence OOM=#276). 4 OxiZ commits on `0.2.4-experiment/iteration-recognizer`.** Earlier: verus full prelude multi-trigger (B/E/D fixed); bisect oracle unsound
- [Release channel model](release_channel_model.md) — Debian-style; dev tiers = single rolling branches (unstable=main, testing=testing). Released tier split by cadence (2026-06-07): `stable` branch = rolling latest across all majors; `stable-v<major>` branch = semi-rolling LTS within one major; `v<major>.<minor>.<patch>` tags = immutable point releases. First cut tags v1.0.0 + forks stable + stable-v1. adsmt-contrib mirrors in lockstep.
- [Cross-project local request inbox](local_requests_inbox.md) — poll `.local-requests-from/<sender>/` for asks from other local projects
- [v1.0.0 scope expansion](v1_0_0_scope_expansion.md) — 2026-05-31 option A: bundle D1+L1+L2+L3+L4 into v1.0.0 cut; release timeline now leo4-gated; testing channel is consumer line until cut
- [adsmt-lean-binding out-of-tree workspace](adsmt_lean_binding_repo.md) — ~/adsmt-lean-binding hosts L1/L2/L3 implementation slot; v0.1 skeleton + JSON wire format landed 2026-06-01
- [L3 callback minimum set](l3_callback_minimum_set.md) — 2026-06-01 user-confirmed union of hints 1+2+3+3-1: AcceptanceFilter (Bool) + Labeller (Option<String>) + CostFunction (u32). Three callbacks, opt-in. Implementation = task 2 after L1 polish + tactic 실장.
- [verus-fork integration as lu-smt's primary downstream](verus_fork_integration.md) — rc.7→rc.29 driven by verus-fork. rc.27 = verus-fork rc.26 retry P0 SOUNDNESS fix (opaque assert masks `(assert false)` → unsound `sat`), fixed by (S.1)+(S.3); verus_smoke returns `unsat`. rc.28 = verus-fork rc.27 retry — §3.5.J FUNCTIONAL SUCCESS (`verus -V adsmt` → `1 verified, 0 errors` 511ms, baseline verus_smoke `unsat` 8ms, three orders inside the ≤1500ms window — the P-vb finish line + quantitative close of the rc.7→rc.27 perf arc) BUT the rc.27 (S.1) fix never reached `--aot-load`: baked opaque OR-of-AND + `(assert false)` → AOT-load drops the empty clause → `sat`-for-unsat at 1/8/16/19/24 opaque asserts. (S.1-AOT) extends it: `restore_cdcl_state_into` keeps genuine empty clauses (explicit `ok` flag) + trailing v1.2 `CdclSection::had_opaque` wire field → `Solver::aot_prelude_had_opaque` → seeds `check_ground`'s `had_opaque`. Divergence closed; 951/951 green. CONFIRMED by verus-fork rc.28 retry (mirror 6491a58): all three paths (baseline/AOT/JIT) sound — full verus_smoke `--aot-load` → `unsat` 13ms, §3.5.I AOT env path (`VERUS_ADSMT_AOT_LUART` → `--aot-load`) drives `verus -V adsmt` → `1 verified, 0 errors` 530ms — §3.5.I proven sound end-to-end, DONE. rc.29 = (S.2) Tseitin OR-of-AND completeness, CONFIRMED on all three paths (mirror 78a7bf5). STATUS: §3.5.H ALSO DONE (verus-fork `5533adfe`, frontend-agnostic `scripts/aot-bake-prelude.sh` + `just aot-bake-prelude`, NOT a vargo hook — bake→activate→verus `1 verified, 0 errors` 292ms vs 511ms unbanked). **Every technical item across rc.7→rc.29 is now landed on BOTH sides**; the ONLY remaining v1.0.0-stable-cut gate = formal audit-sweep scope (sign-off-holder's call) + explicit user sign-off. POST-arc (rc.30-34, verus-fork pins EXPECTED_ADSMT_VERSION): rc.30 Y4 surface + OxiZ delegation; rc.32.x P0 theory-atom soundness + OxiZ simplex; rc.33 cert-emit gaps A+B; rc.34 = §3.5 JIT-on-AOT replay mechanism (adsmt-side) — BUT verus-fork's §3.5.J run showed it never fired (consult always fell through); **rc.34.1 (`deb7e11`) fixed it** — two bugs the rc.34 unit tests masked (recorder content-HASH vs replay pool-INDEX → always `diverged`; root conflict never recorded → `root_conflict` false). **§3.5.J CONFIRMED by verus-fork on rc.34.1** (tight-rlimit rows flipped to `unsat`, rlimit-independent; arc functionally closed; wall win fixture-gated). **rc.34.2 = slim-trace (verdict-only) `--jit-trace-emit-slim`** (drop the event stream — turned out 0.6% of a prelude trace). **rc.34.3 = signature digest** (the real lever): a 32-byte clause-set K12 digest (`jit_trace_digest`, `lu_common::k12`) replaces the 99.4% megabyte GF(2) basis — `.lutrace` v2 trailing `signature_digest`, consult hashes the canonical clause set without the polynomial encoding; 1074 green. **rc.34.4 = incremental clause-fold digest** — verus-fork re-baked rc.34.3 + found the consult wall still ~0.42s (the whole-formula re-canonicalise per `(check-sat)`); now an AdHash `(sum,count)` clause-fold (per-clause K12 by ATOM NAME, mod 2²⁵⁶ not XOR) with the **prelude fold precomputed at `--aot-bake` into bank v1.3 `prelude_clause_fold`** → consult is O(query delta). `.lutrace` unchanged; 1080 green. **rc.34.5 = precomputed prelude atom map** — verus-fork re-baked rc.34.4 + found the consult wall STILL ~0.38s: the digest went O(delta) but `live_atom_map()` (§3.5.F replay resolver) rebuilt a map over the whole bank∪query formula every consult. Fix (lever 2): precompute the prelude atom map once at `--aot-load` (`aot_prelude_atom_map`) + chain a per-query map over it via a resolver closure (`replay_events` → `resolve: impl Fn`); slim traces never touch the prelude → consult O(query delta), `(3)−(2)`≈0. No wire/bank change; 1082 green. See [[jit-aot-replay-section-3-5]].
- [§3.5 JIT-on-AOT replay (mechanism rc.34, fired only after rc.34.1 fix)](jit_aot_replay_section_3_5.md) — replay a recorded CDCL trace at `(check-sat)` to skip the prelude search. §3.5.F replay+consult, §3.5.E canonical GF(2) **exact-match** verdict cert (NOT ideal-subset — non-Gröbner `reduce` unreliable). Unsat-only, cache-trust like `--aot-load`. rc.34 mechanism did NOT fire end-to-end; **rc.34.1 `deb7e11` fixed two bugs** (recorder content-HASH vs replay pool-INDEX → `diverged`; root conflict never recorded) the rc.34 unit tests masked by hand-building trace payloads. PROCESS LESSON: round-trip replay tests through the REAL producer. Fires on exact-formula re-runs (§3.5.J 5-mode); cross-query reuse = §3.5.C seed follow-up. **§3.5.J CONFIRMED on rc.34.1** (verdict-independent; arc closed; wall win fixture-gated). **rc.34.2 = slim-trace `--jit-trace-emit-slim`** (verdict-only emit: signature + synthetic terminal conflict, drop the propagation stream, MB→hundreds of bytes). **rc.34.3 = signature digest** — a 32-byte clause-set K12 digest (`jit_trace_digest`) replaces the 99.4% megabyte GF(2) basis; `.lutrace` v2 trailing `signature_digest`. **rc.34.4 = incremental clause-fold digest** — the digest goes O(query delta): per-clause K12 by ATOM NAME → `(sum,count)` AdHash (mod 2²⁵⁶, not XOR; exact multiset homomorphism), **prelude fold precomputed at `--aot-bake` into bank v1.3 `prelude_clause_fold`**, per-`(check-sat)` folds only the query delta; `.lutrace` unchanged. **rc.34.5 = precomputed prelude atom map** — the LAST prelude-scale consult term: `live_atom_map()` (the replay's content-hash→Term resolver) rebuilt a whole-formula map every consult; now precomputed once at `--aot-load` (`aot_prelude_atom_map`) + a per-query map chained over it via `replay_events(resolve: impl Fn(u32)->Option<Term>)`; slim traces never touch the prelude → consult O(query delta), `(3)−(2)`≈0.
- [NBG → FOL/HOL translation as adsmt self-challenge](nbg_fol_hol_challenge.md) — translate Gödel-18 / Bernays-8 finite NBG axiomatisation into adsmt-core's HOL surface so adsmt ingests NBG directly; class-quantification mapping + finite-schema preservation; independent of §3 sub-cycles, no roadmap slot yet.
- [Higher-order SLD non-pattern deferral](ho_sld_nonpattern_deferral.md) — HO abductive SLD covers only the Miller Lλ pattern fragment (non-pattern flex heads return sound `false`); constraint-postponement handling deferred to v2.0.0-rc.1+ (decided 2026-06-08)
- [OxiZ SAT core redesign](oxiz_sat_core_redesign.md) — open-ended: pure-SAT 1-UIP soundness bug fixed (cc3872c); design = integration-layer clean-room, not full rewrite; Phase 0 DONE (5527290), design doc reviewed+revised (3312eb5)
- [OxiZ redesign verification pipeline](oxiz_redesign_verification_pipeline.md) — [선검증→구현→후검증]: Verus pre-verification (L2 + UNSAT soundness) of §4 DONE (28 verified, submodule external/oxiz-sat-redesign-verification); branch 0.2.4-redesign; Phase 1 next
- [Prefer Arc<[T]> over Arc<Vec<T>> for read-only shares](feedback_arc_slice_over_arc_vec.md) — `Arc::<[T]>::from(vec.as_slice())`; single exact-sized alloc, no Vec header/capacity slack (read-only/non-growable only)
- [AOT/JIT profile finding](aot_jit_profile_finding.md) — prelude-scale native solve is ~75% term/type-DAG construction + hash-consing, <5% solving; AOT-load already saves ~62%; JIT kernel/meta-tracing targets the <5% (low payoff, "진짜 meta-tracing 불필요" confirmed). Reframes the `portable-algebraic-aotjit` extraction → AOT state-reuse is the value; hybrid = prelude-method ⊕ per-query-trace by interned-node handle. **rc.39.2 measured: `--aot-load` = 37× (75s→2s) on the full-prelude `(abduce)` repro** (per-subset `decide_fh` rebuilds F → AOT interns prelude once; verdicts byte-identical, `[]` unchanged); `--jit-trace-load` adds 0 there (consult only short-circuits top-level exact-match unsat, not the abduce path).
- [OxiZ upstream MR draft](oxiz_upstream_mr_draft.md) — drafted (not opened) the upstream PR for the §4 redesign; `external/oxiz/MERGE_REQUEST_DRAFT.md` (uncommitted); 3-layer lineage streaming-stdin→cdqi→redesign; 5-tier split; companion repos newsniper-org/{oxiz-sat-redesign-verification, oxiz-fuzz-harness}; awaiting upstream response
- [OxiZ audit findings 2026-06-20](oxiz_audit_findings_2026_06_20.md) — 13-agent adversarial audit: CCFV SOUND; div/mod-as-subtraction false-proof FIXED (`8de523a`); SAT-core binary_graph/pop watcher-leak (spurious-unsat) + nlsat Sat-gate (spurious-sat) CONFIRMED-OPEN; + simplex/combination/datatype soundness-relevant items
- [Multi-paradigm hybridization](multi_paradigm_hybridization.md) — vision+decisions for adsmt as a multi-paradigm substrate: a strict-superset lang (SMT-LIB-3.0 ⊕ typed-Datalog/ASP, def/open, stable-from-start, FO(·) on 3.0 CIC kernel) on one CDCL/MCSAT trail+propagators; UNIFIED VERIFICATION GATE spine (doc AD1 `c5f8355`); 선검증 keystone `answer_set_gate.rs` VERIFIED (oxiz-nl2-verification `c262c2c`)
