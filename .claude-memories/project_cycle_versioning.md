---
name: adsmt-cycle-vs-cargo-version-mapping
description: "Cycle names referenced in code comments (\"v0.5\", \"v0.13\", \"v0.15\") map directly to adsmt's Cargo workspace minor version. Current cycle is 1.0.0-rc.34.1 (2026-06-10; rc.34.1 = **§3.5.J fix** — the rc.34 §3.5.E/F replay mechanism did NOT actually fire end-to-end; verus-fork's §3.5.J run showed the consult never short-circuited. TWO bugs the rc.34 unit tests masked (they hand-built traces with pool *indices* as atoms): (A) recorder writes `atom_key_hash_u32(term)` content-HASH but `replay_events` indexed `aot_pool_terms[atom]` POSITION → always `diverged` (+ bank-only pool omitted per-query atoms); (B) CDCL returns Unsat on a ROOT conflict without recording a terminal `Conflict` → `root_conflict` false. Fix `deb7e11`: `replay_events(events, atom_map: &HashMap<u32,Term>)` via new `Solver::live_atom_map()` over bank∪query; session-boundary fallback appends `Restart`+level-0 `Conflict` to non-empty Unsat traces; backstop gated to empty-signature+collision-free. + regression `real_recorder_trace_replays_through_hash_atom_map` (real recorder→replay round-trip, the missing test). CLI-verified end-to-end; 1070 green. PROCESS LESSON: round-trip replay tests through the REAL producer, not hand-built payloads. See [[jit-aot-replay-section-3-5]]. PENDING verus-fork §3.5.J re-run on rc.34.1. PRIOR rc.34 (2026-06-10) = §3.5.A–G mechanism (§3.5.F real event replay + `(check-sat)` consult; §3.5.E canonical GF(2) signature **exact-match** verdict cert, NOT ideal-subset-via-reduction since non-Gröbner `reduce(x,[1+x,x])`→1) — but see rc.34.1, it didn't fire end-to-end until the fix. PRIOR rc.33 (2026-06-09; rc.33 = close the verus-fork R7.11 **cert-emit pipeline** gaps A + B. **Gap B** = flatten `Term` serialization (`adsmt-core/src/serde_impl.rs`): the recursively-nested `TermWire` shadow made CBOR depth ∝ term size, so a prelude-sized cert blew **ciborium's decode recursion limit** at `adsmt-emit-contract::decode` (rejected before the ITP render). Now a topologically-ordered, dedup'd **hash-cons pool** (`Vec<FlatTermNode>`+`Vec<FlatType>` with `u32` index refs, `FlatTerm{types,terms,root}`) → decode depth O(1), shared subterms pooled once (wire shrinks), still rebuilt through hash-cons ctors on deserialize (forward/out-of-range indices + bad kinds rejected); ciborium added as adsmt-core dev-dep + a 1000-deep-term test. **Gap A** = `Solver::build_delegated_unsat_cert(delegate)` synthesises a cert for an OxiZ-**delegated** unsat (real Poly/fuel obligations native can only decide via delegation, where `certificate:None` made `--emit-cert*` a no-op): each assertion → `Assume`, closes `⊢ false` with an `oxiz-delegation` `TheoryWitness::Opaque` (emitter renders it as an axiom — adsmt trusted the delegate, same status as the SAT/DRAT step), built on a fresh `CertBuilder`; CLI delegation branch builds + `write_emit_cert`s it directly (native dispatch already passed `record_result`). Verified end-to-end (`(or (< x 0)(> x 0))∧(= x 0)` + `ADSMT_OXIZ_PATH` → native Unknown → OxiZ unsat → `1.cert.cbor` written). 1034→**1057** green. **NOTE: adsmt-contrib Isabelle/Rocq emitters must be REBUILT against the new flat cert format** (no code change — `git=testing` dep rebuild; gated on the adsmt push to testing). Doc/comment/book sweep: books carry no cert-wire-format content (version refs historical, accurate); README gained an rc.33 row, PORTFOLIO current-state labels → rc.33. See [[adsmt_emit_system]]. PRIOR rc.32.3 = move the `external/oxiz` submodule to the **0.2.4 base** — testing showed upstream 0.2.4 already fixed the rc.32.2 simplex pop/tableau bug with the IDENTICAL approach (their `saved_tableaux` = my `cached_tableaus`/`cached_basic`), so my `102e377` patch is redundant there; submodule now tracks `0.2.4-feat/streaming-stdin` (`26b8454` = upstream/0.2.4 + streaming-stdin work minus the patch, adsmt `80896bb`), Cargo.lock oxiz 0.2.2→0.2.4, in-process `--features oxiz` compiles clean against the 0.2.4 API; upstream-MR plan revised (`docs/upstream/oxiz-streaming-stdin-pr.md` — simplex fix dropped, streaming-stdin features only) + verus-fork reply §5 addendum; `EXPECTED_ADSMT_VERSION` → rc.32.3; cert wire + `-V adsmt` verdicts unchanged; bump `7c553e4`. See [[oxiz_relationship]]. PRIOR rc.32.2 = verus-fork P0 — native path returned a confident **unsound `sat`** for theory-`unsat` formulas like `(and (> x 0) (< x 0))` because arithmetic atoms were abstracted to free booleans. THREE root causes fixed: (1) `Combination::assert` (polite.rs) routed comparison atoms by their Bool result sort → never reached LinArith; now route `<,<=,>,>=` by operand sort; (2) the theory check routed top-level `lits`, stripping `(and …)` wholesale so conjuncts never reached theories — now `collect_forced_literals` descends through asserted-true `and` + De Morgan duals (routing only *entailed* literals keeps `unsat` sound), then a **two-stage** check validates the SAT model's chosen disjunct atoms and returns `Unknown`→delegate (not unsound `sat`) on a model-infeasible conflict (no DPLL(T) refinement loop natively); (3) soundness backstop — a sort-specialized theory ignoring a non-equality atom over its sort downgrades `Sat`→`Unknown` (rc.27 S.1 `had_opaque` generalized to theory atoms), with LinArith now accepting positive equality as conjoined bounds to close the `(= x 5)∧(= x 6)` numeral-distinctness gap. The audit then surfaced + fixed a SECOND, independent soundness bug **inside OxiZ's simplex** (`external/oxiz` submodule, branch `0.2.3-feat/streaming-stdin`, commit `102e377`): `pop` didn't restore the tableau/basis pivoted by `check()`'s `make_feasible` at a higher decision level, so `(or (< x 0) (> x 0)) ∧ (= x 0)` returned a spurious `sat` after backtracking the first disjunct — fixed by snapshotting tableau+basic on push, restoring on pop. adsmt commits `2bfee35` (native) + `2a315be` (get-model phrasing, verus-fork §6) + `49e4ae2` (rc.32.2 bump + submodule pointer); 11 adsmt regression tests, 1040→**1051** green; OxiZ oxiz-theories 1364 + oxiz-solver 684 green. See [[oxiz_relationship]], [[feedback_soundness_opaque_fallback]]. PRIOR rc.32.1 = hotfix on the rc.32 tag — Arch PKGBUILD fix so the rc.31 adsmt-emit/adsmt-env binaries actually install into the adsmt-cli split package + Y4 reply push-note correction; no source/cert change. rc.32 (2026-06-08) was **abductive SLD** (first-order schematic rules + higher-order Miller-Lλ pattern unification for flexible heads + hypothesis-set candidate dedup) + lu-smt **`--emit-cert`** flag (CBOR-default/JSON wire, canonical `adsmt-cert::Certificate`) + the Y4 reply; rc.31 was the **WASM emitter package manager + runtime**: prover-emit backends (Rocq/Isabelle/Lean) become *packages* (a single file = `"
metadata: 
  node_type: memory
  originSessionId: 32a1dc0d-7730-4862-8df4-6958199ce84f
---

TOML frontmatter --- #!shebang BUILD-script`, PKGBUILD-style) run by a dedicated runtime, NOT system-installed binaries. New `adsmt-emit/` group: **contract** (language-neutral WIT world + cert wire codec — default **CBOR** via ciborium / JSON via serde_json; generic `encode`/`decode`); **pm** (project `adsmt-emit.toml` + `adsmt-emit.lock` + content-addressed store of `contents/` *trees* + pluggable `.tar.zst` codec [ZstdCodec; **bzip4** slot ~/bzip4, deferred v1.1+] + `build` orchestrator + resolver); **runtime** (the SOLE backend = pure-Rust **wasmi** under WASI Preview 1 — cert bytes stdin→prover text stdout, exit 0/2/3; `wasm_memory64(true)` lifts the wasm32 4 GiB wall; `Runtime::emit_many(jobs,-j N)` thread-pool shares one compiled module, per-job Store, no GIL); **adsmt-env** (managed `/usr/bin/env` replacement: interpreter trampoline + injects build `$srcdir`/`$pkgdir` when `$ADSMT_EMIT_BUILD_ROOT` set — pkgdir provided ONLY through adsmt-env); **cli** (`adsmt-emit install`/`run [targets…] [-j N] [--from-json] [--out-dir]`/`list`/`pack`); **adsmt-emit-lean** (REFERENCE port — `adsmt_cert::emit_lean` → `wasm32-wasip1` command, verified under wasmi from a real CBOR cert; **adsmt-core + adsmt-cert incl. scc compile cleanly to wasm32-wasip1**). DECISION: path A (core-wasm+WASI-stdio+**wasmi**, NOT Component Model — our cert is string→bytes so CM's typed records buy nothing; wasmi = pure-Rust/no-Cranelift/no-C, `-j N`-friendly, memory64+many extensions) over wasmtime; wizard-engine (Virgil, not Rust-embeddable)+Lunatic (actor)+WasmEdge (C++/partial-CM) poor fits. **makepkg model**: build script runs through adsmt-env → installs `.wasm` to $pkgdir → `contents/` tree content-addressed into `<cwd>/.adsmt-emitters/` (gitignored; manifest+lock committed at root) → wasmi loads `<sha>/contents/<main>`. **Single source-build tier** — the earlier "(b') dual tier, Script tier = run body as live emitter" was a MISREAD (body is a BUILD script; ScriptEmitter deleted R5). Also: **parser reorg** — `adsmt-parser` → dep-free `adsmt-parsers/{smtlib2,lfsc-drat,lu-kb}` + `adsmt-shims/shim-lu-kb`; re-export shims (`lu_common::kb`←lu-kb, `adsmt_cert::drat`/`prover_emit::lfsc_parse`←lfsc-drat) = 0 churn; `adsmt-parser-lfsc-drat` = wasm shared core. **`Certificate` serde** — adsmt-core `serde` feature: hand-written Kind/Type/Term **re-intern through hash-cons constructors on deserialize** + kind-check rejects malformed wire; `Theorem` NOT deserializable (kernel trust token → no forged proofs; cert stores Sequent). 970→**1034** green. See [[release_channel_model]] (bzip4 codec slot). PRIOR rc.30 = the Y4 request: parameterized `declare-datatypes` + the full vstd SMT surface (BV sorts/literals/ops, `let`, indexed-id apps, testers, polymorphic ctors) + canonical reason-unknown + **OxiZ delegation** (`oxiz_fallback` via `ADSMT_OXIZ_PATH`; native `Unknown`/degraded → replay buffered SMT-LIB through vendored OxiZ, take its verdict — adsmt's Path-A+B "abductive+ITP on top of OxiZ" design). RESULT: `verus -V adsmt --rlimit 30 src/lib.rs` on the Y4 tree → `54 verified, 0 errors` (= Z3). 970 green. Verus fully monomorphizes datatypes (real blocker was the Poly/fuel quantifier encoding, not datatypes — solved by OxiZ delegation). PRIOR rc.29 = verus-fork (S.2) Tseitin OR-of-AND completeness fix, the last gap before v1.0. `flatten_to_clauses` (adsmt-engine/src/cnf.rs) returned `None` on nested OR-of-AND → opaque `had_opaque` path → `Unknown` where z3 says `unsat` (sound but incomplete; witness `(or (and P (not P)) (and P (not P)))`). (S.2) = standard Tseitin: a conjunction where a flat literal list is needed → fresh aux Bool `aux ⟺ subformula`, content-named `!tseitin!<subterm>` (per-call counter would alias different sub-formulas onto the same hash-consed Term → unsound), const-folded, equisatisfiable, linear. All three paths inherit completeness (bake side bakes real clauses, `had_opaque` degrades to deadline/size only). Witness → unsat on baseline+AOT+JIT; `(or P (and Q R))` → sat; rc.27 repro + rc.28 divergence table stay unsat; 951→956 green. v1.0 cut = (S.2)[done] + full audit + explicit user sign-off. PRIOR cycle rc.28 = the verus-fork rc.27-retry (S.1-AOT) soundness fix extending the rc.27 (S.1) opaque-assert fix to the `--aot-load` path. verus-fork rc.27 retry confirmed `verus -V adsmt` → `1 verified, 0 errors` 511 ms (baseline verus_smoke `unsat` 8 ms — P-vb finish line, three orders inside §3.5.J's ≤1500 ms window) BUT found the rc.27 fix never reached the AOT-prelude-bank path: baked opaque OR-of-AND + `(assert false)` → `--aot-load` drops the empty clause → `sat`-for-unsat (baseline `unsat` vs aot `sat` at 1/8/16/19/24 opaque asserts). rc.28 fix: (1) `restore_cdcl_state_into` keeps genuine empty clauses via explicit `ok` flag (was blanket `if !lits.is_empty()`); (2) trailing v1.2 `CdclSection::had_opaque` wire field (`at_end()`-gated, v1.0/v1.1 default false) threads bake-time opaque flag → `Solver::aot_prelude_had_opaque` → seeds `check_ground`'s `had_opaque` for the `Sat`→`Unknown` downgrade. Divergence table fully closed; 2 regression tests + 1 round-trip ext; 951/951 green. CONFIRMED by verus-fork rc.28 retry (mirror 6491a58): all three paths sound, full verus_smoke `--aot-load` → `unsat` 13ms, §3.5.I AOT env path (`VERUS_ADSMT_AOT_LUART`) drives `verus -V adsmt` → `1 verified, 0 errors` 530ms — §3.5.I DONE; only §3.5.H vargo bake hook remains. rc.7→rc.28 arc essentially closed. PRIOR cycle rc.27 was the verus-fork rc.26-retry P0 SOUNDNESS fix. verus-fork confirmed the rc.26 performance milestone (deadline budget-exact at every rlimit) but found the real §3.5.J blocker was never performance: an opaque OR-of-AND assert (`(=> X (and Y Z))`, ubiquitous in verus fuel axioms) co-occurring with a contradiction returned unsound `sat`, because `check_ground`'s `flatten_to_clauses → None` arm dropped the whole clause accumulator (the empty clause from `(assert false)` included) and bailed to a theory path that never evaluates propositional `false`. This had masked the trivially-unsat verus_smoke fixture (`(assert (not true))`) across the ENTIRE rc.7→rc.26 arc. (S.1) the opaque arm keeps the flattenable subset + a `had_opaque` flag downgrades a final `Sat`→`Unknown` (Unsat stays sound: subset-unsat ⟹ full-unsat); (S.3) propositional-`false` short-circuit to `Unsat` in the theory route; 3 regression tests; 949/949 green. verus_smoke now returns `unsat`. (S.2) Tseitin-encode OR-of-AND (completeness for contradictions buried *inside* the opaque structure, which (S.1) soundly returns `Unknown` for) is the next follow-up. Soundness lesson captured in `feedback_soundness_opaque_fallback.md`. PRIOR cycles rc.21→rc.26 were a single throttle-unmask chain (each correct hash-cons-hot-path fix exposed the next-slowest phase): rc.21 CDCL String keys → rc.22 term/type α-eq → rc.23 UF membership → rc.24 ematch universe → rc.25 UF `close()` signature-hashed congruence + (T0''') theory-phase deadline cascade → rc.25-retry UF `derive_equalities` HashSet dedup (USER-landed `6a3f0cd`/`6dc6f7c`) → rc.26 E-matcher tail: (e⁗⁗.3) `ematch::extend_match`/`substitute_in` + `quant_conflict` matcher binding `alpha_eq`→`==` (ground); (e⁗⁗.4) `Combination::check` Nelson-Oppen `Vec`+`iter().any` → `HashSet<(Term,Term)>` norm_pair dedup (4.9 % of cycles); (T0'''') `extend_with_equalities_until` per-equality deadline. MILESTONE at rc.26: the SMT-solving hot path (CDCL → theory combination → UF → quantifier E-matching) is FULLY de-quadratified; a workspace-wide grep for production `iter().any(.*alpha_eq` comes back clean (only comments + tests + cold abduction, which is off the SMT path + deliberately left). verus-fork rc.25 retry confirmed `:rlimit` is now EXACT (rlimit 1 s → 1 011 ms). Per-cycle detail in the table rows below.  Workspace grew the parser/shim/emit groups at rc.31; 1034 tests, 0 cargo doc warnings.
type: project
originSessionId: 32a1dc0d-7730-4862-8df4-6958199ce84f
---
adsmt's development happens in *cycles* whose names appear as
`vX.Y` in source comments. **A cycle name IS the Cargo
workspace minor version at that time.** Concretely:

| Cycle | Cargo version when cycle landed | Focus |
|---|---|---|
| v0.1 | 0.1.0 | core kernel + cert skeleton |
| v0.3 | 0.3.0 | quantifier loop, lu-kb assertion routing |
| v0.5 | 0.5.0 | theory layer alpha (BV/FP placeholders), CaDiCaL backend |
| v0.7 | 0.7.0 | source-position scaffolding, recorder maturation |
| v0.9 | 0.9.0 | SAT backend survey, oxiz fork preparation |
| v0.11 | 0.11.0 | P1 — `oxiz_backend` |
| v0.13 | 0.13.0 | P2 — oxiz-math wired, hand-rolled LIA retired |
| v0.15 | 0.15.0 (closed 2026-05-16) | P3 — proof byte-form pipeline, source-loc end-to-end, Lean reflection initial, oxiz-contrib-abduction + oxiz-contrib-bindings spinouts. Archival branch `v0.15` pinned at merge SHA |
| v0.17 | 0.17.0 (closed 2026-05-29) | P4 — upstream coordination, prover_emit refactor for OxiLean + Lean4 co-equal sibling targets (Rocq and other ITPs out-of-tree, per 2026-05-28 user directive), cert text-emission (NOT FFI), compound-rule reconstruction, LFSC proof term reconstruction, E-matching congruence-closure deepening, Tier 3 quantifier enumeration, BV bit-level fact propagation, Arrays extensionality, LinArith Fourier-Motzkin closure + Simplex bridge, abductive SLD chaining, parser lu-kb integration. Language-binding work DEFERRED until user's `leo4` library v1.0 (local repo `~/leo4/`). |
| v0.18 | 0.18.0 (closed 2026-05-29) | Classical-axiom-imports (on-demand) pipeline per ypeg's 2026-05-29 request: per-step `should_import_classical` / `allow_to_import_classical` markers with `(lazy, scan)` truth table, four-layer additive attachment, closed-enum `StepPattern` + desugar helpers, hierarchical classical-module families + per-ITP precise variants, strict hard-failing emit-time check, pair-to-pair parent inheritance, `adsmt-heuristic-checker` subcrate + `adsmt-heuristic-checker-macros` proc-macros + `adsmt-lints` (runtime audit + JSON for IDE), adsmt-minimum heuristic lu-kb table + 8-layer offline safeguard (σ+γ+ε+ι+κ+π+τ+λ) with KangarooTwelve-256 double-pass frozen hash via `lu-common::k12`, mid-block + pattern-marker cert AST, v0.x exclusion policy adopted (pre-1.0 is out of scope for the safeguard). Follow-up deepening: A/B/C/F/G/H/I/J + K (Rocq Trans/EqMp only)/L scaffold/M lightweight + P/Q operational. |
| v0.19 | 0.19.0 (closed 2026-05-29) | P5 — v1.0 architectural decision **opened** + v0.18 carry-over deepening + DPLL(T) engine maturation + theory deepening + quantifier work + tooling + CI + benchmarks. **Landed**: K-full compound-rule proof terms (Lean+Rocq+Isabelle); A.3 partial — trigger-learning helper (`learn_triggers` greedy depth-ordered cover, full incremental E-graph deferred to v0.21); A.4 J-full proc-macro OUT_DIR auto-use; A.5 scan=true two-pass wiring; B.1 built-in DPLL fallback under Luby restarts; B.2 dpllt eager-conflict short-circuit; B.3 Luby restart sequence + dpll_with_restarts (full 1-UIP CDCL deferred to v0.21); B.4 nested push/pop audit; C.1 BV bit-blasting to CNF (`adsmt-engine::bv_blast`, bvand/bvor/bvxor; arithmetic ops deferred); C.2 LinArith introspection API; C.3 Arrays store-store normalisation; C.4 Datatypes per-constructor arities + injectivity propagation; C.5 Polite multi-sort + disagreement diagnostic; D.1 Tier 4 abductive escalation via minimize+rank; D.2 HOL quantifier instantiation tests; D.3 Abductive SLD typed-arg unification (SchematicHornRule); E.3 C ABI header + policy; F.1 VS Code extension scaffold; F.2 lu-smt --audit-json; G.1 criterion benchmark suite; G.2 GitHub Actions CI; G.3 status timeseries; G.4 contrib docs polish; H.1 mid-cycle memory + layout audit. **Carried over to v0.21** (substantial): A.2 LFSC parser, A.3 full E-graph, B.3 full 1-UIP CDCL, C.1 BV arithmetic bit-blasting. **Carried over to v0.21** (decision-blocked): E.1/E.2/E.4/F.3/H.2. |
| **v0.21** | **0.21.0 (current — opened 2026-05-29)** | **Post-v0.19 cycle.** Headline theme is the P5 v1.0 architectural decision (21E.1/E.2/E.4/F.3/H.2 — all carried over from v0.19, all blocked on user decision). Substantial carry-overs: 21A.1 LFSC parser scaffold + per-ITP consumer, 21A.2 full EUF-tracked incremental E-graph + per-trigger learning loop, 21B.1 full 1-UIP CDCL clause learning + non-chronological backjumping. New: 21C.1 BV arithmetic bit-blasting (bvadd/bvsub/bvmul) — depends on 21B.1's Tseitin aux allocator. Tooling: 21G.1 criterion re-baseline under the new restart loop. 21H.1 cycle audit. logicutils v0.x-smt bumps 0.21.0 → 0.23.0 in lockstep (+2 offset preserved; 0.22 skipped, same convention as 0.19/0.20). **Progress as of 2026-05-30 (autonomous-track close)**: 21G.1 baseline + LIA bench recorded ✓; 21A.1 LFSC byte-stream parser (`adsmt-cert::prover_emit::lfsc_parse`) + per-ITP consumer (Lean/Rocq/Isabelle, kernel-valid `axiom`/`Axiom`/`axiomatization` lines via `sort_to_*` + `term_to_*` lowering including lambda/Pi/annotation/holds/true/false) ✓ (deeper proof reconstruction still queued for v0.23); 21A.2 stages 1-4 + Solver wiring TODO — hash-cons + UF + congruence cascade + `terms_in_class` / `as_universe` bridge to `EMatcher` + `push`/`pop` scope stack ✓; 21B.1 stages 1-4 + follow-ups — trail backbone + 1-UIP + learnt clauses + non-chronological backjump + Luby restart wrapper + VSIDS + clause deletion + activity-based retention + LBD glue protection + phase saving + `cdcl_solve_with_model` API + Solver fallback wired to `cdcl_with_restarts` ✓ (two-watched literals + LBD-based restart triggers queued for v0.23); 21C.1 BV arithmetic — ripple-carry `bvadd` + 2's-complement `bvsub` + shift-and-add `bvmul` ✓; 21H.1 mid-cycle audit ✓; C.3 follow-up: Arrays store-store normalisation now wired into `Arrays::assert` ✓. **All autonomous v0.21 tasks landed.** Remaining pending = decision-blocked: 21E.1 (adsmt ⇔ OxiZ), 21E.2 (v1.0 unification plan), 21E.4 (first v1.0 breaking attribute), 21F.3 (LSP exploration scope), 21H.2 (v1.0 transition notes).
| **v0.23** | **0.23.0 (current — opened 2026-05-30)** | **Phase 1 of the v1.0 RC roadmap** (`lsp_roadmap.md`). Headline theme: **preemptive stabilisation** — every public surface that LSP (phase 2) and v1.0 RC (phase 3) will depend on gets frozen here. **Cycle close (2026-05-30)**: 23A.1 C ABI freeze ✓ (commit `1bb5035`); 23A.2 SMT-LIB / lu-kb dialect freeze ✓ (commit `2bf966a`); 23A.3 Certificate format freeze ✓ (commit `e7e058d`); 23B.2 EgraphTheory wrapper ✓ (commit `b067499`); 23H.1 mid-cycle audit ✓; 23H.2 Phase 1 sign-off ✓ (`lsp_roadmap.md` §"Phase 1 sign-off (23H.2, 2026-05-30)"); 23C.1 theory deepening = BV bvnot + bvneg unary ops + bit-blast lowering with reusable `ripple_carry_add_with_carry_in` helper ✓ (commits `9051428`, `ba95b18`). **Carry-over to v0.25**: 23B.1 CDCL two-watched literals (substantial restructure — defers to phase 2 cycle so the LSP work and the inner-loop rewrite don't collide). Workspace lib test count grew from 76 (v0.21 close) to 83 (v0.23 close). logicutils v0.x-smt at 0.25.0 — last separately-versioned release before v1.0 absorption. |
| **v0.25** | **0.25.0 (current — opened 2026-05-30)** | **Phase 2 of the v1.0 RC roadmap** — LSP implementation + vscode-extension split. **All 6 LSP capabilities landed**: 25LSP.1 adsmt-lsp crate scaffold (tower-lsp lifecycle, `9550e93`) ✓; 25LSP.2 textDocument/publishDiagnostics from SMT-LIB parser (`78f2017`) ✓; 25LSP.3 textDocument/definition + symbol index (`aeecd5e`) ✓; 25LSP.4 textDocument/hover + 1-based↔0-based position fix (`913a0d5`) ✓; 25LSP.5 textDocument/completion with 39-item static table (`7ae1bfd`) ✓; 25LSP.6 workspace/symbol indexing (`f7c3439`) ✓; 25LSP.7 textDocument/codeAction placeholder for kb migration (`06d1a20`) ✓; 25EXT.1 vscode-extension split + LSP client wiring (`feda783`) ✓. **Phase 2 mandatory completion achieved.** logicutils stays at 0.25.0 (last separately-versioned release per 21E.2 option 2-A'). **Carried over**: 25B.1 CDCL two-watched literals (substantial inner-loop restructure — deferred to phase 3 RC prep cycle so v1.0 RC bump isn't gated on it). |
| v1.0.0-rc.1 | 1.0.0-rc.1 (closed 2026-05-31) | **Phase 3 of the v1.0 RC roadmap — all 6 RC1.* tasks landed.** RC1.1 sign-off re-verify ✓; RC1.2 CDCL two-watched literals ✓ (`03229c0`); RC1.3 1.0.0 marker → real attribute ✓ (`eed2427`); RC1.4 infra + RC1.4.A atomic logicutils absorption ✓ (`d04e4fc`, `cb34793`); RC1.5 adsmt-meta metacrate with no-cli/only-cli/full features ✓ (`728ba00`); RC1.6 lu-common::migration::v0_to_v1 chained converter ✓ (`931a59c`); RC1.H.1 mid-cycle audit ✓; RC1.H.2 RC sign-off ✓ (`lsp_roadmap.md` §"Phase 3 sign-off"). Workspace now contains 14 adsmt-* + 12 absorbed lu-* + vscode-extension under tooling/. |
| v1.0.0-rc.2 | 1.0.0-rc.2 (closed 2026-06-01) | Pre-stable audit cycle (feature-matrix / publish dry-run / warning sweep / doc audit / bench / LSP smoke / contributions audit). RC2.H.2 sign-off held per `feedback_stable_signoff_user_approval.md`. |
| v1.0.0-rc.3–rc.6 | 1.0.0-rc.{3..6} (closed 2026-06-01) | typed-enum hot patch chain (see `thoughts/` revision 6). |
| v1.0.0-rc.7 → rc.10 | 1.0.0-rc.{7,7.1,8,8.1,9,10} (closed 2026-06-04) | **Driven by `verus-fork` integration** (see `verus_fork_integration.md`) + a parallel lu-smt placeholder sweep. Landed in order: **Phase 1/2/3 placeholder sweep** (`9418640`/`6de643d`/`13e6cde`, lu-smt CLI rewrite + engine Model/UnsatCore surface + cert/engine LFSC reconstruction via Alethe→LFSC converter); **echo command** (`8b3e388`, `(echo "msg")` for response-batch sentinel); **ranked abductive JSON** (`adac31d`, `SatResult::Abductive { candidates: Vec<RankedCandidate> }` with score preserved through engine boundary into stdout JSON line); **rc.7 bump** (`c06a139`); **parser + CLI + core quantifier surface** (`3500e65`, `forall`/`exists` in convert_expr + `Term::mk_exists`/`dest_exists` in core + `declare-fun` arity > 0 fix via `validate_sort_only_refs`/`declare_fn_type` split, also fixes a regression where declare-fun with any params errored "malformed parameter binder"); **engine NNF + Skolemization** (`460d7b5`, new `adsmt-quant::skolemize` module: `nnf` / `skolemize` / `normalize_for_engine` / `contains_quantifier`; wired into `Solver::assert_with_polarity_at` so all assert paths get normalized; `quant::instantiate_one` rewires trigger choice via `collect_trigger_patterns` to walk through `not/and/or/=>`); **stdin streaming** (`602192a`, lu-smt now ships per-command output instead of buffering to EOF); **Verus prelude surface widening** (`63ad893`, `(! expr :keyword v)` § 3.3 attributed expressions, numeric literals, +/-/*/div/mod/abs / `</<=/>/>=`, distinct, `/` Real division — all routed through existing arith/EUF theories); **deadline-aware check-sat** (`dd233a7`, `(set-option :rlimit N)` / `:timeout N` honored, threaded through `Solver::check_sat_with_deadline` → `check_ground_with_deadline` → `cdcl::cdcl_with_restarts_deadline` / `cdcl_with_restarts_with_model_deadline` / `cdcl_solve_with_model_deadline` at every restart boundary + 256-iter cadence inside the model-carrying loop; clears on `(reset)` via `Options::default()`); **verus-fork engine refactor R1→R2→R3 + §2.3 hash-cons** (`855c01a`/`231777a`/`322308d`/`2b765d2` — Term enum → `Term(Arc<TermInner>)` struct with `pub enum TermInner`, App/Lam children = bare `Term` (no inner `Arc<Term>`), PascalCase associated constructors retained; ~214 pattern sites migrated to `match t.kind() { TermInner::… }`; then `scc::HashIndex 3.7.1` global hash-cons cache with `Weak<TermInner>` values for auto-GC; `Term::PartialEq` = `Arc::ptr_eq`, `Hash` = pointer hash → `gather_subterms` per-node Hash/Eq O(1), cumulative O(N) instead of O(N²)); reply mirrored to verus-fork (`7b26047`). workspace test count: 129 engine + 44 cert + 35 parser + others = **754 lib/integration tests at the end of rc.10**. |
| v1.0.0-rc.11 → rc.12 | 1.0.0-rc.{11,12} (closed 2026-06-04) | rc.11 = bump + memory sync (`d146a82` + `545a547`). rc.12 = **(get-info :reason-unknown) Z3-canonical mapping** (`05a3214` parser + dispatcher routes through Z3's `"canceled"` / `"unknown"` / `"timeout"` / `"resource"` / `"interrupted"` reason set so verus `ValidityResult::Canceled` bookkeeping fires) + **T0 deadline cascade into `propagate_two_watched` inner loop** (`c5964db` — earlier deadline catches only at outer CDCL boundaries; T0 adds an in-loop check per literal-watch iteration so the 1 s `:rlimit` budget catches without 5+ s overshoot). rc.12 bump `a3aa4e4`. verus-fork ran a smoke matrix retry at rc.12; structural path complete but `~5.32 s` wall-clock floor surfaced for `--rlimit 1 s`. |
| v1.0.0-rc.13 → rc.14 | 1.0.0-rc.{13,14} (closed 2026-06-04) | rc.13 = **§3.4 Buchberger v0** — dense Gröbner-basis decider in new `adsmt-theory-finite-field` crate (`bde2f8c` → `98159c1`: monomial / polynomial / s_polynomial / reduce / Buchberger main loop + SAT-to-GF(2) encoder, `pigeonhole_3_into_2_is_unsat` regression). Bump `db05c14`. rc.14 = **§3.4 F4 v1** — bit-packed Gröbner (`3ecf7eb` → `cada5a3`: SmallVec<[u64;4]> bit-packed monomials + F4 batched pair selection + symbolic preprocessing + Gauss reduction; `buchberger_and_f4_agree_on_*` regression tests confirm both backends agree) + **§3.4 Combination::register integration** (`5ca3de7`: `FiniteFieldTheory` Theory trait impl + `Solver::with_finite_field` builder + budget-exhaustion `force_check` hook with periodic + budget-exhaustion knobs; 94 plugin tests pass) + **§3.1 AOT prelude bank counter-proposal filed** (`8ba77e1` proposes `lu-smt --aot-bake` / `--aot-load` + `.luart` v0 binary layout; asks verus-fork to ack CLI shape + build-cache convention + SHA scheme). Bump `af04b6e`. |
| v1.0.0-rc.15 | 1.0.0-rc.15 (closed 2026-06-04→2026-06-05) | **rc.15 cycle = §3.4 CLI surface + §3.1 AOT prelude bank end-to-end + §3.2 + §3.3 skeletons.** T1.1 `--finite-field-periodic <N>` / `--finite-field-budget-exhaustion` CLI flags (`e0e3f77`); T1.2 `(set-option :finite-field-…)` mid-session SMT-LIB handler with auto-register on first call (`50931f2`); §3.1.A `adsmt-aot` scaffold + `.luart` v0 writer (`a547a5b` + `0eebf57`); §3.1.B `lu-smt --aot-bake / --aot-output / --aot-sha` CLI (`699bd5b`); §3.1.C `.luart` v0 reader + Term-DAG reconstruction via hash-cons re-intern + minimal `Type::Display` inverse parser (`941163d`); §3.1.D `Solver::with_aot_prelude(ReconstructedPrelude)` builder + `intern_external(&Term) -> Term` helper + `lu-smt --aot-load` CLI (mutually exclusive with `--aot-bake`) (`38fd8ee`); §3.2 `adsmt-jit` crate skeleton with `JitGuard` enum (PolyInvariant via shared GF(2) `reduce` / EquivClass / SkeletonShape depth-3) + `JitCache::lookup` (`d11aafb`); §3.3 `adsmt-stalmarck` crate skeleton with `ImplicationGraph` + `Saturator::saturate_simple` transitive closure + `detect_contradiction` BFS witness (`52efc77`); bump `c53ec60`; docs `2b4d2da` + `34dba51`. verus-fork rc.15 5-mode smoke matrix localised the `~5.3 s` floor *inside `(check-sat)` itself* (Mode C `--aot-load` invariance vs Mode A baseline). verus-fork filed §3.5 JIT-on-AOT-prelude design 2026-06-04; adsmt-side §3.5 ack at `.local-replies-to/verus-fork/2026-06-04-3.5-jit-on-aot-prelude-ack.md` (commit `b484369`) mirrored same day. Pending — both sides: T0' finer-grained deadline cascade inside CDCL inner work between `propagate_two_watched` calls (adsmt side will land in parallel with §3.5.A). Workspace now has 31 crates (adsmt-jit + adsmt-stalmarck added) and 901 lib/integration tests. |
| v1.0.0-rc.16 | 1.0.0-rc.16 (closed 2026-06-05) | **rc.16 cycle = T0' deadline cascade refinement + §3.5 JIT-on-AOT-prelude A→G end-to-end.** Driven by verus-fork §3.5 counter-ack (2026-06-05) — all six adsmt-side recommendations accepted; T0' parallel sequencing confirmed; new §3.5.J.pre row added between §3.5.I and §3.5.J on the verus-fork ledger.  **T0'**: T0'.1 (`627aded`) deadline check inside new `analyze_conflict_1uip_deadline` variant + new `AnalyzeOutcome { Done, Expired }` enum; T0'.2 + T0'.3 (`03649f3`) deadline checks inside the learnt-clause reduction loop + unconditional check before the next outer `propagate_two_watched`.  `DEADLINE_CHECK_INTERVAL = 256` + `expired(deadline)` helper promoted to module-level in `adsmt-engine/src/cdcl.rs` so every `*_deadline` function shares the cadence.  **§3.5.A** (`df18edd`): new `adsmt_aot::cdcl` module with `CdclSection { binary_sha256: [u8; 32], flatten_version: u32, clauses, trail, watches, vsids, saved_phase }` + sub-record types; `write_cdcl_section` + `read_luart_with_cdcl(buf) -> (LuartFile, Option<CdclSection>)`.  v0 readers ignore trailing v1 bytes; v1 `watch_count: u64` + inner `Vec<u32>` fixed-width per counter-ack §(b).  **§3.5.B** (`00ce626`): `lu-smt --aot-bake --aot-include-cdcl` composable flag + mutex rules (exit 12 on misuse) + `current_binary_sha256()` SHA-256-of-`current_exe()` helper + `FLATTEN_VERSION: u32 = 0` constant.  v0 emits `CdclSection::empty` placeholder (real capture is the §3.5.F follow-up).  **§3.5.C** (`f91bea5`): `Solver::with_aot_cdcl(prelude: ReconstructedCdclPrelude)` builder + new `ReconstructedCdclPrelude { prelude, cdcl_section }` + `reconstruct_with_cdcl(&[u8])` helper.  v0: assertions thread through `with_aot_prelude`; `cdcl_section` stashed for the §3.5.F replay-or-fallback dispatcher.  **§3.5.D** (`95efa45`): new `adsmt_jit::cdcl` submodule with `CdclTraceEvent` 5-event vocabulary (`Propagate { atom, polarity, antecedent: i64 (-1 sentinel) }` / `Conflict { learnt, lbd }` / `Backjump { to_scope }` / `Decide { atom, polarity }` / `Restart`); `GF2Snapshot { basis, classes }` + `CdclCheckpoint { at_event, signature }` + `CdclTrace { events, signature, checkpoints, guards: Vec<JitGuard>, kernel_id }` — shares guard surface with §3.2's bytecode `Trace` per counter-ack §5.5; `CdclTracer { events }` append-only recorder.  **§3.5.E** (`5fac19d`): `FiniteFieldTheory::current_generators() -> Vec<Polynomial>` + `GF2Snapshot::capture(theory, classes)` helpers.  Capture is one cheap CNF-to-polynomial pass, not a Gröbner computation.  **§3.5.F** (`77ea879`): `Solver::replay_aot_cdcl_trace(trace, classes) -> ReplayOutcome` + new `ReplayOutcome { GuardMiss, GuardsPassed }` enum.  v0 skeleton (guard-evaluation gate only; actual event replay is follow-up that wires `restore_cdcl_state`).  adsmt-engine grows an `adsmt-jit` dep.  **§3.5.G** (`7706327`): new `adsmt_jit::cdcl_io` module + `LUTRACE_MAGIC = "lutrace\0"` + `LUTRACE_VERSION = 0` + `write_trace` / `read_trace` byte-level codec; `lu-smt --jit-trace-emit / --jit-trace-load` CLI flags + mutex rule (exit 12).  v0 wire shape covers events + `kernel_id` only.  **rc.16 bump** (`ae12a9f`) + **books cheatsheet** §3.5 section (4 langs, `4de2727`) + **docs** README + PORTFOLIO + submodule pointer (`44ef399`).  Workspace test count: 901 → 923; 0 cargo doc / cargo build warnings.  Pending — verus-fork side: §3.5.H vargo hook + §3.5.I SmtProcess argv + §3.5.J.pre / §3.5.J smoke matrix retries. |
| v1.0.0-rc.17 | 1.0.0-rc.17 (closed 2026-06-05) | **rc.17 cycle = §3.5 v0 → v0.x promotion + §3.2 dynasm-rs + §3.3 phase 2.**  Three sub-cycle groups:  **§1.1/§1.2/§1.3/§1.5 in one commit** (`f91ed5f`): `cdcl::initial_bcp(&[Clause]) -> CdclState` helper; `Solver::dump_cdcl_state(&self) -> (Vec<Clause>, CdclState)` (real BCP-fixpoint bake); `Solver::aot_cdcl_state: Option<CdclSection>` field + `with_aot_cdcl` no longer drops `cdcl_section`; `Solver::jit_tracer: Option<CdclTracer>` field + `start_jit_recording()` / `take_jit_recording()` lifecycle; `Solver::check_sat_with_deadline` posts macro-shape events (Restart session boundary + per-Sat-model Decide events + Conflict-shaped event on Unsat) when tracer active; `atom_key_hash_u32` helper; `ReplayOutcome::Replayed { verdict: SatResult }` variant + `compute_live_skeleton` actual hash + event-replay scan (empty → Sat, Conflict-without-Restart → Unsat, else GuardsPassed); CLI `build_cdcl_section` + `collect_atom_mapping` glue thread Term-to-pool-index mapping into the v1 bake.  **§1.4 + §1.6 in one commit** (`8f8fbb1`): `CdclTracer::record_checkpoint(signature)` + `CdclTracer::checkpoint_count()` + `finalize` folds checkpoints into `CdclTrace`; `.lutrace` `LUTRACE_VERSION` bumped 0 → 1; new `write_polynomial`/`read_polynomial`/`write_signature`/`read_signature`/`write_guard`/`read_guard` helpers; v1 wire shape covers events + GF(2) signature (basis + classes) + guard list + checkpoint table end-to-end; empty-trace size grows 24 → 56 bytes.  **§3.2 dynasm-rs (3 commits)**: `3ed23b6` new `adsmt-jit::kernel` with `KernelStore` + `CompiledKernel` (RAII `ExecutableBuffer`) + `emit_noop_kernel` (x86_64 `xor rax, rax; ret`; non-x86_64 `UnsupportedHostTriple`); `07bcacb` new `adsmt-jit::JitRegistry` joint cache + store with `register_trace` (emit + cache + assign id) + `lookup_kernel` (guard gate + store lookup); `51835a2` `Solver::jit_registry: Option<JitRegistry>` field + `start_jit_caching()` / `register_jit_trace()` / `jit_registry()` lifecycle + replay-time kernel invocation hook inside `replay_aot_cdcl_trace`.  `dynasm` + `dynasmrt` pinned to v5.0.0+ per the `feedback_dynasm_min_version.md` rule.  **§3.3 phase 2 + §3.5.A v1.1 in one commit** (`09b33b2`): `ImplicationGraph::intersect_with` + `into_edges`; `Saturator::dilemma_step(&mut graph, pivot)` (case-splits, runs simple rule on both branches, folds common consequences back); `Saturator::n_saturate(&mut graph, depth)` (outer loop over every atom for `depth` rounds, short-circuits on zero-progress); new `adsmt_aot::StalmarckEdge { from_atom_pool_idx, from_polarity, to_atom_pool_idx, to_polarity }`; `CdclSection::stalmarck_edges: Vec<StalmarckEdge>` trailing field; v1.0 readers ignore the trailing bytes (`Cursor::at_end()`-gated v1.1 decode); CLI `stalmarck_edges_for` glue runs `from_binary_clauses` + `saturate_simple` + `n_saturate(depth=1)` on the prelude's binary-clause subset, projects edges into the v1.1 wire shape.  rc.17 bump (`ae12a9f → workspace.package.version = "1.0.0-rc.17"`) + docs (README + PORTFOLIO).  Workspace test count: 923 → 945; 0 cargo doc / cargo build warnings.  Pending — verus-fork side: §3.5.H vargo hook + §3.5.I SmtProcess argv + §3.5.J.pre / §3.5.J smoke matrix retries. |
| v1.0.0-rc.18 | 1.0.0-rc.18 (closed 2026-06-05) | **rc.18 cycle = verus-fork rc.17 retry follow-ups in priority order.**  Triggered by `.local-replies-from/verus-fork/2026-06-05-rc17-smoke-retry-and-section-bake-regression.md` (3 prioritised asks).  **(1) `.luart-cdcl` v1.1 bake `u32::MAX` forward-ref leak fix** (`f859ffa`): root cause was `build_cdcl_section`'s phase-1 `collect_atom_mapping` walking only the assertion sub-terms; CNF flatten introduces `Lit::atom` terms (negation push-downs, Tseitin aux atoms from `bv_blast`, synthesised `:pattern` triggers) that the assertion list never carries, leaving the `unwrap_or(u32::MAX)` sentinel to leak to disk where the reader's topo-sort guard caught it at entry 6542.  Fix is 3-phase registration: (a) assertion Term-DAG walk, (b) every CNF-flattened `Lit::atom` walk, (c) defence-in-depth — residual `CdclState` bookkeeping keys get a synthesised `Term::var(key, Bool)` interned.  `lookup` signature switched from `unwrap_or(u32::MAX)` to `Option<u32>`; unmapped entries drop silently.  **(2) `cdcl::*_recording` per-Propagate / per-Backjump hooks** (`78284bc`): new `pub trait CdclEventSink` with `on_propagate / on_conflict / on_backjump / on_decide / on_restart`; new `initial_bcp_recording` / `cdcl_solve_with_model_deadline_recording` / `cdcl_with_restarts_with_model_deadline_recording` variants that emit per-event records along every observable state transition.  `Solver` gains a private `CdclTracerSink<'a>` adapter that funnels every sink hook into the active `adsmt_jit::CdclTracer`; `check_sat_inner`'s model-carrying CDCL call picks the recording variant when `self.jit_tracer.is_some()`.  Replaces the v0.x post-hoc macro-event shape in `check_sat_with_deadline` (which left `.lutrace` artefacts empty on `unknown`-shaped warmups — the verus-fork rc.17 retry §4's gating piece for §3.5.J).  **(3) `reconstruct` parse-type cache** (`b6d1da9`): `HashMap<String, Type>` collapses per-pool-entry tokeniser cost to one parse per *distinct* ty-string, addressing the +700 ms v0 `--aot-load` regression rc.15 → rc.17 (verus-fork rc.17 retry §2 hotspot).  rc.18 bump + docs / memory.  Workspace test count: 945 → 946; 0 cargo doc / cargo build warnings.  Pending — verus-fork side: §3.5.J full retry against §3.5-baked artefact + per-Propagate `.lutrace` (now unblocked on both prerequisites). |
| v1.0.0-rc.19 | 1.0.0-rc.19 (closed 2026-06-05) | **rc.19 cycle = verus-fork rc.18 retry follow-ups in priority order.**  Triggered by `.local-replies-from/verus-fork/2026-06-05-rc18-three-asks-status-followup.md` (3 prioritised asks).  **(a') v1.1 bake topo-order fix** (`aa079d9`): root cause was the rc.18 retry's `f859ffa` running its 3-phase atom-key registration on a *separate* `PoolBuilder` from the one `write_luart` used for the v0 sections.  When Phase-2 / Phase-3 installed atoms (CNF-flatten `Lit::atom` walk, residual `CdclState` bookkeeping), they landed at pool indices > the v0 sections' `pool_len`, surfacing as the reader's `pool entry 6542 references out-of-range / forward index 6550` rejection.  Fix routes both halves through a single shared builder — `bake_to_path` inlines `write_luart`'s former body, calls `build_cdcl_section` with `&mut builder` + `&mut HashMap<String, u32>`, then drains via `builder.into_entries()` so every reference (Phase-2 / Phase-3 included) points into the same emitted pool.  **(b') CLI start/take recording wiring** (`d9b9fb2`): the rc.18 retry §2 finding was that `78284bc`'s engine hooks landed correctly but `lu-smt --jit-trace-emit` still wrote 56 B header-only artefacts because the CLI never installed a tracer.  Fix: `main()` calls `driver.solver.start_jit_recording()` before the dispatch loop when `--jit-trace-emit` is set, then drains via `take_jit_recording()` + `finalize(GF2Snapshot::empty(), vec![])` after.  `emit_jit_trace` renamed to `emit_jit_trace_with(path, &trace)` taking the populated `CdclTrace` instead of constructing an empty one.  Smoke: tiny SAT fixture trace size 56 B → 84 B (+28 B for recorded events), medium fixture 104 B, reload-via-`--jit-trace-load` verdict matches.  **(c') v0 load redundant `intern_external` walk drop** (`c554be8`): both `Solver::with_aot_prelude` and `Driver::new` were running a post-order walk over the prelude assertions via `adsmt_aot::intern_external` — even though `reader::reconstruct` had *already* interned every pool entry through the canonical `Term::var / Term::const_ / Term::app / Term::lam` chain.  On a verus_smoke-sized prelude (~10⁵ pool entries) the per-node cache-hit loop accumulates measurable wall-clock; dropping both call sites addresses the +700 ms rc.15 → rc.18 regression the rc.17/18 retries §2/§3 flagged.  `adsmt_aot::intern_external` itself stays exported for FFI consumers / deserialised cert payloads / language bindings that may have externally-built Terms.  rc.19 bump + docs / memory.  No new test cases (all three fixes were behavioural — same outputs on same inputs, modulo correctness on the previously-failing rc.18 fixtures).  Workspace test count: 946; 0 cargo doc / cargo build warnings.  Pending — verus-fork side: §3.5.J full retry against §3.5-baked artefact + per-Propagate `.lutrace` (now unblocked on both CLI prerequisites). |
| v1.0.0-rc.20 | 1.0.0-rc.20 (closed 2026-06-05) | **rc.20 cycle = §3.5.J gate + verus-fork rc.19 retry follow-ups.**  Triggered by `.local-replies-from/verus-fork/2026-06-05-rc19-three-asks-second-round.md`.  **(NEW) `Solver::restore_cdcl_state_into(section, pool_terms)`** (`371e5aa`): the §1.2 commit message's queued v1 follow-up.  `ReconstructedPrelude` grows a `pool_terms: Vec<Term>` field (the reader's canonical interned pool, exposed via direct move from `interned: Vec<Term>` — no extra walk); reader populates it during `reconstruct`.  `Solver` grows two fields: `aot_prelude_clauses: Vec<Clause>` (post-flatten CNF cache) + `aot_prelude_term_set: HashSet<Term>` (Arc::ptr_eq-keyed dedup set for the per-query flatten skip).  `with_aot_cdcl` calls `restore_cdcl_state_into` *before* stashing the section, so the cache is live on the very next `(check-sat)`.  `check_sat_inner` prepends `aot_prelude_clauses` to the freshly-flattened CNF + filters out any literal whose Term lives in `aot_prelude_term_set`.  v0.x scope ships the clause vec only — trail / watches / VSIDS / saved-phase restoration queued for the rc.21 `cdcl_solve_with_model_deadline_with_seed` variant.  Largest single per-`(check-sat)` shortcut the v1 artefact unlocks lands here.  **(b'') Satisfiability-only CDCL recorder routing** (`104106b`): pre-rc.20 only the model-carrying re-run on the Sat branch routed through the recording variant; Unsat / Unknown exit paths bypassed it.  New `cdcl::cdcl_with_restarts_deadline_recording` pub-fn wraps `cdcl_with_restarts_with_model_deadline_recording` + discards the model via the existing `CdclOutcome → BoolResult` impl.  `check_sat_inner`'s satisfiability-only first stage picks it when `self.jit_tracer.is_some()`.  Smoke: `(p) ∧ (¬p)` tiny-unsat fixture trace size 56 B → 70 B (+14 B of Conflict + Backjump events).  **(c'') v0 load +662 ms hotspot — static audit + Term-set micro-fix** (`66d2a13`): verus-fork rc.19 retry §3 ruled out `intern_external` as the hotspot (the rc.18 c' drop was correct in principle but didn't move the wall).  Static audit at rc.20: `intern_external` grep — no other call sites; `compute_live_skeleton` — only invoked from `replay_aot_cdcl_trace`, not on the v0 load path; `aot_cdcl_state` — `None` for v0 artefacts, zero-cost.  None of the rc.19 retry's three candidates account for the +662 ms.  Flamegraph request flagged to verus-fork.  Forward-looking micro-fix: `aot_prelude_term_set` switched from `HashSet<String>` (with `to_string()` per insert / check) to `HashSet<Term>` (Arc::ptr_eq O(1)) for the rc.20 v1 path.  rc.20 bump + docs / memory.  Workspace test count stays at 946 (behavioural fixes + new method); 0 cargo doc / cargo build warnings.  Pending — verus-fork side: §3.5.J full retry against §3.5-baked artefact + the rc.20 `restore_cdcl_state_into` shortcut. |
| v1.0.0-rc.21 | 1.0.0-rc.21 (closed 2026-06-05) | **rc.21 cycle = verus-fork rc.20 retry's three priorities in order, all landed.**  Triggered by `.local-replies-from/verus-fork/2026-06-05-rc20-restore-cdcl-state-clause-only-payoff.md`.  **(1) §3.5.J runtime gate** (`706b7bf`): new `cdcl::cdcl_solve_with_model_deadline_with_seed(clauses, max_conflicts, deadline, seed: Option<CdclState>) -> CdclOutcome` inner-loop variant + matching Luby wrapper + sat-only wrapper.  `Solver::aot_pool_terms: Vec<Term>` field for atom_pool_idx → Term resolution; `with_aot_cdcl` populates it from `prelude.prelude.pool_terms`.  New private `Solver::prepare_cdcl_seed(&self) -> Option<CdclState>` projects the v1 CdclSection's `trail` / `vsids` / `saved_phase` records onto engine-side atom shapes (trail entries tagged `decision_level = 0, reason = Decision` since they're all root-level BCP-fixpoint propagations).  `check_sat_inner`'s non-tracer satisfiability-only stage + model-carrying re-run both pick `_with_seed` variants when `aot_cdcl_state` is present.  This is the missing half of the §3.5.J payoff the rc.20 `restore_cdcl_state_into` clause-cache predicate said was queued.  **(b''') Tracer Unknown / deadline-cancel coverage** (`78eff65`): session-boundary fallback in `Solver::check_sat_with_deadline` — after `check_sat_inner` returns, if the active `jit_tracer.is_empty()`, force-record a Restart event + verdict-shaped event (Unsat → empty Conflict, Sat → one Decide per model atom).  Covers every CDCL path the inline recorder can't reach (flatten_to_clauses deadline-cancel, theory-check timeout, Unknown verdicts from outer-loop budget exhaustion).  Smoke: deadline-cancel fixture under `--rlimit 1` ms → trace size 56 → 57 bytes (header + Restart tag), satisfying the verus-fork rc.20 retry §(b'') ask.  **(c''') v0 `--aot-load` regression — diagnosed AND eliminated**.  Profile phase (`e2eaec8`): cargo-flamegraph on a 5 000-Bool / 5 000-ternary-clause verus_smoke-shaped fixture identified ~12.6 % of cycles in the allocator chain (`__libc_malloc` 7.3 % + tcache_get 2.3 % + checked_request2size 1.6 % + `__libc_free` 1.4 % + Rust alloc + tcache_put), every hit tracing to `cdcl::atom_key(lit) -> lit.atom.to_string()` (cdcl.rs:1171) allocating fresh owned Strings per propagation step on String-keyed CdclState maps.  Elimination phase (`de0aedb`): full migration `CdclState`'s atom-key surface from `String` → hash-consed `Term` — `TrailEntry::atom_key` → `atom`, `CdclState::{assign, activity, saved_phase, watches}`, `HashSet seen` in both `analyze_conflict_1uip` variants, `pick_vsids_atom` return, `evaluate_clause` assign arg.  Post-rc.10 `Term::Hash`/`Eq` are `Arc::ptr_eq` O(1) so probe cost is identical but the per-step `to_string()` malloc disappears.  `atom_key(lit)` body is now `lit.atom.clone()` (Arc refcount bump).  `CdclOutcome::Sat`'s external `model: HashMap<String, bool>` API + `CdclEventSink` trait's `&str` shape both preserved — boundary conversion fires once per Sat verdict / once per recorded event, not once per propagation step.  Dead code removed: `term_for_atom_key`, `entry_to_atom_term`, `any_atom_of_clause`.  Solver-side `prepare_cdcl_seed` + CLI-side `build_cdcl_section` updated to write Term-keyed CdclState directly / convert at the writer boundary so `.luart-cdcl` v1 wire format stays bit-identical.  **Measurement** (verus_smoke-shaped fixture, `--rlimit 5 s`, median of 3): rc.20 baseline 5 955 ms → rc.21 1 923 ms (≈ 67 % wall-clock reduction; drops the v0 load path below rc.15's 5 208 ms baseline).  Post-migration flamegraph: allocator chain absent from top-40 frames; remaining cycles are CDCL algorithm itself (9.2 % `clone<TermInner>` Arc refcount, 5.85 % `pick_vsids_atom`/`evaluate_clause`, 4.30 % HashMap probe, 2.80 % hash machinery).  §3.5.J implication: the verus-fork-measured ~5.3 s BCP-fixpoint "floor" was a downstream symptom of this allocator hotspot, not a fundamental algorithmic limit — with the wall-clock at ~1.9 s the §3.5.J replay path should now fire cleanly under verus_smoke's 5 s budget.  rc.21 bump (`dd9f720`) + profile assets at `.claude-notes/profiling/{README.md, 2026-06-05-rc20-v0-load-flamegraph.txt, 2026-06-05-post-migration-flamegraph.txt}`.  Workspace test count: 946 (no test count change vs pre-migration); 0 cargo doc / cargo build warnings. |
| v1.0.0-rc.22 | 1.0.0-rc.22 (closed 2026-06-06) | **rc.22 cycle = verus-fork rc.21 retry §(d) verus_smoke flamegraph follow-ups.**  Triggered by `.local-replies-from/verus-fork/2026-06-06-rc21-verus-smoke-flamegraph-alpha-eq-hotspot.md`.  rc.21's `String→Term` migration removed the CDCL allocator hotspot but verus_smoke's fixture-shape (85 forall quantifiers + 26 ground + theories) routes the dominant cost upstream — the rc.21 retry Mode C' 23 ms variance signature (matching the adsmt-side 13 ms post-migration shape) confirmed the saved cycles got reabsorbed in a different, fully-deterministic hot path.  The verus-fork-side flamegraph (staged at `.claude-notes/profiling/2026-06-06-verus_smoke-flamegraph-rc21.{svg,txt}`) attributed **62.16 % of cycles to `adsmt_core::term::alpha_eq_rec`** and **17.20 % to `<adsmt_core::ty::Type as PartialEq>::eq`** = ~79 % combined, neither using the rc.10 hash-cons handle.  **(e.1) `alpha_eq_rec` Arc::ptr_eq fast path** (`c54e71c`): 5-line guard at the top of `adsmt-core/src/term.rs::alpha_eq_rec` gated by `a_bound.is_empty() && b_bound.is_empty() && Arc::ptr_eq(&a.0, &b.0)`; the empty-stack guard restricts the fast path to closed sub-terms in identical bound-variable contexts (soundness preserved — two open terms can share an Arc yet be α-distinct under different binders).  Every top-level entry (mk_forall parse, nnf_pos assert, UF `set.iter().any(alpha_eq)`, SLD `existing.alpha_eq(h)`, proof-rule preconditions) hits this short-circuit.  **(e.2) `Type::eq` hand-rolled Arc::ptr_eq-first PartialEq** (`d01d78a`): drop `PartialEq` from `Type`'s derive list, hand-roll with `Arc::ptr_eq(a, b) || **a == **b` on each `Var` / `Const` / `App` arm; soundness-equivalent to the derive (the `||` fallback restores structural comparison on a ptr-eq miss).  `Hash` stays derived (structural); the hand-rolled eq returns identical results to the derived shape.  **(e.3) `feedback_hashcons_hot_paths.md` memory generalisation** (`d703956`): rule renamed from "HashMap key" to "Take the Arc::ptr_eq short-circuit on hash-consed types in hot paths" + restructured into three numbered sections — §1 HashMap / HashSet keys (rc.21 surface), §2 Structural equality fast paths (rc.22 surfaces), §3 Outer linear-scan callers (uf.rs / sld.rs / rule.rs).  Diagnostic anchor recorded: rc.21 Mode C' 23 ms variance signature (preserve → algorithmic fix; grow → new allocator churn).  **Verus-fork-predicted wall recovery** on verus_smoke Mode C' (per `.local-replies-from/verus-fork/2026-06-06-rc21-verus-smoke-flamegraph-alpha-eq-hotspot.md` §7): alpha_eq_rec ~3 670 → ~50 ms + Type::eq ~1 010 → ~30 ms ⇒ total ~5 898 → ~1 300 ms (inside §3.5.J's `≤ 1 500 ms` expected window).  **Adsmt-side direct wall measurement is host-environment-limited** — lu-smt direct invocation does not catch the in-flight `:rlimit 5 s` deadline inside the assert-stage hot path (the flamegraph caller chain `dispatch → assert_with_polarity_at → nnf_pos → mk_forall → alpha_eq_rec` runs *before* the per-`(check-sat)` deadline path).  Verus-fork wall numbers in the rc.21 retry were external-SIGTERM-driven (verus's own timeout wrapper around lu-smt at 5 s + cleanup overhead), which the adsmt host cannot reproduce.  rc.22 retry against the verus-fork host is the only path to direct wall confirmation.  rc.22 bump (`bf4b52f`).  Workspace test count stays at 946 (additive guards + hand-rolled eq returning identical results to derive); 0 cargo doc / cargo build warnings.  Pending — verus-fork side: rc.22 retry to confirm predicted wall + decide whether (3) outer linear-scan replacement (`iter().any(alpha_eq)` → `HashSet<Term>` lookup at uf.rs:66/77/88, sld.rs:66) is still needed if the wall doesn't drop the predicted ~4.6 s. |
| v1.0.0-rc.23 | 1.0.0-rc.23 (closed 2026-06-07) | **rc.23 cycle = verus-fork rc.22 retry §4 UF iter().any(alpha_eq) hot path follow-ups.**  Triggered by `.local-replies-from/verus-fork/2026-06-06-rc22-e1e2-landed-uf-iter-any-next-priority.md`.  rc.22 (e.1)+(e.2) recovered ~1 100 ms at rlimit ≤ 4 s and pushed `unknown` exit threshold 5-6 s → 4-5 s; rlimit ≥ 5 s now hits a deadline-uncatchable loop (the engine reaches phase 2 UF/SLD/quant work the T0' commits never extended deadline cascade into).  rc.22 flamegraph at rlimit 3 s showed `alpha_eq_rec` at 97.98 % of cycles (rc.21: 62.16 %; rc.22 (e.2) cleared Type::eq's 17.20 % but the proportional shift surfaced that recursive `App`-arm descent never hits the `is_empty()` fast path since sub-terms differ at the leaves).  Cost model: ~10⁴ `add_known` per `(check-sat)` × ~10³ `known` size = ~10⁷ alpha_eq invocations × avg depth 20 ≈ 2 × 10⁸ `alpha_eq_rec` body executions per query.  **(e''.1) UF Vec<Term> → IndexSet<Term>** (`5d347c2`): `adsmt-theory/src/uf.rs` `known` / `pos_atoms` / `neg_atoms` migrated.  `indexmap::IndexSet` chosen over `std::collections::HashSet` because: (a) `IndexSet::truncate(n)` is a 1:1 drop-in for `Vec::truncate(n)` in `UfSnapshot.{pos,neg}_len` rollback; (b) `IndexSet::get_index(i)` keeps `close()`'s `for i in 0..n; for j in (i+1)..n` indexed pair scan readable; (c) insertion-deterministic iteration order preserves reproducibility of certificate emit + union sequence.  `indexmap` is already a workspace dep (`adsmt-core/src/term.rs` etc.) so zero new-dependency cost.  `register()` drops the redundant pre-`iter().any()` check since `IndexSet::insert` dedups itself in O(1).  Bonus reproducibility side-fix: `derive_equalities`'s scratch `HashMap<Term, Vec<Term>> classes` swapped to `IndexMap` so emitted Nelson-Oppen equalities surface in `self.known`'s insertion order (the `HashMap::values()` non-determinism was a pre-existing reproducibility break).  **(e''.2) abductive Candidate::merge HashSet<Term> dedup** (`e2c1761`): `adsmt-abduce/src/sld.rs::Candidate::merge` pre-stages a `HashSet<Term>` populated from `self.hypotheses`, keys the dedup off `HashSet::insert`'s `bool` return.  `HashSet` (not `IndexSet`) here because the scratch set is never iterated / index-accessed / serialised — smaller per-entry overhead wins for the one-shot allocation.  Parallel `hypotheses` / `explanations` / `sources` `Vec` layout preserved.  **(e''.3) feedback memory rule generalisation** (`c97a3ba`): `feedback_hashcons_hot_paths.md` §3 retitled from "Outer linear-scan callers" to "Container-shape: `Vec<T>` + `iter().any(custom_eq)` → `(Index)Set<T>::contains`" with picking-the-container matrix (HashSet for dedup-only scratch / IndexSet for rollback / indexed-loop / reproducibility), soundness checks (hash-cons coverage on closed Skolemized terms, reproducibility, rollback shape), and rc.23 row added to the four-incident measured-recoveries table.  **Verus-fork-predicted Mode C' wall** on verus_smoke (per `.local-replies-from/verus-fork/2026-06-06-rc22-e1e2-landed-uf-iter-any-next-priority.md` §7): `alpha_eq_rec` driven by UF iter().any ~3 600 → ~50 ms + residual single-comparison sites unchanged ~50 ms + residual CDCL/theory/parser unchanged ~900 ms ⇒ total **4 600 ms → ~1 100 ms** (inside §3.5.J's `≤ 1 500 ms` window).  Predicted variance signature: Mode C' spread 235 → ≤ 50 ms (rc.21 23 ms gold-standard with tolerance for whatever phase the engine reaches next).  **Adsmt-side direct wall measurement is host-environment-limited** — lu-smt direct invocation does not catch the in-flight `:rlimit 5 s` deadline inside the assert-stage hot path; verus-fork wall numbers were external-SIGTERM-driven through verus's own timeout wrapper.  rc.23 retry against the verus-fork host is the path to direct wall confirmation.  rc.23 bump (`7addc5e`).  Workspace test count: 946/946 (per-crate: adsmt-theory 80/80, adsmt-abduce 26/26); 0 cargo doc / cargo build warnings.  Pending — verus-fork side: rc.23 retry to confirm predicted wall + variance signature collapse.  If the wall doesn't drop the predicted ~3.5 s, the deadline-cascade extension into UF/SLD/quant phase-2 loops (currently uncovered by T0' commits) becomes the next priority. |
| v1.0.0-rc.24 | 1.0.0-rc.24 (closed 2026-06-07) | **rc.24 cycle = verus-fork rc.23 retry — the recurring-pattern / narrow-grep cautionary tale.**  Triggered by `.local-replies-from/verus-fork/2026-06-07-rc23-ematch-termuniverse-next-priority.md`.  rc.23's (e''.1) UF + (e''.2) abductive landed verbatim but the verus_smoke wall held flat (Mode C' 4 635 → 4 581 ms, noise band) and `alpha_eq_rec` stayed at 97.50 % of cycles — because the *actual* dominant caller was the bit-for-bit identical `Vec<Term>+iter().any(|x| x.alpha_eq(&t))` pattern in `adsmt-quant/src/ematch.rs::TermUniverse::insert`, missed by both the rc.22 grep (scoped to `adsmt-theory`) and the rc.23 fix scope.  When the user asked to sweep `clone`-heavy spots + audit `extend_with_equalities` O(N²), a workspace-wide grep (`iter\(\)\.any\([^)]*\.alpha_eq`, excluding tests) found **eight more** production sites the per-reply greps never covered.  **(e'''.1) ematch `TermUniverse` Vec→IndexSet** (`27df7d2`): `terms: Vec<Term>` → `IndexSet<Term>`; `insert` drops the pre-scan (O(N²·depth) build → O(N)); new O(1) `contains`; `extend_with_equalities` snapshots into an explicit `Vec` (cheap Arc-handle copy, not an IndexSet clone) so its insert loop is O(M·N) not O(M·N²).  THE 97.5 %-of-cycles hot site.  **(e'''.2) engine quant hot path** (`f155c24`): `quant.rs` Tier-classification `universe.iter().any(body.alpha_eq)` → `universe.contains(body)`; `instantiate_one` seen-set `HashSet<String>`+`to_string()` → `HashSet<Term>` (the rc.21 CdclState String-key incident recurring on the quantifier hot path, `seen.contains+insert` → `if !seen.insert(...)`); `solver.rs` quantifier-loop `instantiations` `Vec<Term>` → `IndexSet<Term>` across the three Tier-1/2/3 `iter().any` dedup sites (IndexSet not HashSet because `instantiations` is iterated to rebuild `combined` + `.len()` drives the fixpoint check — insertion order must stay deterministic).  **(e'''.3) cold-path sweep** (`4e5b971`): same pattern via order-preserving parallel-`HashSet<Term>`-scratch in `adsmt-core/src/theorem.rs::union_hyps`, `adsmt-engine/src/quant_conflict.rs::conflict_instantiate`, `adsmt-theory/src/polite.rs::max_disequality_clique` (Vec accumulator kept for positional clique walk, only dedup probe → O(1)); subset-test `adsmt-abduce/src/minimize.rs::subsumes` builds a `HashSet<Term>` from `b.hypotheses` once (O(|a|·|b|) → O(|a|+|b|)).  Two abduction membership sites in `workflow.rs` (`is_accepted` over a `Vec<AcceptedHypothesis>` struct field; `is_rejected` over a `Vec<Term>` exposed via the public `rejected() -> &[Term]` accessor) deliberately left as `Vec` — cold (abduction off the SMT path) + public-API constraint, documented in code.  **(e'''.4) memory rule** (`e124fe3`): new "ALWAYS grep workspace-wide, every cycle" subsection recording the rc.23 narrow-grep-held-the-wall-flat tale + the canonical grep commands + the bar (clean workspace-wide run = "eliminated", not single-file); audit-locations rewritten to rc.24 workspace-wide state; fifth incident row.  **Verus-fork-predicted** Mode C' wall (rc.23 retry §8): ~4 580 → ~830 ms (inside §3.5.J's `≤ 1 500 ms` window); variance 305 → ≤ 50 ms; rlimit ≥ 5 s timeout should resolve.  **Adsmt-side direct wall measurement host-environment-limited** — lu-smt direct invocation does not catch the in-flight `:rlimit` deadline inside the assert-stage hot path; verus-fork wall numbers external-SIGTERM-driven.  rc.24 retry against the verus-fork host is the confirmation path.  rc.24 bump (`b712e68`, includes a `.gitignore` fix for `*.data` perf captures + a malformed `docs/upstream/*.md` line carried since rc.21).  Workspace test count: 946/946 (adsmt-quant 43/43, adsmt-core 44/44, adsmt-theory 80/80, adsmt-abduce 26/26, adsmt-engine 148/148); 0 cargo doc / cargo build warnings.  Pending — verus-fork side: rc.24 retry to confirm wall + variance + whether rlimit ≥ 5 s timeout resolves (if not, the deadline-cascade extension into UF/SLD/quant phase-2 loops — T0''' — is the next priority). |
| v1.0.0-rc.25 | 1.0.0-rc.25 (closed 2026-06-07) | **rc.25 cycle = verus-fork rc.24 retry — the throttle-unmask cautionary tale + the first *algorithmic* (not container/key) fix in the hash-cons-hot-path series.**  Triggered by `.local-replies-from/verus-fork/2026-06-07-rc24-uf-congruence-closure-on2-exposed.md`.  All four rc.24 (e'''.*) commits were correct + the workspace grep is clean, but the verus_smoke wall went **UP 7×** (Mode A 3 971 → 26 832 ms, Mode C' 4 581 → 10 564 ms, rlimit-*independent*).  verus-fork bisected the jump to `27df7d2` (e'''.1 ematch) and ruled out a dedup regression by instrumentation (`ptr_eq_dedup_size == alpha_eq_dedup_size == 5665`, bloat 1.00× — the universe is all-ground + hash-cons canonical, so IndexSet's Arc::ptr_eq dedup == the old alpha_eq dedup).  **Mechanism**: rc.23's O(N²) `TermUniverse` build was an *accidental throttle* — the engine deadline-fired *inside* `collect_universe` at ~4 s and never reached the next phase.  (e'''.1) correctly made the build O(N), so the engine now reaches `UF::close()`'s congruence closure over the full ~5 665-term `known` universe — a pre-existing **naive O(N²·rounds·alpha_eq)** loop the throttle had been masking.  rc.24 flamegraph: `alpha_eq_rec` 81.35 %, `Uf::check` 9.86 %, UF the sole visible caller (matcher/quant absent).  **(e⁗.1) signature-hashed congruence closure** (`uf.rs::close()`): replace the O(N²) pairwise App-congruence scan with the standard Downey–Sethi–Tarjan / Nelson–Oppen signature pass — index each `App(f, x)` by `(find(f), find(x))` in a `HashMap<(Term, Term), Term>`; two App-terms congruent iff signatures collide.  `find` returns a canonical hash-consed root `Term` (Arc-unique per class) so the signature key has O(1) `Hash`/`Eq` via `Arc::ptr_eq` — no integer class-id indirection.  Each round is one O(N) pass over the known App-terms (snapshotted to a `Vec` since the pass needs `&mut self` for `find`'s path compression); loop runs to union-find fixpoint.  O(N²·rounds) → O(N·rounds·α(N)).  **(e⁗.2) Arc::ptr_eq union-find roots**: `find` / `union` / `same_class` / the `derive_equalities` root-chain walk compare roots with `==` (= `Arc::ptr_eq` post-rc.10), not recursive `alpha_eq`.  The `parent` map is keyed on hash-consed `Term`s so every root is a canonical Arc; the prior `alpha_eq` re-walked the full structure on every find-step + class compare — the same hash-cons-hot-path violation as rc.21/rc.22, one layer into the congruence machinery.  **(T0''') theory-phase deadline cascade**: new `Theory::set_deadline(&mut self, Option<Instant>)` default-no-op trait method (the other 7 impls inherit no-op) + `Combination::set_deadline` fan-out + `dpllt::run_once_with_deadline` (calls `combo.set_deadline` before the check round; `run_once` stays a `None` wrapper) + `Solver::check_via_theories_with_model` gains a `deadline` param threaded from the quantifier-loop Sat path.  `Uf` stores the deadline + a `timed_out` flag; `close()` checks `expired` per signature-pass round and bails; `check()` returns `CheckResult::Unknown` on a half-built closure (sound — never reports `Sat` off a partial congruence relation).  Extends the rc.16 T0' CDCL-phase deadline cascade into the theory phase.  **(e⁗.3) feedback memory** (`feedback_hashcons_hot_paths.md`): new "A throttle removal can EXPOSE a masked downstream O(N²)" subsection — the "wall went *up* after a correct optimization" signature means you unblocked a worse downstream cost; bisect + re-profile, don't revert.  Sixth incident row (first algorithmic member); title generalised to "an O(1) handle (or a near-linear standard algorithm) existed but the hot path used a quadratic / allocating shape".  **Verus-fork-predicted**: the 5 665-term closure drops ~22 s → tens of ms; Mode C' wall back below rc.23's 4.6 s and toward the §3.5.J `≤ 1 500 ms` window; rlimit ≥ 5 s timeout resolves.  **Adsmt-side direct wall measurement host-environment-limited** (lu-smt direct invocation does not catch the in-flight `:rlimit` deadline inside the assert-stage hot path; verus-fork wall numbers external-SIGTERM-driven).  rc.25 retry against the verus-fork host is the confirmation path.  rc.25 bump.  Workspace test count: 946/946 (adsmt-theory 80/80); 0 cargo doc / cargo build warnings.  Pending — verus-fork side: rc.25 retry to confirm the closure recovery + the §3.5.J window.  If the wall *still* doesn't drop, the next concentration is a genuinely different shape (the workspace is grep-clean of the container pattern + the congruence O(N²) is gone) — likely `extend_with_equalities`'s `substitute_in` recursion or theory propagation. |
| v1.0.0-rc.26 | 1.0.0-rc.26 (closed 2026-06-07) | **rc.26 cycle = verus-fork rc.25 retry — the throttle-unmask chain reaches the E-matcher tail and TERMINATES (SMT path fully de-quadratified).**  Triggered by `.local-replies-from/verus-fork/2026-06-07-rc25-close-fixed-derive-equalities-next.md`.  verus-fork rc.25 retry **confirmed (e⁗.1)+(e⁗.2)+(T0''') working**: `:rlimit` is now EXACT (rlimit 1 s → 1 011 ms, 3 s → 3 011 ms; vs rc.24's rlimit-independent ~26 s) and `UF::close()` is off the flamegraph.  But rlimit ≥ 5 s still hung — `close()` got fast enough to reach the *next* phase, `UF::derive_equalities`, whose representative dedup was still `out.iter().any(…alpha_eq…)` (92.8 % of alpha_eq-bearing samples).  **The USER landed that fix directly** (`6a3f0cd`/`6dc6f7c`: `HashSet<(Term,Term)>` `norm_pair` dedup + derive-loop `expired` break + `Self::expired` lift), making the ∞ hang finite (~25 s) and taking `UF::*` entirely off the flamegraph — the verus-fork side prototype-validated this before the user committed it.  The throttle-unmask chain then surfaced its **E-matcher tail**, fixed this cycle: **(e⁗⁗.3)** `ematch::extend_match` + `adsmt-engine/src/quant_conflict.rs` Tier-2 matcher binding `prev.alpha_eq(target)` → `*prev == *target` + `ematch::substitute_in` `t.alpha_eq(from)` → `t == from` (all ground universe terms / congruence equalities, Arc::ptr_eq exact; `extend_match` is the production matcher hot path); **(e⁗⁗.4)** `polite::Combination::check` Nelson-Oppen "already-seen equalities" `Vec<(Term,Term)>` + `seen.iter().any(|(a,b)| a.alpha_eq(&eq.0) && b.alpha_eq(&eq.1) || …)` (4.9 % of cycles on the rc.25 flamegraph — same container pattern one layer up from `UF::derive_equalities`) → `HashSet<(Term,Term)>` keyed on `norm_pair` (two terms ordered by pointer-`Hash` so `(a,b) ≡ (b,a)`), mirroring the user's UF dedup; O(|seen|·alpha_eq) → O(1) per probe; **(T0'''')** `TermUniverse::extend_with_equalities_until(equalities, deadline)` per-equality `expired` check (extends the rc.25 T0''' UF deadline cascade into the congruence-ematch phase; `extend_with_equalities` stays a `None` wrapper — the v0.18 M congruence-ematch path is currently test-only in production, so this future-proofs it).  **MILESTONE**: after rc.26 the SMT-solving hot path (CDCL → theory combination → UF → quantifier E-matching) is **fully de-quadratified**.  A workspace-wide grep for production `iter().any(.*alpha_eq` comes back clean — only doc-comments (describing the fixed patterns), tests, and 3 cold **abduction** sites (`adsmt-abduce/src/abducible.rs::matching` pattern lookup + `workflow.rs::is_accepted`/`is_rejected` membership), which fire only when a stuck ground check requests abductive output (off the SMT solving / verus_smoke path) and are deliberately left (cold + public-API / struct-field constraints; `matching` additionally needs real `alpha_eq` since the abducible *pattern* can be non-ground, so `==` would be unsound there).  The throttle-unmask chain that ran rc.21 → rc.26 — one phase deeper each cycle — terminates here; the terminating condition is a clean grep, not a flat wall (the wall relocated at every intermediate step).  Soundness: every `==` swap is on ground hash-cons-canonical terms (rc.24 instrumentation proved `ptr_eq == alpha_eq` on the verus_smoke universe); single-comparison `alpha_eq` sites that may see non-ground patterns (proof-rule preconditions in `rule.rs`, `Lit::matches` in `cnf.rs`, abduction) keep `alpha_eq` — they already hit the rc.22 (e.1) Arc::ptr_eq fast path and need the structural fallback.  rc.26 bump.  Workspace test count: 946/946 (adsmt-quant 43/43, adsmt-theory 80/80); 0 cargo doc / cargo build warnings.  Pending — verus-fork side: rc.26 retry to confirm rlimit ≥ 5 s resolves to a clean budget-bound `unknown` + the full Mode C' 3-run + variance lands in the §3.5.J `≤ 1 500 ms` window (the qualitative ∞ → finite milestone is already hit; rc.26 is the quantitative close).  If §3.5.J finally measures end-to-end, the rc.7 → rc.26 verus-fork-driven performance arc closes and attention can return to the §3.5.H/I vargo-side wiring + the v1.0 stable cut. |
| v1.0.0-rc.27 | 1.0.0-rc.27 (closed 2026-06-07) | **rc.27 cycle = the verus-fork rc.26-retry P0 SOUNDNESS fix — an opaque assert masked a contradiction into `sat`.**  Triggered by `.local-replies-from/verus-fork/2026-06-07-rc26-CRITICAL-soundness-opaque-assert-masks-false.md` (+ `…-minrepro.smt2`).  verus-fork's rc.26 retry **confirmed the performance milestone** (deadline budget-exact at every rlimit: 10 s → 10 028 ms, 30 s → 30 088 ms, 60 s → 60 099 ms; the rc.25 ~25 s natural exit gone, throttle-unmask chain genuinely terminated) — **but performance was never the §3.5.J blocker**.  The blocker is a **critical P0 soundness bug**: an opaque OR-of-AND assert (`(=> P (and Q R))` / `(or X (and Y Z))` — un-decomposable by the v0.3 CNF flattener, and ubiquitous in verus fuel-axiom implications) co-occurring with `(assert false)` returned `sat` (z3: `unsat`).  Root cause: `Solver::check_ground_with_deadline` folds each assertion's CNF into a `clauses` accumulator (`(assert false)` → empty clause = immediate unsat), but the `flatten_to_clauses → None` arm did `return self.check_via_theories(&lits)` — abandoning the *whole accumulator (empty clause included)* and re-routing through the theory path, which skips every compound `and`/`or`/`=>` term (`check_via_theories_with_model` `routable` filter) and never evaluates a bare propositional `false`, so `dpllt::run_once` returned `Sat`.  This had silently masked the trivially-unsat verus_smoke fixture (`(assert (not true))`) across the **entire rc.7 → rc.26 arc** — every verus_smoke check routed through the opaque path because the fuel-axiom OR-of-AND was always present, so the engine never saw the `(not true)` contradiction; the whole de-quadratification performance arc was optimising the path the engine took *because it never saw the false*.  **(S.1)** the opaque `None` arm no longer bails to the theory route; it sets a `had_opaque` flag + skips *only* that un-encodable assertion (keeping the flattenable subset, empty clause included).  The SAT solve then runs on the subset: `Unsat` → return it (sound — subset-unsat ⟹ full-set-unsat); `Sat` → downgrade to `Unknown` (cannot claim satisfiability while ignoring an assertion we couldn't encode).  The dead no-model `check_via_theories` wrapper (whose only caller was the removed bail) is dropped.  **(S.3)** defence-in-depth: an asserted constant `false` (or `(not true)`) reaching `check_via_theories_with_model` short-circuits to `Unsat` before `dpllt::run_once`.  **(S.2)** Tseitin-encode OR-of-AND in `flatten_to_clauses` (so verus preludes flatten cleanly + reach a real `unsat` rather than `Unknown` when the contradiction lives *inside* the opaque structure) is the deeper completeness follow-up — deferred to the next cycle; (S.1) already makes the engine *sound* and yields `unsat` on verus_smoke (whose contradiction is the flattenable `(not true)`, not buried inside the OR-of-AND).  Verified against the verus-fork truth table: `(=> P (and Q R))` + `false` → `unsat` (was `sat`); `(or P (and Q R))` + `false` → `unsat` (was `sat`); `(and Q R)`/`(=> P Q)`/`false`-alone → `unsat`; `(=> P P)` [no false] → `sat` (sat path intact); `(or P (and Q R))` [opaque, no false] → `unknown` (was unsound `sat`).  3 regression tests (`opaque_assert_does_not_mask_false_into_sat`, `opaque_assert_alone_is_unknown_not_sat`, `false_alongside_satisfiable_prefix_is_unsat`).  rc.27 bump.  Soundness lesson → `feedback_soundness_opaque_fallback.md` ("a fallback that drops constraints may return Unsat or Unknown but never Sat; dropping constraints preserves Unsat, destroys Sat").  Workspace test count: 946 → **949** (+3 soundness regressions); 0 cargo doc / cargo build warnings.  Pending — verus-fork side: rc.27 retry to confirm verus_smoke + general obligations finally return a real `unsat` in the §3.5.J `≤ 1 500 ms` window (the rc.26 budget-exact deadline + de-quadratified hot path means it should land well inside once the verdict is correct).  If §3.5.J measures end-to-end, the rc.7 → rc.27 verus-fork-driven arc closes; (S.2) Tseitin is the remaining completeness item. |
| v1.0.0-rc.28 | 1.0.0-rc.28 (closed 2026-06-07) | **rc.28 cycle = the verus-fork rc.27-retry residual (S.1-AOT) — the rc.27 soundness fix reaches the `--aot-load` path.**  Triggered by `.local-replies-from/verus-fork/2026-06-07-rc27-retry-J-FUNCTIONAL-SUCCESS-plus-aot-soundness-gap.md` (+ `…-minrepro.smt2`).  verus-fork's rc.27 retry was the **§3.5.J FUNCTIONAL SUCCESS**: `verus -V adsmt` → `1 verified, 0 errors` in 511 ms (baseline verus_smoke `unsat` 8 ms, rlimit-independent), three orders inside the §3.5.J `≤ 1 500 ms` window — the P-vb finish line, the quantitative close of the rc.7 → rc.27 performance arc.  **Residual**: while checking Mode C' (the AOT-prelude-bank path) verus-fork found the rc.27 (S.1) fix had **not** reached the `--aot-load` (`with_aot_cdcl` / `aot_prelude_clauses` / `prepare_cdcl_seed`) path — a single opaque OR-of-AND baked alongside `(assert false)` made AOT-load **drop the empty clause and return `sat`** (baseline `unsat` vs `--aot-load` `sat` at 1/8/16/19/24 opaque asserts — the rc.26 bug isolated to the AOT path).  Doesn't affect today's functional success (Verus default = baseline; AOT path gated behind the still-pending §3.5.H/I `VERUS_ADSMT_AOT_LUART` wiring) but **must be fixed before §3.5 wires the prelude bank** or every obligation routes AOT and risks false-positive verification.  Two load-side root causes: (1) `Solver::restore_cdcl_state_into` swallowed *genuine* empty clauses via a blanket `if !lits.is_empty()` so the baked `(assert false)` contradiction never reached the seeded CDCL solve; (2) `dump_cdcl_state` discarded opaque asserts at bake time with no record.  **(S.1-AOT) fix**: (1) `restore_cdcl_state_into` keeps genuine empty clauses, distinguished from the defensive out-of-range drop by an explicit `ok` flag (the empty clause IS the contradiction — a blanket emptiness test conflates the two); plus `self.aot_prelude_had_opaque = section.had_opaque;`.  (2) a trailing v1.2 `CdclSection::had_opaque: bool` wire field (writer `adsmt-aot/src/cdcl.rs` appends 1 byte; reader `adsmt-aot/src/reader.rs` `at_end()`-gated → v1.0/v1.1 default `false`; `CdclSection::empty` + the byte-length test updated) carries the bake-time opaque flag from `dump_cdcl_state` (now returns a 3-tuple `(Vec<Clause>, CdclState, bool)`) → CLI `build_cdcl_section` → reader → `restore_cdcl_state_into` → new `Solver::aot_prelude_had_opaque: bool` field → seeds `check_ground_with_deadline`'s `had_opaque` (was `let mut had_opaque = false`, now `= self.aot_prelude_had_opaque`), mirroring the baseline `Sat`→`Unknown` downgrade onto the AOT path.  Divergence table **fully closed** (baseline `unsat` == `--aot-load` `unsat` at every opaque count, verified via the rc.28 CLI); minrepro (`(=> P (and Q R))` + `(not true)` → `unsat`, fixed by the empty-clause-keep), Case B (opaque `(or P (and Q R))` + `(not true)` → `unsat`), Case C (opaque alone → `unknown`, never `sat`, fixed by `had_opaque`) all verified end-to-end.  JIT path (`--jit-trace-load`) inherits the fix automatically (no independent verdict logic).  2 engine regression tests (`restored_empty_clause_is_kept_and_yields_unsat`, `restored_had_opaque_downgrades_sat_to_unknown`) + 1 reader round-trip extension (`had_opaque` survives write→read).  rc.28 bump.  Soundness lesson appended to `feedback_soundness_opaque_fallback.md` ("when a soundness fix lands on one verdict path, grep for every OTHER path that re-implements the accumulate-and-verdict shape — cache/AOT/JIT/restore; a blanket `if !lits.is_empty()` near a clause accumulator is a soundness smell").  Workspace test count: 949 → **951** (+2 AOT-soundness regressions); 0 cargo doc / cargo build warnings.  **CONFIRMED** by verus-fork rc.28 retry (mirror `6491a58`, verus-fork `c1b06735`): all three paths (baseline / AOT / JIT) sound — divergence table closed live (baseline `unsat` == `--aot-load` `unsat` at 1/8/16/19/24 opaque asserts), minrepro bake+`--aot-load` → `unsat`, **full verus_smoke `--aot-load` → `unsat` 13 ms** (was `unknown` at rc.27), JIT-over-AOT → `unsat`, and the **§3.5.I AOT env path** (`VERUS_ADSMT_AOT_LUART` → `--aot-load`) drives `verus -V adsmt` → **`1 verified, 0 errors` 530 ms** — **§3.5.I proven sound end-to-end, DONE**.  Only **§3.5.H** (vargo post-build bake hook — stages `prelude-<sha>-<lu_smt_version>.luart-cdcl` + sets `VERUS_ADSMT_AOT_LUART`; per-query AOT win materialises once it elides the re-asserted prelude from the per-query stdin) remains.  The rc.7 → rc.28 verus-fork-driven arc is essentially closed; remaining = §3.5.H (verus-fork) + (S.2) Tseitin OR-of-AND (adsmt completeness, deferred — sound `Unknown` today) + v1.0 stable cut (after §3.5.H + explicit sign-off). |
| v1.0.0-rc.29 | 1.0.0-rc.29 (closed 2026-06-07) | **rc.29 cycle = verus-fork (S.2) Tseitin OR-of-AND — the last completeness gap before v1.0.**  Triggered by `.local-replies-from/verus-fork/2026-06-07-S2-tseitin-request-and-v1-stable-cut-gate.md`.  After the rc.26→28 soundness arc closed (all three paths sound, `verus -V adsmt` verifies, §3.5.H AOT bake hook done on verus-fork side), the one remaining item before the adsmt 1.0.0 stable cut was *completeness*: `flatten_to_clauses` returned `None` on a nested OR-of-AND (`(or X (and Y Z))` / `(=> X (and Y Z))`), routing it through the opaque `had_opaque` path → `Unknown` where z3 says `unsat` (sound but incomplete; canonical witness `(or (and P (not P)) (and P (not P)))`).  **(S.2)** implements the standard Tseitin transform in `adsmt-engine/src/cnf.rs`: a conjunction appearing where a flat literal list is required is replaced by a fresh aux Boolean `aux` with defining clauses `aux ⟺ subformula` (`encode`/`encode_and`/`encode_or` + `Encoded {True,False,Lit}` const-folding + `Tseitin` ctx struct threading deadline + aux-clause accumulator + `defined: HashSet<Term>` dedup), so the disjunct becomes the literal `aux` and flatten returns `Some`.  Equisatisfiable + linear in term size (no exponential blow-up).  **Aux atoms are content-named** (`!tseitin!<subterm>` via `aux_var_for`) — CRITICAL: a per-call counter (`aux!0`…) would make assertion A's `aux!0` and assertion B's `aux!0` the SAME hash-consed `Term`, aliasing two different sub-formulas under one contradictory definition → unsound; content-naming makes identical sub-formulas share one definition (sound) and distinct ones never collide.  Constants folded (`(and Y true)` → `Y`) so no `true`/`false` lands in an aux clause.  **All three paths inherit completeness automatically** — (S.2) lands in `flatten_to_clauses` itself, which both the baseline `check_ground` and the bake side (`build_cdcl_section`/`dump_cdcl_state`) call; once it returns `Some`, the bake side bakes real clauses (no `had_opaque` for these — `had_opaque` degrades gracefully to deadline/size-guard cases only) and `--aot-load`/`--jit-trace-load` inherit.  **Soundness preserved**: empty clause stays sacred; rc.27 5-line repro + rc.28 divergence table (1/8/16/19/24) stay `unsat`.  **Audited end-to-end via the rc.29 CLI**: witness → `unsat` on baseline + AOT + JIT (was `unknown`); `(or P (and Q R))` alone → `sat` on baseline + AOT (was `unknown`); rc.27 repro `(=> P (and Q R))` + `false` → `unsat`; full divergence table baseline == `--aot-load` == `unsat`.  6 new tests (4 cnf flatten-level: `or_of_and_is_tseitin_encoded_not_none`, `structurally_unsat_or_of_and_witness_flattens`, `tseitin_constant_folds_and_with_true`, `negated_and_of_or_flattens_via_tseitin` + 2 solver verdict-level: `or_of_and_alone_is_sat_via_tseitin`, `or_of_and_buried_contradiction_is_unsat`); the rc.27 `opaque_assert_alone_is_unknown_not_sat` test was RENAMED to `or_of_and_alone_is_sat_via_tseitin` because the `Unknown` it guarded is now the correct definite `Sat` (completeness, not a regression).  rc.29 bump.  Workspace test count: 951 → **956**; 0 cargo doc / cargo build warnings (note: a pre-existing adsmt-core `Type` clippy lint — derived Hash + hand-rolled rc.22 `eq` — is unrelated + out of scope).  **v1.0.0 stable cut** = (S.2) [done] + a full completeness/soundness audit + **explicit user sign-off** per `feedback_stable_signoff_user_approval.md` — NOT the §3.5.J functional-success milestone.  Pending — verus-fork: rc.29 retry to confirm (S.2) completeness holds + decide the v1.0-cut audit scope. |
| v1.0.0-rc.30 | 1.0.0-rc.30 (closed 2026-06-08) | Y4 request — parameterized `declare-datatypes` + full vstd SMT surface (BV / `let` / indexed-id / testers / polymorphic ctors) + canonical reason-unknown + **OxiZ delegation** (`ADSMT_OXIZ_PATH`). `verus -V adsmt` on the Y4 tree → `54 verified, 0 errors` (= Z3). 956 → 970 green. Full detail in the description above. |
| v1.0.0-rc.31 | 1.0.0-rc.31 (closed 2026-06-08) | **WASM emitter package manager + runtime** (full detail in the description above). New `adsmt-emit/` group: contract (WIT + cert wire CBOR/JSON), pm (manifest/lockfile/content-addressed `contents/`-tree store/`.tar.zst` codec[bzip4 slot]/build orchestrator/resolver), runtime (sole backend = **wasmi** under WASI P1, memory64, `emit_many` `-j N`), adsmt-env (env trampoline + build `$srcdir`/`$pkgdir`), cli (install/run/list/pack), adsmt-emit-lean (reference: `emit_lean` → wasm32-wasip1, verified under wasmi from a real CBOR cert). makepkg model, single source-build tier, project-local `.adsmt-emitters/`. Also: parser reorg (`adsmt-parser` → `adsmt-parsers/{smtlib2,lfsc-drat,lu-kb}` + `adsmt-shims/shim-lu-kb`, re-export shims = 0 churn) + `Certificate` serde (adsmt-core `serde` feature, re-intern-on-deserialize, `Theorem` not deserializable). 970 → **1034** green; 0 cargo doc / cargo build warnings. (5) bzip4 codec → v1.1+; (6) CLI polish → deferred. |
| **v1.0.0-rc.30** | **1.0.0-rc.30 (closed 2026-06-08)** | **rc.30 cycle = Y4 request — parameterized `declare-datatypes` + full vstd SMT surface + OxiZ delegation; `verus -V adsmt` verifies the real Y4 obligations (54 verified = Z3).**  Triggered by `.local-requests-from/Y4/2026-06-04-declare-datatypes-parameterized.md` (Y4 = sel4/AMD-V-safety umbrella; AV1 `intercept_floor` proof, Verus→adsmt-cert→Isabelle chain).  Stated blocker was `declare-datatypes` parameterized constructors, but driving the REAL vstd-backed obligation end-to-end revealed the whole surface + a solving-completeness gap.  **(1) `declare-datatypes`**: SMT-LIB 2.6 `par` + legacy Z3 + field-bearing `(Some (value Int))` (`ConstructorDecl`/`DatatypeGroup` in smtlib.rs); CLI `register_datatypes` registers sort (parametric `Kind::first_order`) + constructors + selectors + testers (`is-C`) as typed symbols + parametric `resolve_sort_ctx`; theory datatypes.rs gains selector_map + selector reduction + applied-ctor `constructor_id` (non-nullary disjointness) + parametric `handles_sort`; **polymorphic ctor instantiation** (`ty_unify`/`ty_subst` in `convert_application`); engine `Solver::normalize_selectors` = sound definitional `sel(C(a))→aᵢ` + `is-C(D)→bool` rewrite.  **(2) BV surface**: `(_ BitVec N)` + `#x`/`#b` literals (lexer `#`) + `bv{and,or,xor,add,sub,mul,not,neg}` (the BV term/blast layer already existed — only the surface was unwired).  **(3) `let`** (`Term::subst`) + **indexed-id apps** `((_ partial-order 0) …)` (uninterpreted Bool pred).  **(4) canonical reason-unknown**: EVERY Unknown → `(:reason-unknown "canceled")`/`"(incomplete …")` (verus `air::smt_verify` recognises only those for `Adsmt`; a custom string → `UnexpectedOutput` → driver PANIC).  **(5) OxiZ delegation** (`oxiz_fallback` + session `degraded` flag, main.rs): Path-A+B identity = abductive+ITP ON TOP OF OxiZ (`external/oxiz`, 100%-Z3-parity, MBQI) — on native `Unknown` OR a dropped-constraint `degraded` query (trusting native would be UNSOUND), replay buffered SMT-LIB through the OxiZ binary, take its verdict.  Opt-in via `ADSMT_OXIZ_PATH` (unset → native unchanged → tests unaffected).  **RESULT: `verus -V adsmt --rlimit 30 src/lib.rs` on Y4 → `54 verified, 0 errors` = Z3; AV1 module → `3 verified, 0 errors`.**  14 new tests; 956 → **970** green.  Residual `-V adsmt` driver crash on a *fast* `unknown` (PANIC_ON_DROP_VEC teardown) was FORWARDED (`.local-replies-to/verus-fork/2026-06-08-...`) and is now **CLOSED both sides (2026-06-09)** — verus-fork (`.local-replies-from/verus-fork/2026-06-09-driver-fast-unknown-crash-fixed.md`) traced the real trigger one layer below the `Canceled` arm I'd pointed at: `air/src/smt_verify.rs:579` `smt_get_model`'s `discovered_error.expect()`. The `(:reason-unknown "(incomplete …")` shape routes to `Undetermined(false)`→treated-as-sat→`(get-model)`, and lu-smt's no-model error missed the Z3-canonical `"model is not available"` substring → empty-model parse → `.expect()` panic → `PanicOnDropVec` re-panic → abort. verus-fork fix = `Some(...) else { Invalid }` (solver-agnostic, no soundness guard weakened, fast-`unknown` now reports `0 verified, 1 errors`); adsmt **rc.32.1 cosmetic** (`2a315be`, no bump) = reword the `(get-model)`-no-model error to end in the canonical `"model is not available"` substring so air takes its cheap not-verified shortcut (`smt_verify.rs:552`) instead of parsing an empty model. verus-fork pin → rc.32.1.  KEY LESSON: Verus fully monomorphizes datatypes (0 `par`/field datatypes in real output) — the parametric work matters for the LIVE driver's per-query SMT (testers + field ctors DO appear), not the batch Z3-log; and the real AV1 blocker was never datatypes but the full Poly/fuel quantifier encoding, solved by delegating to OxiZ (adsmt's designed solving backend). |
| v1.0.0-rc.32 | 1.0.0-rc.32 (closed 2026-06-08) | **rc.32 cycle = abductive SLD arc + lu-smt `--emit-cert` flag + Y4 reply.**  Driven by the user pipeline `contrib-port → --emit-cert → abductive-SLD (several commits, no bump) → bump rc.32 → Y4 reply`.  **lu-smt `--emit-cert`** (`7faa073`): `--emit-cert <PATH>` (cert of each `unsat` `(check-sat)`, last wins) / `--emit-cert-dir <DIR>` (one `<DIR>/<seq>.cert.<ext>` per unsat — the verus-fork `ADSMT_CERT_DIR` hook target) / `--emit-cert-format <cbor|json>` (CBOR default — the cert is a fixed-schema `adsmt-cert::Certificate`, no self-describing envelope needed; JSON for inspection).  Writes via `adsmt_emit_contract::encode`, the SAME wire the adsmt-emit emitters decode; emits only on `unsat` (the proof-carrying case); `Theorem` stays non-deserializable (no forged trust token).  Round-trip verified: `lu-smt --emit-cert cert.cbor unsat.smt2` → CBOR → adsmt-contrib `rocq`/`isabelle` native bin (stdin CBOR → stdout proof, exit 0/3) → real Rocq/Coq `Axiom s2 : false.`.  **Abductive SLD (4 commits, `adsmt-abduce`)**: (1) first-order schematic-rule resolution (`8080dbf` — `SchematicHornRule` head **unifies** with the goal, the substitution instantiates the body; `SldEngine::with_schematic_rules`/`with_all`; `resolve_body` cross-product; `apply_subst`); (2) hash-consed cycle guard (`38ef670` — `visiting: HashSet<Term>` keyed on Arc::ptr_eq identity, was `format!`-per-recursion); (3) **higher-order Miller-Lλ pattern unification** (`0227169` — a flexible head `F b₁…bₙ` whose spine head `F` is schematic + args are **distinct vars** unifies with goal `t` via MGU `F ↦ λb₁…bₙ.t`, occurs-checked; `spine_head`/`flex_pattern`/`build_lambda`; `apply_subst` β-reduces the resulting redex; a non-pattern flex head returns false rather than falling to structural descent which would spuriously match a partial sub-spine, e.g. `F(x,x)` as `(F x) x`; full HOU is undecidable, the pattern fragment is decidable with MGUs); (4) candidate dedup (`b4615eb2` — `dedup_candidates` collapses candidates with the same **hypothesis set**, order-independent via hash-cons identity, keeping first occurrence; `minimize`'s subsumption is strict-on-cardinality so equal-length identical sets both survived → they leaked to ranked output + multiplied the `resolve_body` cross-product).  **Warning sweep**: dropped a dead `PoolBuilder` import in `adsmt-aot/src/reader.rs` test mod.  rc.32 bump (`7ca56b4`).  **Y4 reply** (`053c2ff`, `.local-replies-to/Y4/2026-06-08-emit-cert-resolved-plus-wasm-emitter-redesign.md`): answers the 2026-06-08 cert-emit-flag request — Ask #1 (the three flags, CBOR-default rationale) + Ask #2 (the emitters are now a WASM package ecosystem, not system-installed crates; adsmt-contrib `rocq`/`isabelle` ship as both native bins [cargo install `adsmt-emit-{rocq,isabelle}-wasm`] and `.adsmt-emit` wasm packages); rc.32 = `7ca56b4`, **on `testing` + tagged `v1.0.0-rc.32`** (user pushed directly), cert wire stable rc.31↔rc.32.  Workspace test count: 1034 → **1040** (adsmt-abduce 26 → 32: +first-order×2, +HO×2 in rule_base + HO-fact + dedup); 0 cargo doc / cargo build warnings.  Pending — verus-fork: the `-V emit-isabelle`/`-V emit-rocq` dispatch that forwards `--emit-cert-dir` (the matching half).  v1.0.0 stable cut still gated on the formal audit-sweep + explicit user sign-off. |
| v1.0.0-rc.32.1 → rc.33 | 1.0.0-rc.{32.1,32.2,32.3,33} (closed 2026-06-09) | Detail in the `description:` frontmatter. rc.32.1 Arch PKGBUILD hotfix; rc.32.2 P0 theory-atom unsound-`sat` fix + OxiZ simplex pop bug; rc.32.3 `external/oxiz` → `0.2.4-feat/streaming-stdin` base; rc.33 = verus-fork R7.11 cert-emit pipeline gaps A (delegated-unsat cert) + B (flat hash-cons `Term` serde). |
| **v1.0.0-rc.34** | **1.0.0-rc.34 (closed 2026-06-10)** | **§3.5 JIT-on-AOT-prelude replay closed on adsmt's side.** Three commits on `main`, no wire/CLI-surface change. **§3.5.F** (`2b13e08` + `ed69df5`): `cdcl::replay_events` re-fires the recorded `Decide`/`Propagate`/`Backjump`/`Restart`/`Conflict` stream onto a fresh `CdclState` (threads `decision_level` so only a genuine **level-0** conflict ⇒ Unsat; the old scan read any `Conflict`-without-`Restart` as Unsat); the decoded `--jit-trace-load` trace is installed on the solver (`set_loaded_jit_trace`) + consulted at the top of `check_sat_inner` (gated on an active `--aot-load` prelude). **§3.5.E** (`c5cfe84`, the speedup-enabler): `--jit-trace-emit` now stamps a **canonical GF(2) signature** (`canonical_gf2_signature`: atom names sorted→1-based indices in `classes`, literals sorted within each clause, clauses sorted+deduped → byte-identical for the same formula across record/replay regardless of order or inline-vs-`--aot-load` prelude delivery; cheap CNF→polynomial pass, no Gröbner, FF-plugin-independent). The consult trusts a replayed **Unsat** only on an **EXACT signature match** (`classes` AND `basis` equal). KEY DESIGN: a first cut tried `reduce(g, live_basis).is_zero()` for ⟨recorded⟩⊆⟨live⟩ monotonicity — UNSOUND because multivariate reduction against a non-Gröbner basis is not a reliable ideal-membership test (`reduce(x,[1+x,x])`→`1`); a per-query Gröbner basis would cost as much as solving, so exact-match (cheaper + correct) won. Trust model = cache-of-a-prior-sound-solve, same as `--aot-load`; Unsat-only (a replayed Sat has no model → falls through). Speedup fires for exact-formula re-runs = the §3.5.J 5-mode case (same obligation, varying `--rlimit`). Cross-query prelude reuse = the §3.5.C seed-integration follow-up. adsmt-side §3.5.A–G **mechanism** in place — BUT it did NOT fire end-to-end (see rc.34.1; the unit tests masked two integration bugs). CLI-verified emit/decode round-trip; 1057 → **1069** green. See [[jit-aot-replay-section-3-5]], [[verus_fork_integration]]. |
| **v1.0.0-rc.34.1** | **1.0.0-rc.34.1 (closed 2026-06-10)** | **§3.5.J fix — rc.34's replay never actually fired.** verus-fork ran §3.5.J (after landing §3.5.H/I) and the consult never short-circuited; every mode fell through. TWO engine bugs the rc.34 unit tests masked (they hand-built traces with pool *indices* as atoms): **(A)** recorder writes `atom_key_hash_u32(term)` (content HASH) but `replay_events` indexed `aot_pool_terms[atom]` (pool POSITION) → always `diverged`; bank-only pool also omitted per-query atoms. **(B)** CDCL returns Unsat on a ROOT conflict (level-0 / empty-learnt) without calling `on_conflict` (can't 1-UIP a root contradiction) → no terminal `Conflict` event → `root_conflict` false. Fix `deb7e11` (bump `52dad19`): `replay_events(events, atom_map: &HashMap<u32,Term>)` resolves via new `Solver::live_atom_map()` over the FULL live formula (bank pool ∪ prelude clauses ∪ per-query assertions), same hash key, `collision` flag; `atom_key_hash_u32` → `pub(crate)`. Session-boundary fallback appends `Restart`+level-0 `Conflict` to non-empty Unsat traces. `level0_falsifies_prelude_clause` backstop gated to empty-signature (mutually exclusive with exact-match) + collision-free. + regression `real_recorder_trace_replays_through_hash_atom_map` (real recorder→finalize→replay round-trip — the test rc.34 lacked). CLI-verified bake→emit-with-bank→`--aot-load`+`--jit-trace-load`→unsat. 1070 green. **PROCESS LESSON: round-trip replay/JIT tests through the REAL producer — a hand-built trace payload can pass while the real record→emit→load→replay path is fully broken.** PENDING: verus-fork §3.5.J re-run on rc.34.1 (tight-rlimit rows should flip to `unsat`). See [[jit-aot-replay-section-3-5]]. |
| v1.0.0 stable | 1.0.0 (planned after explicit user sign-off) | First stable release. **Pre-cut requirements** from RC2.2: workspace-level repository/homepage/documentation. Then the `breaking_changes_semver("1.0.0")` attribute becomes the live baseline; every future change is gated by the 8-layer safeguard. Subsequent v1.x minor bumps are additive per the three policy docs. |

**Why:** Confirmed 2026-05-16 by user. The codebase has many
comments like `// v0.5 brings Simplex` or `// placeholder for
v0.1` — these are cycle markers, not arbitrary version labels.
When the cycle ships, the version bumps to match (0.13.0 brought
Simplex, 0.15.0 brought the proof bridge, etc.).

**How to apply:**
- When auditing stale "vX.Y" comments, check the table above: if
  the cycle has passed and the feature is implemented, replace
  the marker with a current-state description (or drop it).
- When writing new comments referring to future work, use the
  next applicable cycle name (`v0.19` for current cycle work,
  `v0.21` for post-v1.0-decision work).
- The corresponding logicutils version follows the **restored
  "+2 offset"** rule per `logicutils_version_rule.md` (the v0.17
  audit's match-minor rule was rolled back at the v0.19 cycle
  boundary). adsmt 0.19 ⇔ logicutils 0.21; patch bumps remain
  independent.
