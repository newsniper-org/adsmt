---
name: typed-cic-ir
description: "adsmt-ir — the typed CIC kernel IR (language-agnostic lingua franca for the multi-paradigm substrate); standalone ~/adsmt-ir repo, M1→M2.8+HO core + M3-1 AOT-bank + M3-2 hash-consing + M3-3 conversion-memo (AOT+JIT directive complete) + the SMT-LIB-3.0 face ~/adsmt-ir-smtlib (slice 1 prop/EUF/quant, slice 2 def/let/ite, slice 3a datatypes->inductives, M3-7a nested datatypes via FACE monomorphization kernel-unchanged) landed, Verus-후검증 (50 verified/7 modules). M3-next per user roadmap = lowering(kernel->adsmt-core), DISCUSS ASP face, arith+define-fun-rec, build ASP face"
metadata: 
  node_type: memory
  type: project
  originSessionId: 32a1dc0d-7730-4862-8df4-6958199ce84f
---

**adsmt-ir = the typed CIC kernel IR** — the language-agnostic core lingua franca of the multi-paradigm substrate (see [[multi-paradigm-hybridization]]). Surface langs (SMT-LIB-3.0 face, typed-Datalog/ASP face, unified-superset interior) **elaborate TO** these kernel terms; the solver **lowers them TO** the adsmt-core CDCL(T) working rep. The kernel sits BEFORE recursion/defs/HO are flattened into quantified axioms — the structure plain SMT-LIB discards and the verus MBQI/trigger-hell is made of (= the "strictly better IR" thesis).

**Location: standalone `~/adsmt-ir` git repo** (`git init`, first commit `29565d3`, UNPUSHED — user's to push), NOT an AD1 workspace member. Same separation pattern the user applied to [[unified-gate-verification]]/oxiz-nl2: forward-looking post-v1.0 work kept out of the rc.40 stabilization workspace (pre-stable freeze posture [[pre_stable_feature_freeze]]); absorbable when mature (logicutils-absorption precedent). Zero-dependency trusted core. edition 2024, license = workspace's tri-license.

**M1 LANDED (2026-06-25, user: "typed CIC IR부터") = the dependent λΠ core.** Files: `term.rs` (de Bruijn nameless terms — α-eq is structural ==; impredicative `Prop` + predicative `Type(n)` tower; `shift`/`subst_top` via an `inst` that decrements+lifts; PTS product rule), `env.rs` (global `Env` + the **def/open `Modality`** — the multi-paradigm ownership hook: `Def(body)` δ-unfolds in conversion = closed-world/inductive/ASP ownership; `Open` stays opaque = classical/theory ownership; reduction reads the flag so ownership is STRUCTURAL not a side table), `reduce.rs` (`whnf` β/ζ/head-δ + `is_def_eq` convertibility — after whnf a `def` const can't be at the head so a surviving `Const` is open/unknown, compares by name = the opaque theory reading), `check.rs` (bidirectional `infer`/`check`/`infer_univ` + the **checked admitters `define`/`postulate`** — nothing reaches the Env unchecked = the IR-level instance of the verdict-verification gate), `error.rs` (every `TypeError` ctor is a rejection). **12 conformance tests + 1 doctest green, clippy clean** — incl. 4 REJECTION paths (non-function app, type mismatch, ill-typed def, unbound index): the gate must say no. Prime directive upheld as subject reduction (accepted ⟹ well-typed ⟹ lowering can't manufacture a wrong verdict). NOTE: M1 `def` bodies are non-recursive (admission checks body before insertion → head-δ terminates); recursion = guarded `fix` in M2.

**M2 LANDED (2026-06-25, same session, "바로 이어서 진행해") = inductive types + constructors + dependent recursor** (commit `e974a78`). `inductive.rs` `declare_inductive` admits **parameterized, non-indexed** inductives (Nat/Bool/List/records/verus field-bearing datatypes; indices/GADTs = M2.5). **Strict positivity**: each ctor arg is either recursive (exactly `I params`) or mentions `I` nowhere → else REJECTED (`NonPositive`, conservative=sound). **Dependent recursor as a kernel PRIMITIVE `Term::Elim(ind, motive, minors, major)`** — NOT a generated constant; this is how a dependent eliminator is expressed WITHOUT universe polymorphism: a typing rule (`method_type` builds each minor-premise type, params recovered from the major's type) + ι-reduction in `whnf` (`ind.rec P m… (c_j P b…) ⟶ m_j b… (ind.rec P m… b_recᵢ)…`). **Prop large-elimination guard**: eliminating an impredicative-Prop inductive into Type is REJECTED — exactly the restriction that keeps impredicative Prop consistent (Prop+large-elim = the known inconsistency; singleton/empty exception forgone = completeness loss not soundness). `Modality` gained `Inductive`/`Constructor` (opaque to δ; a ctor app is a WHNF value consumed only by ι). 9 inductive tests (Nat/List/Bool ctors + add/length/ite by recursion + dependent eliminator `P:Nat→Type` + 4 rejection paths) → **22 tests total green, clippy clean**.

**⚠️ CROSS-CUTTING DIRECTIVE (user, 2026-06-25): AOT prelude-bank + algebraic JIT are "매우 중요한" optimizations to apply to the ENTIRE hybridization, NOT just the SMT core.** Captured in [[multi-paradigm-hybridization]] and DESIGN.md §8. For the IR specifically: **AOT** = serialize a type-checked `Env` bank (datatypes/defs/axioms/inductives checked ONCE + precomputed WHNF/normal-form cache + per-decl content digests) → `--aot-load` checks only the query delta (the §3.1 `.luart` bank one level up — TYPED prelude; its lowering feeds the existing propositional AOT bank, so they compose). **Algebraic JIT** = conversion/NbE memoization keyed by an incremental content digest of hash-consed terms (the §3.5 AdHash/K12 machinery over CIC terms instead of clauses) — `is_def_eq`'s repeated reduction of prelude lemmas is the hot path. **Soundness rule (non-negotiable)**: a digest MISS falls through to full re-checking; a HIT is exact-match, never stale trust (rc.34.1 round-trip-through-real-producer lesson). Makes hash-consing a PREREQUISITE not a nicety. Same bake-once+guarded-replay shape carries into the ASP grounder, propagator bus, SMT core.

**M2.5 LANDED (2026-06-25, same session, "M2.5") = indexed inductives (GADTs)** (commit `53336a9`). Inductives gain an **index telescope** (`declare_inductive_indexed`; `declare_inductive` = non-indexed shorthand): params stay UNIFORM across ctors, indices may VARY — `Vec (A:Type0) : Nat → Type0`, `vnil : Vec A 0`, `vcons : Π(n). A → Vec A n → Vec A (S n)`. Type former : `Π params. Π indices. Sort`. Recursive-arg detection relaxed to "head = I applied to the uniform params, then ANY indices" (non-uniform/negative still rejected). The dependent recursor generalizes: motive `Π(indices). I params indices → Sort`, result `motive indices major` (indices recovered from the major's type), each recursive arg's IH taken at ITS OWN indices. **KEY soundness/clarity move**: each ctor's dependent method type is built ONCE at admission as a **template `Π params. Π(motive). MethodType`** (all de Bruijn shifting done a single time in one uniform context — params+motive as binders, never mixed with ambient terms); `method_type` just instantiates by substitution at elim time. ι UNCHANGED (indices never appear in a ctor value, only its type). `infer(Elim)` recovers params AND indices, validates the motive by inferring the sort of `motive indices major` (forces correct arity). 5 indexed tests (Vec ctors, vlen by index-family recursion, truly-dependent result type `P:Π(n).Vec Nat n→Type`, non-indexed regression, wrong-index-arity motive rejected) → **27 tests total green, clippy clean**.

**M2.6 LANDED (2026-06-25, same session, "M2.6") = `Match` + guarded `fix` + mutual induction** (commit `6c62a4a`). **`Match`** (`Term::Match`) = non-recursive case analysis = the recursor WITHOUT IHs (per-ctor "match template" = method template with rec_positions=[]); the case principle a `Fix` destructures with. **Guarded `fix`** (`Term::Fix{rec_arg, ty, body}`) μ-reduces only when applied with its rec_arg in CONSTRUCTOR form (whnf REWRITTEN to collect the application spine). Admission runs `guard.rs`'s **structural-decrease guard**: `f` may be called ONLY as a fully-applied head whose rec_arg-th arg is a VARIABLE that is a strict structural subterm of the recursive argument (exposed by a Match/Elim on it or on an already-smaller var). The guard is **DELIBERATELY CONSERVATIVE** — rejects anything it can't prove decreasing (the only safe direction: too-lenient → non-terminating term → inhabits every type → False-proof → prime-directive violation). 7 recursion tests: plus/length compute + 4 rejection paths (self-call on same arg, call on non-subterm, too few abstractions, non-inductive rec arg). **Mutual** (`declare_mutual` over `MutualMember`): all type formers registered first so ctors cross-reference (Tree/Forest, Even/Odd indexed); positivity over the WHOLE GROUP (self-recursive arg → IH; cross-ref at the arg's head → allowed NO IH; group member elsewhere → rejected incl. negative cross-ref); recursors **independent** (not yet mutual) — sound, mutual recursors a later step. 3 mutual tests. `is_def_eq` gained Match (congruent) + Fix (structural) arms. **37 tests total green, clippy clean.**

**M2.7 LANDED (2026-06-25, same session, ultracode "M2.7+ → 선검증") = mutual recursors (tuple-of-motives eliminator)** (adsmt-ir `f2c301a`, docs `af8617c`; + a P1-1 ι-totality bugfix `2eadf9a` the design-workflow's adversarial agent FOUND in pre-M2.7 code — whnf panicked on a malformed Elim, now stuck). `Term::MutElim(member, motives, minors, major)`: g-tuple of motives (one per group member), one method per ctor of EVERY member; a recursive arg of type I_b gets an IH at **P_b** (cross-member) and ι **dispatches the sub-call to member b's recursor** (`rec_member`). `build_mut_method_template` = the single-motive de Bruijn arithmetic generalized to a g-wide motive block (conclusion at P_{self_b}, IH at P_{rec_member[t]}); `infer_mut_eliminator` enforces R1 shared-param telescope, validates EVERY motive at one elim universe (R3), per-member Prop large-elim guard (P0-3, reject if ANY group member is Prop & elim→Type). Restrictions R1-R5 sound-by-restriction; nested/HO recursion stays rejected. 8 mutual tests (tree-size cross-member-IH compute, Even/Odd indexed Prop, R1/R4/R5 rejections); 43 tests total green. **Built from a 3-agent design workflow** (mutual-recursor spec + 선검증 plan + soundness-landmine analysis) **then ADVERSARIALLY RE-REVIEWED by a 4-skeptic workflow with traced probes against the real kernel → 0 soundness holes** on P0-1 (cross-member motive)/P0-2 (dispatch + subject reduction)/P0-3 (Prop guard)/general.

**후검증 LANDED = the kernel METATHEORY in Verus** — standalone `~/adsmt-ir-verification` (`0d3df4f`, UNPUSHED), **35 verified across 5 modules**, same firewall as [[unified-gate-verification]] (abstract uninterpreted term/typing, prove ONLY the bookkeeping ⟹ runtime re-checks the hypotheses, kernel bug → conservative-reject not false-accept): `subst.rs` (de Bruijn shift/subst bookkeeping + a concrete nat-model for NON-VACUITY, 9), `subject_reduction.rs` (THE keystone — single+multi-step preservation as the congruence-closure of the 5 per-rule facts + conv-respects-typing = the is_def_eq firewall, 9), `guard_wf.rs` (fix termination — admitted calls decrease a nat measure ⟹ no infinite descent ⟹ no False, 7), `eliminator.rs` (ι preserves type = discharges pres_iota + Prop large-elim gate, 5), `positivity_mutual.rs` (positivity ⟹ well-founded + cross-ref-no-IH = M2.6 claim, 5). Together: **subject reduction + strong normalization of the accepted fragment** = the metatheory behind FALSE_UNSAT=0/FALSE_SAT=0 at the IR. Completes the `[선검증→구현→후검증]` discipline for the kernel.

**M2.8 LANDED (2026-06-25, same session, ultracode "M2.8+") = guard `let`-aliasing** (adsmt-ir `4f4d777`, 후검증 `9e609c2`). A **4-perspective design/soundness-assessment workflow** assessed the WHOLE M2.8+ frontier and recommended — under the prime directive "rather ship nothing than a hole" — exactly ONE confidently-sound small change: guard let-aliasing. `guard.rs`'s `Let` arm tracks a ζ-alias: `let y := <strict-subterm var> in … f y …` makes y a recognized strict subterm (ζ fires before μ inspects the rec arg, so `f y` reduces identically to `f j`); only GROWS the recognized-subterm set with an alias of an already-smaller var (never x itself, never a non-variable) → call-acceptance rule unchanged, over-acceptance structurally impossible. 3 tests (let-bound predecessor recursion computes; let-aliasing the rec arg / a ctor-app superterm still rejected); **46 tests total**. 후검증 `guard_wf::zeta_alias_preserves_subterm` (36 verified total). **HO-RECURSION LANDED (2026-06-25, same session, ultracode "HO-recursion") = higher-order / function-typed recursive arguments** (adsmt-ir `2caeb37`, docs `49a7e77`; 후검증 `6269405`). A ctor arg `Π(z:D). I params idx` with D group-member-free (inductive ONLY in the codomain = strict-positive W-type) gets a **functorial IH `Π(z:D'). motive idx' (a_r z)`** and ι **threads the recursor THROUGH the function**: `ind.rec P m… (c … g) ⟶ m… (λz. ind.rec P m… (g z)) …`. The flat `motive a_r` (motive applied to a FUNCTION) is the P1-3 ill-typing/unsound route — functorial Π is the only sound form. `process_constructor` peels q function domains, requires `domains_clean` (a domain occurrence = NEGATIVE position ⇒ reject, e.g. `(Bad→Bad)→Bad`), checks body at uniform params with offset **j+q** (params under q extra binders — soundness-critical). Shared `build_functorial_ih` (motive at +q depth, a_r applied to the q z's, domains shifted cutoff j/q; mutual adds g-block offset); **q=0 collapses EXACTLY to first-order** (46 prior tests = safety net). `reduce.rs`: `constructor_split` recovers params; `rec_subcall` builds `λz. Elim(…, g z)` (whnf does NOT reduce under the λ → inner recursor fires only when later applied, on the smaller g z → **termination = W-type**). 3 HO tests (Inf=node:(Nat→Inf)→Inf, depth recursor computes 0/1/2, negative-domain reject); **49 tests total**. Built from the M2.8 banked spec, **adversarially re-reviewed by a 4-skeptic workflow with traced probes → 0 soundness holes** (functorial IH correct + flat-IH rejected 3 ways + j+q param-smuggle rejected; ι terminates no-reduce-under-λ + `fix self:=λz.node self` infinite-tree generator REJECTED by guard + 30-level tree fast; strict positivity rejects all negative domains; mutual/indexed sound). 후검증 `positivity_mutual::ho_rec_is_positive_and_wellfounded` + `eliminator::functorial_ih_uses_genuine_value` (38 verified).

**M3-1 LANDED (2026-06-25, same session, ultracode "진행해" → M3) = the AOT-bank, as an ADMISSION JOURNAL** (adsmt-ir `123bef1` + totality fix `a98f9f2`; 후검증 `a0b863f`). The cross-cutting AOT directive (DESIGN §8) realized at the IR — the FIRST M3 increment. **A 4-lens design-review workflow found the originally-proposed "serialize the checked `Env` state + trust it behind a 128-bit digest + a re-encode check" design FATALLY UNSOUND**: digest+re-encode prove codec SELF-CONSISTENCY, not TYPE-CORRECTNESS, and the load path would trust the derived recursor bookkeeping (`method_template`/`rec_positions`/`rec_member`) VERBATIM via the raw unchecked `register_inductive` — the **rc.28 S.1-AOT shape one level up** (a self-consistent-but-wrong bank → FALSE_UNSAT). All 4 lenses converged on the same fix. **→ the admission-JOURNAL design**: `Env` records every checked-admitter call's INPUTS in order (`AdmissionStep` = Define/Postulate/Inductive(IndSpec)/Mutual; `env.rs` `journal`/`record`; the checked admitters `define`/`postulate`/`declare_inductive_indexed`/`declare_mutual` append on success, the raw `declare` paths do NOT). `bank_encode` serializes the journal (zero-dep canonical binary codec — every Term/Univ/step variant, distinct tags, fixed LE widths, length-prefixed — + an FNV-1a-128 **corruption** digest over header+payload + a self-check that replaying reproduces the Env → else `IncompleteJournal`). `bank_decode` verifies → decodes (a **bounds-checked, depth-bounded, totality-respecting** decoder: malformed → `BankError`, never panic; `u64>usize` hard-reject, byte-exact UTF-8) → **REPLAYS through the SAME checked admitters**. **Soundness = re-admission**: loading IS re-type-checking, so a loaded bank can NEVER admit what the running kernel wouldn't (it admits it, on load); the bank adds NO trust beyond the (후검증'd) admitters; a corrupt/incompatible/garbage journal fails to decode or re-admit (`BankError`) → caller falls through to elaboration (Unknown-safe). **No `KERNEL_RULES_VERSION` gate is needed for soundness** — re-admission self-validates against whatever rules the running kernel has. Scope: banks the admissible set only; the WHNF/normal-form conversion-memo (the O(query-delta) JIT half) is DEFERRED to the post-hash-consing increment (the §7 prerequisite). **Adversarially re-reviewed (4 lenses, traced probes against the REAL bank): 0 soundness holes** — 14/14 wrong-acceptance attacks (ill-typed def, non-terminating fix, non-positive inductive, duplicate/forward-reference, Prop large-elim, inhabit-Empty) correctly REJECTED on replay; codec/totality/selfcheck lenses all clean (100k+ fuzzed decoder inputs; IncompleteJournal self-check + exactly-once recording verified). It found **ONE totality (NOT soundness) bug, pre-existing in the kernel, surfaced via the forged-input path**: `check.rs` typed a `Fix` via `peel_pis(ty, rec_arg+1)` with attacker-controlled `rec_arg`, and `peel_pis` did an UNCONDITIONAL `Vec::with_capacity(n)` → a forged `Fix{rec_arg=2^34}` requested ~1.37 TB → SIGABRT; `rec_arg=usize::MAX` overflowed → OOB panic. NOT a wrong verdict (the ill-typed Fix is never trusted — replay rejects it), but it broke the "malformed → BankError, never panic" promise. Fixed: `peel_pis` → `Vec::new()` (the loop returns None at the first non-Π, so it only grows to the real Π-count; the only unbounded `with_capacity` in the kernel) + `rec_arg.checked_add(1)` → BadElim on overflow. **63 tests green, clippy clean** (round-trips a rich multi-paradigm prelude through the REAL producer per the rc.34.1 lesson + query-battery identical loaded-vs-fresh + corruption/IncompleteJournal/bit-flip-fuzz + forged-Fix regression). 후검증 **Module 6 `bank_soundness.rs`** (`replay_is_well_formed`/`load_is_well_formed` — replay preserves well-formedness; `rejected_step_falls_through` — over-acceptance structurally impossible; `trusting_load_needs_false_premise` — records WHY journal-replay beats state-trust): **44 verified / 6 modules** (was 38/5). **M2.8+ re-review (standing rule) applied: no deferred item needed** — the bank serializes the admissible set, it does not widen it. **PROCESS LESSON: the "trust serialized state + digest" AOT pattern is UNSOUND at a typed-kernel level — bank the INPUTS and re-derive the outputs (re-admission), don't bank the derived outputs and trust them.**

**M3-2 LANDED (2026-06-25, same session, user chose "Hash-consing → JIT memo" via AskUserQuestion) = the hash-consing port** (adsmt-ir `70f105c`; 후검증-README note `f7fae33`). The §7-prerequisite for the §8 conversion/NbE memo (the JIT half of the AOT+JIT directive). **`Term` reshaped from an enum into a hash-consed handle `Term(Arc<TermData>)`** with payload `enum TermKind` (children are `Term`, matched via `t.kind()`); a **global zero-dependency interner** (`term.rs`, `LazyLock<Mutex<HashMap<u64,Vec<Term>>>>`) dedups structurally-equal terms to one allocation → **`==` = `Arc::ptr_eq`, `Hash` = cached structural hash, both O(1)** regardless of term size. All consumers (reduce/check/inductive/guard/env/bank) migrated to `.kind()` / `TermKind::*` / lowercase interning constructors. **SOUNDNESS (the new surface)**: the interner probes its bucket by **structural** `kind_eq` (tag + leaf data + child `==`), so a 64-bit hash collision CANNOT cause a false dedup (identity is decided by kind_eq, not the hash), and bottom-up interning makes the child `ptr_eq` comparison a faithful structural eq → **structurally DISTINCT terms NEVER share an Arc** (the invariant `is_def_eq`'s O(1) `==` relies on — a false dedup = FALSE_UNSAT) and **equal ALWAYS share one**. `is_def_eq` gains **NO** ptr_eq fast-path (stays whnf-driven structural recursion). First-cut: strong-ref arena (Weak-GC later; not a soundness concern); Mutex poison-recovery. **Behavior preserved EXACTLY** (representation refactor): `whnf` restructured compute-next-then-assign (the `match cur.kind()` borrow precludes in-arm reassign; terminal arms early-return; the App arm binds `whead = whnf(head)` once and reuses it) with β/ζ/δ/ι/μ + spine + μ guard + every stuck form intact. PROCESS: the ~190-site mechanical consumer migration was **delegated to one agent** (precise rules + the 63-test suite as a behavioral oracle, iterate-to-green) while I **reviewed the soundness-critical files** (reduce.rs whnf/iota/is_def_eq, guard.rs structural-decrease) line-by-line myself + added interner-faithfulness regression tests. **66 tests green** (+3 interner faithfulness: structural-equals share an Arc incl. cross-thread + 200/500-deep; distinct never share); **adversarially re-reviewed (3 lenses, probes vs. pre-refactor `a98f9f2`): ALL CLEAN, 0 must_fix** — interner (every TermKind field load-bearing by exhaustive field-sensitivity; no false dedup; collision handled by kind_eq), fidelity (**BYTE-IDENTICAL** whnf/type_of/is_def_eq/guard output vs an a98f9f2 worktree across β/ζ/δ/ι/μ, μ partial-app, functorial+mutual ι, indexed GADT), totality (10k-deep interns stack-safe — kind_hash/kind_eq are O(arity), 60k nodes on a small stack; no re-entrant deadlock; poison-recovery; bank round-trips to interned-identical terms; forged-Fix totality fix holds). **후검증: NO new Verus lemma** — the metatheory (44 verified) abstracts over term identity/typing, so the representation change sits BELOW its firewall; the `==`-faithfulness obligation is discharged by the interner tests + the review (documented DESIGN §7 / README / verification README). **Unblocks the WHNF/NbE conversion-memo (the O(query-delta) JIT half) as the next increment.** M2.8+ re-review: orthogonal to the inductive frontier, deferred maintained. **PROCESS LESSON: a big behavior-preserving trusted-core refactor — delegate the mechanical grind to an agent with precise rules + a strong test oracle, but review the soundness-critical files yourself + run an adversarial DIFF-vs-baseline review (byte-identical output is the gold standard for a representation change).**

**M3-3 LANDED (2026-06-25, same session, "계속 진행해" → the unblocked JIT half) = the conversion memo** (adsmt-ir `3b9f3f5`; 후검증 Module 7 `60af8c8`). The **algebraic-JIT half of the AOT+JIT directive** — so the directive is now **COMPLETE at the IR** (M3-1 AOT bank + M3-3 JIT memo). Type-checking is dominated by repeated reduction (the same prelude lemmas `whnf`'d/convertibility-checked over and over inside `is_def_eq`); the memo records each `whnf(env,t)` and `is_def_eq(env,a,b)` verdict once. **Because terms are hash-consed (M3-2), the Term handle IS the content digest (O(1) identity)** — so the memo is keyed DIRECTLY by the handle, no separate AdHash/K12 encoding (simpler than §3.5's clause digests). Impl: `env.rs` per-Env `Memo` (`RefCell<HashMap<Term,Term>>` whnf + `RefCell<HashMap<(Term,Term),bool>>` defeq, pair canonicalized by `Term::id()` = the interned Arc address); `reduce.rs` `whnf`/`is_def_eq` are memoized wrappers (+ `is_def_eq` short-circuits `a == b` ⟹ true = the hash-cons α-equivalence fast-path). **SOUNDNESS (the §3.5 discipline)**: `whnf`/`is_def_eq` are PURE functions of the env-STATE, so a cache entry is valid only for the state that produced it → the memo is **cleared on every state mutation** (`declare`/`register_inductive`/`register_group` — the ONLY typing-relevant mutators; `record` touches only the journal, correctly no clear) and **reset to empty on clone** (so a `work=env.clone()` admission probe neither inherits nor pollutes the live cache) → every surviving entry is valid for the current state → **a hit equals recompute → the memo is TRANSPARENT** (no verdict changes). A stale hit (wrong whnf/defeq) would be FALSE_UNSAT/FALSE_SAT — proven impossible. **70 tests green** (+4 `tests/memo.rs`: the soundness-critical staleness regressions — `define`(δ) + `register_inductive`(ι) poison-then-mutate-then-assert-fresh, which FAIL if the clear is missing; + the `a==b` short-circuit + reused-subterm transparency). **Adversarially re-reviewed (2 lenses, probes vs. the un-memoized baseline): ALL CLEAN, 0 must_fix** — staleness (14 probes: δ/ι staleness, dependency chains, clone isolation BOTH directions, admitter `work=env.clone()` no-leak, a hot-vs-cold **ULTIMATE differential** across interleaved mutations, cross-env no-bleed via the global interner + per-Env memo, 40-step LCG fuzz, a static audit that every typing-relevant write sits inside a clearing mutator), robustness (a **git-stash DIFFERENTIAL**: all 16 probes pass IDENTICALLY against the un-memoized build = byte-for-byte transparent; no BorrowMutError on deep/mutual recursion — accessors borrow-and-drop in one statement; `a==b` sound; canon symmetric + collision-free; deterministic cold==warm). **후검증 Module 7 `memo_soundness.rs`** (6 verified): `memoized_equals_uncached` (clear-on-mutate maintains the invariant ⟹ the cache is transparent) + `stale_entry_breaks_validity` (clearing is NECESSARY); **50 verified / 7 modules**. M2.8+ re-review: orthogonal (a cache over reduction), deferred maintained. **PROCESS: a verdict-gating cache's soundness = "clear on every state change"; validate it with poison-then-mutate STALENESS regressions (fail-if-no-clear) + a git-stash DIFFERENTIAL (memoized output ≡ un-memoized output, byte-for-byte) — the cache must be provably TRANSPARENT.**

**M3-4 LANDED (2026-06-25, same session, "계속 진행해" → M3-next faces) = the SMT-LIB-3.0 surface FACE, slice 1** (NEW sibling crate `~/adsmt-ir-smtlib`, own git repo `663c3ea`, path-dep on adsmt-ir; adsmt-ir DESIGN note `63989dc`). The FIRST surface face — the cross-cutting AOT+JIT directive being complete, M3-next is "make the IR real" = faces + lowering, and a face comes first (faces feed lowering); SMT-LIB-3.0 first (primary downstream = verus). **A FRONTEND, NOT the trusted core** — per adsmt-ir Cargo.toml's "surface parsers are out-of-kernel follow-ups" + the [[adsmt-ir-verification]]-sibling pattern, it's a separate crate so the kernel stays zero-dep; **everything it elaborates is RE-CHECKED by the kernel admitters (`postulate`/`infer`/`type_of`), so a face bug can only ever yield a `FaceError`, never a trusted ill-typed term — soundness is AUTOMATIC (downstream of the gate)**. Slice 1 (propositional + EUF + quantifier core): a zero-dep S-expr parser (`sexpr.rs`: symbols/`|quoted|`/strings/`;`-comments + a **nesting-depth guard** = a DoS guard) + a typed elaborator (`elab.rs`). **`Bool` ↦ `Prop`**; the connectives are a small **`open` prelude** (`true`/`false : Prop`, `not`, `and`/`or`, `= : Π(A:Type0).A→A→Prop`, `exists : Π(A:Type0).(A→Prop)→Prop`); **`=>` ↦ the kernel arrow** (implication = a function into Prop, impredicatively a Prop); **`forall` ↦ `Π`** (`build_pi`). Commands `declare-sort`(0)/`declare-const`/`declare-fun`/`assert` → an `Elaborated{ env, goals }` = the checked kernel Env (prelude + user decls) + the asserted Props (each `infer`-checked to be a closed Prop). Local quantifier context is **de Bruijn** (outermost binder first); a bound var shadows a global. **Deferred, rejected with a clear message (sound by omission)**: `define-fun`/recursion, datatypes, `Int`/`Real`/`Array`/`BitVec` theories, `ite`/`let`, n-ary `=`/`distinct`. **11 tests green** (4 sexpr-unit incl. depth-guard + 6 elaborate-integration incl. EUF/quantifier scripts + rejection paths + 1 doctest), clippy clean. **Fidelity-reviewed (a focused adversarial reviewer, traced probes vs. hand-built expected kernel terms)**: SOUNDNESS-BYPASS = **none** (every goal is a kernel-checked Prop; 8 bypass attempts cleanly rejected), FIDELITY = **correct** (de Bruijn nesting, shadowing, `exists` right-fold, `=>` right-assoc, variadic `and`/`or` + units, `Eq` sort inference all match the hand-built term via `is_def_eq` — the off-by-one negatives are load-bearing because `open` consts never δ-unfold ⇒ strict structural compare). One ROBUSTNESS (not soundness) finding: **unbounded recursion → stack-overflow abort on pathologically deep input** (the parser/elaborator AND the kernel's recursive `infer`/`shift`/`is_def_eq`, ~3000-deep on an 8 MB stack) → fixed at the face boundary with the parser **nesting-depth guard** (a frontend DoS guard, NOT a soundness hole — can only crash, never wrong-verdict); the residual *huge-flat-n-ary* case (a shallow-parse but deep-fold `(=> a₁…aₙ)`) is documented for a later slice + **flagged for the kernel owner** (the deepest recursive layer is the trusted kernel's own `infer`/`shift`). **M3-5 = face SLICE 2** (`f86daf8`): adds the **`def` side** (definitional + conditional structure — the structure the lowering will preserve) — **`define-fun`** (non-recursive → kernel `define`, a real δ-reducing def; test confirms `id a ≡ a`), **`let`** (→ a β-redex `(λ…body) e…`, SMT-parallel so each `e` is OUTER-scoped + applied as an arg; test confirms `(let ((y (f a))(z (f b)))(= y z))` β-reduces to `(= (f a)(f b))`), **`ite`** (an `open ite : Π(A:Type0).Prop→A→A→A`), **n-ary `=`** (consecutive-Eq and-chain) + **`distinct`** (pairwise not-Eq and-chain); soundness still AUTOMATIC (kernel re-checks `define` + the goals); 12 tests; `define-fun-rec`/arith deferred. **M3-6 = face SLICE 3a** (`198b656`): `declare-datatype`/`declare-datatypes` → kernel **inductives** — single → `declare_inductive_indexed`, multi-type group → `declare_mutual` (non-parametric, arity 0); **constructors become usable kernel ctors** (test: `succ² zero : Nat`; mutual Tree/Forest values typecheck); field sorts = a declared sort / `Bool`→`Prop` / **the datatype name(s) being declared** (so recursive `(succ (pred Nat))` + mutual cross-refs `(fcons (head Tree)(tail Forest))` resolve before the type is in the env); **the kernel's STRICT-POSITIVITY gate re-checks every admission** ⇒ soundness AUTOMATIC (a non-positive datatype is rejected, never trusted); deferred (rejected sound-by-omission): selectors/testers, parametric, **nested-container fields** `(List Foo)` — **the nested case is EXACTLY where the kernel's M2.8+ nested-recursive-container frontier un-defers, but 3a REJECTS nested ⇒ it STAYS deferred** (the standing-rule flag now has a concrete trigger point); 14 tests. **PROCESS: a FACE/frontend's soundness is AUTOMATIC (the kernel re-checks), so the review focus is FIDELITY — elaboration ≡ surface semantics, validated by `is_def_eq`-against-hand-built-expected-terms (with off-by-one negatives that genuinely fail) — plus robustness (the recursion DoS), NOT a deep 후검증.** M2.8+ re-review (standing rule): slice 1 has no datatypes, so nested-containers/mutual-fix not hit (deferred maintained) — **but FLAG: the SMT-LIB datatype-face slice (M3-4 slice ≥2) is exactly where `declare-datatypes` will need M2.8+ nested recursive containers** (SMT datatypes can nest, e.g. a field of `(List OtherDatatype)`), so that frontier un-defers when the datatype slice is built.

**M3-7a LANDED (2026-06-25, same session, "계속 진행해" → NESTED datatypes) = nested datatypes via FACE-level monomorphization (DECLARATION), kernel UNCHANGED** (`~/adsmt-ir-smtlib`, uncommitted at review-time → committed this increment; adsmt-ir kernel untouched). **A 4-lens design workflow unanimously chose approach C (face monomorphization) over kernel container-functor work** — so the M2.8+ nested-recursive-container frontier **STAYS deferred** (the face solved nesting WITHOUT touching the kernel; termination, not soundness, was the real obligation). A constructor field whose sort is a *container application of the datatype being declared* (`Tree.node : (List Tree)`, `List` parametric) is admitted by **specializing** the parametric container into a fresh dedicated mutual member (`(List Tree)` → `List#Tree = nil#List#Tree | cons#List#Tree (head Tree)(tail List#Tree)`), emitting the whole `{Tree, List#Tree}` group through the kernel's existing `declare_mutual`. A parametric datatype (`(par (T …) …)`, arity>0) is **stored as a `ParamDt` template, NOT admitted**; monomorphized on first nested use, driven by a **bounded dedup'd worklist** (`Mono`, canonical `#`-mangled names): **`MONO_MAX_SITE_DEPTH=12`** (argument-shape depth, checked FIRST — before the O(size) mangle — because a non-regular site can DOUBLE in size per level; the **load-bearing guard**, added after the agent's first version HUNG on `Nest A = … (Nest (Pair A A))`) + **`MONO_BUDGET=256`** (count). Non-regular/polymorphic-recursive nesting (`Nest (Pair A A)`) doesn't saturate → **rejected `FaceError::Unsupported`, never looped**. **Soundness AUTOMATIC** (the face thesis): the kernel's strict-positivity + `DuplicateConst` gates re-check EVERY emitted member, so a monomorphization bug can only be rejected, never trusted. **Using** a specialized container ctor in a term (overload res / `(as ctor sort)`) = M3-7b, deferred; the output datatype's own ctors (`node`) are usable now. **ADVERSARIAL REVIEW (mine, per the FACE=fidelity-not-후검증 rule) found + fixed 3 things**: (1) the `mangle` doc claimed "`#` cannot appear in surface symbols" — **FALSE**: the lexer admits `#` in a simple-symbol run AND any char via `|quoted|`, so a user CAN write `List#Tree`. **Not unsound** — a collision hits the kernel's `DuplicateConst` → clean `FaceError::Kernel`; doc corrected + **pinned with `slice3a_mangle_collision_is_clean_kernel_rejection`** (`|List#Tree|` pre-decl colliding with the generated specialization → Kernel rejection, no panic/no silent sat). mangle is injective on standard `#`-free input (each head's param-vs-sort role is fixed per program). (2) the kernel's `NonPositive` gate is **UNREACHABLE from this surface** — datatype field sorts elaborate ONLY to atoms or *covariant* container apps; there is no surface form for an arrow/function field sort (`elab_sort` rejects `Sexpr::List`), so no negative occurrence is expressible ⇒ the surface is positivity-conservative by construction (gate = defense-in-depth); documented + `slice3a_function_typed_field_is_rejected_not_negative`. (3) **round-trip through the REAL producer** ([[feedback_roundtrip_through_real_producer]]): added `slice3a_monomorphized_group_survives_bank_roundtrip` — a monomorphized group records into the journal via `declare_mutual` like any mutual group, so it survives AOT-bank `bank_encode→bank_decode→`replay and a value over the specialized type type-checks in the freshly-replayed env (the M3-1 admission-journal end-to-end). **21 tests green** (4 sexpr-unit + 16 elaborate-integration incl. nested/rose/double-nesting-saturates/non-regular-rejected + the 3 adversarial + 1 doctest), clippy clean. The clone-hygiene rule [[feedback_avoid_nested_vec_clones]] held (the elaborator builds `CtorSpecs` once and MOVES it into `declare_*`). **PROCESS: nested datatypes were solved ENTIRELY in the frontend (monomorphize → emit through the unchanged kernel), so the hard part was TERMINATION (the depth guard before the count budget) not soundness — and the review's value was catching a false doc claim + confirming the two soundness backstops (DuplicateConst collision, NonPositive-unreachable) actually fire/hold, plus the bank round-trip.**

**M3-8 LOWERING — DESIGN PHASE DONE (2026-06-25, same session) = the concrete kernel CIC → adsmt-core HOL lowering design** (adsmt-ir `f7239e3`, DESIGN.md §5.1; implementation = next increment in a NEW sibling crate `adsmt-ir-lower`, path-dep on adsmt-ir + the frozen adsmt-core/engine = a downstream consumer, rc.40 workspace untouched). The pipeline-closing headline. **Source** = `TermKind` (de Bruijn, dependent, def/open). **Target** (mapped via an Explore agent): `adsmt-core::Term` = simply-typed HOL `Var|Const|App|Lam` (NAMED vars, hash-consed by `(name,ty)`, type-checked at construction) over a stratified `Type`+`Kind` (HKT, NO dependent types); quantifiers = HOL consts `mk_forall`/`mk_exists`; NO de Bruijn, NO `def`/`let` layer; **native datatypes live in the adsmt-engine `Datatypes` THEORY** (`DatatypeDecl`→`declare_datatype`) + `Solver::assert(Term:Bool)`→`check_sat`. **PRIME DIRECTIVE at lowering = abstain, never mislead**: lowering is a PARTIAL function; any construct with no faithful simply-typed-HOL image → `Unlowerable`; **whole-query all-or-nothing** (any unlowerable subterm → the WHOLE query reports Unknown, never a strict subset) = the IR instance of [[feedback_soundness_opaque_fallback]] (dropping a constraint preserves Unsat, destroys Sat). Per-construct map: `Prop`→`Bool`, Π-into-Prop→`mk_forall` (the anti-trigger-hell win), implication, `open`→uninterpreted `Const`/theory symbol, `def`→inline-everywhere XOR `Const`+axiom, `Match`→ite/tester/selector, `Inductive`→`DatatypeDecl`; `Elim`/`MutElim`/`Fix`/indexed-GADTs/genuinely-dependent-Π → abstain. **A single-agent ADVERSARIAL DESIGN REVIEW (run BEFORE any code) found FOUR P0 verdict-manufacturing holes — none triggering the abstain rule because lowering "succeeds" structurally — now folded in as explicit guards**: (P0-1) **`Match`→ite/tester chain is UNSOUND**: the engine's `Datatypes` theory has disjointness+injectivity+selector-reduction but **NO exhaustiveness, NO acyclicity** (testers stay uninterpreted on a variable), so the unguarded chain returns the LAST branch on a junk model (FALSE_SAT) / diverges from the kernel's *stuck* `Match` (FALSE_UNSAT vs injectivity) → restrict to finite/enum majors + emit the coverage disjunction `⋁ is-Cᵢ`; inductive-ω `Match`/recursors **abstain until an initial-algebra datatype theory exists**. (P0-2) **`def`/`Fix` recursion-axiom over-constrains**: "kernel proved termination licenses the total `∀x. f x=body` equation" was WRONG — a total HOL equation needs the initial algebra; gate on a per-axiom fully-faithful+first-order flag; recursive form deferred to the initial-algebra theory NOT merely a `define-fun-rec` face; all-or-nothing per-constant (inline-everywhere XOR axiom-everywhere). (P0-3) **`Bound`→named must use GLOBALLY-fresh names** (monotonic counter), NOT in-scope-unique — the target hash-conses `(name,ty)`, so distinct binders sharing a name+sort alias onto ONE `Var` → `mk_forall` captures across them (the rc.29 hash-cons-aliasing lesson mirrored: distinct binders need GUARANTEED-distinct names). (P0-4) **prelude `=`/`exists`/`ite` polymorphic type-arg**: dispatch must be identity-checked (not string-keyed — a user `and`/shadowing bound var must not mis-route) + arity-exact (partial app abstains) + DROP `args[0]` (the `Type0` arg) for `=`/`ite`, CONSUME-as-binder-sort for `exists`, and abstain on a non-first-order sort arg (function/`Prop`-valued = extensional eq the solver can't do). Plus P1 (proof-as-data binder abstain; quantify-over-`Sort` abstain; `Pi` classification via whnf/δ-REDUCED occurs-check, undecidable⇒abstain; `App` partial/shadowed/HO-bound-var-head abstains) + P2 (per-slice **z3-differential as a LANDING gate** through the REAL face→kernel→lower→solver pipeline over a randomized corpus per [[feedback_z3_differential_for_unsat_trust]]+[[feedback_roundtrip_through_real_producer]]; **immutable-`Env`-during-lowering** invariant so the conversion memo can't go stale). **PROCESS: the design-review-BEFORE-implementation phase earned its keep — 4 FALSE-verdict holes caught on paper, all of the form "lowering succeeds structurally but the image means something else" (so the abstain keystone never fires) → the fix is an enumerated "structural success ≠ faithfulness" abstain checklist + never enabling a path whose soundness depends on datatype-theory closure (initial algebra) the engine doesn't provide.** Increment plan M3-8a (FO/EUF/quant core, pairs face slices 1-2) → M3-8b (datatypes, finite/enum `Match` only) → M3-8c (defs); 후검증 = meaning-preservation + the differential gate.

**M3-8a LANDED (2026-06-25, same session, "진행해") = the lowering CORE — kernel CIC → adsmt-core HOL, first-order/EUF/quantifier slice** (NEW standalone repo `~/adsmt-ir-lower`, `git init` first commit `5363a26`, UNPUSHED; adsmt-ir DESIGN §5.1 marked ✅ `7e2df3f`). Path-dep on `adsmt-ir` + the frozen `adsmt-core`/`adsmt-engine` (a downstream CONSUMER — the external path-dep into the AD1 workspace members resolves fine despite their `version.workspace=true` inheritance; rc.40 workspace untouched). **Lowered faithfully**: `Prop`→`Bool`; `true`/`false`/`not`/`and`/`or` (adsmt-core `mk_*`); `=>` (the kernel ARROW=`Pi` → `mk_imp`, NOT a Const); n-ary `=` (face already and-chains it → binary; the polymorphic `Type0` arg DROPPED → `mk_eq`); `Π`-into-`Prop`→`mk_forall`; `exists`(`= Const "exists"` applied to a `λ`, the type arg consumed as binder sort)→`mk_exists`; declared sorts/funcs/consts→uninterpreted target symbols (EUF, `Term::const_`); non-recursive `def` δ-INLINED (whnf unfolds it). **Impl technique**: `lower_term` calls `whnf(env,t)` FIRST (auto-handles δ-unfold/β-redex/ζ-let uniformly → the head is exposed), then matches `TermKind`; a parallel `Vec<Frame{ir_sort, cvar}>` carries BOTH the kernel `Ctx` (for `infer`-based classification) and the target name env; `db_occurs(t,idx)` (a small de-Bruijn occurs walker — the kernel's `occurs` is by-NAME not by-index) splits a non-dependent function type (case 3) from a dependent one (case 4). **All four P0 design-review guards WIRED + TESTED**: (P0-3) binder names **globally fresh** (`x!{counter}`, monotonic across the whole query — the target hash-conses `Var` by `(name,ty)` so distinct binders MUST get distinct names else `mk_forall` captures); (P0-4) prelude dispatch is **arity-exact + identity-checked** (`Env::lookup`==`Open`, not bare-string) and DROPS `=`/`ite`'s leading type arg / CONSUMES `exists`'s as the binder sort, abstaining on a function-sorted op; (P0-1/P0-2) datatypes (`Match`/`Elim`/`Constructor`/`Inductive`), `Fix`, `ite`, dependent `Pi`, proof-as-data, higher-order application all **abstain**. **WHOLE-QUERY all-or-nothing**: `lower(env,&goals)` returns `Err(Unlowerable)` if ANY subterm of ANY goal is unlowerable (the `?` propagates atomically) → the caller reports Unknown, never a partial assertion set ([[feedback_soundness_opaque_fallback]]). **Self-review found a deliberate COMPLETENESS gap (documented, sound)**: a `∀` over `Bool` abstains — the face's `Bool`↦`Prop` collapse makes `∀(h:Bool).φ` and second-order `∀(P:Prop).φ` the SAME kernel term `Pi(Sort(Prop),·)`, indistinguishable, so abstaining on both is the safe choice (classically they'd even be lowerable, but conservative-first). **15 tests, all through the REAL face producer** ([[feedback_roundtrip_through_real_producer]]): `alpha_eq` FIDELITY vs hand-built adsmt-core terms (propositional/forall/exists/EUF-app/def-inline — alpha_eq abstracts the fresh binder name, every FREE symbol must match = the gold standard for a translation pass), capture-safety (nested same-name shadowing → distinct vars), whole-query abstain (datatype/ite/mixed-goal), + **4 END-TO-END verdicts through the real `adsmt-engine::Solver`** (EUF-congruence unsat, propositional unsat/sat, EUF sat = the §5.1 P2 per-slice LANDING GATE proving the lowered terms are solver-consumable AND verdict-correct, not just structurally pretty). clippy clean. **PROCESS: the design-review-first paid off again — the implementation was a straight encoding of the reviewed guards + the fidelity tests (exact `alpha_eq`) are stronger than a verdict differential for the covered cases; the end-to-end Solver gate adds the consume-side round-trip.** NEXT: M3-8b (datatypes — `Inductive`→`DatatypeDecl`, `Match`→finite/enum-only ite/tester chain per the P0-1 closure restriction) + M3-8c (defs as `Const`+axiom); the full randomized z3-differential (render lowered→SMT-LIB→z3) is M3-8's closing 후검증.

**M3-8b + Bool-ite LANDED (2026-06-25, same session, "진행해") = datatype lowering + the slice-2 ite gap closed** (adsmt-ir-lower `01723ba` datatypes, `461cd67` ite; DESIGN §5.1 `f25d1b7`). **M3-8b**: `lower_datatypes` walks `env.journal()` — every `AdmissionStep::Inductive(spec)`/`Mutual(specs)` → one engine `DatatypeDecl` (`adsmt_theory::datatypes::DatatypeDecl`, dep added) in `Lowered::datatypes` (the driver registers via `Solver::declare_datatype` BEFORE asserting goals); `spec_to_decl` reads `IndSpec.{name,ctors→constructors,arities,sort}`, synthesizes selector names (`{ctor}!sel{i}` — the face drops them + no `Match`/selector term references them), `is_finite` = all-nullary (CONSERVATIVE — a finite-but-arg-bearing datatype reports infinite = sound, engine just forgoes finite-domain reasoning), `params:vec![]`. **Abstains** an indexed (GADT) / parametric / non-`Type(0)` inductive or a non-first-order ctor field (whole-query). Constructor USE in terms: `Modality::Constructor` merged with `Open` in `lower_const`/`lower_app` (a ctor lowers to a target `Const` of its lowered type, applied structurally; the engine's `Datatypes` theory recognizes it BY NAME); `lower_sort`'s Inductive arm → `CType::const_(name, Type)`. **SOUNDNESS**: declaring datatypes + emitting ctor terms is FAITHFUL — disjointness/injectivity are valid initial-algebra rules (no FALSE_UNSAT); the engine's MISSING acyclicity/exhaustiveness only WEAKENS it (completeness gap, safe — could miss an unsat that needs acyclicity, never invents one). **`Match`/`Elim`/selectors NOT lowered — the face produces none yet (M3-7b deferred)**; when it does, the P0-1 finite/enum-only restriction applies. **Bool-ite** (`461cd67`): the solver has NO native `ite` term, but a Bool-branch ite is classically `(c→a)∧(¬c→b)` (faithful) — `try_prelude` "ite" arm lowers it when `lower_sort(args[0])==Bool`, else abstains (non-Bool ite needs fresh-var flattening = later). **20 tests** (was 15): + datatype-decl fidelity (enum Color + recursive Nat: constructors/arities/is_finite) + Bool-ite `alpha_eq` fidelity + 4 end-to-end Solver verdicts (enum disjointness unsat, enum distinct sat, recursive-datatype `succ zero=zero` unsat, Bool-ite unsat); clippy clean. **M3-8 lowering now covers the ENTIRE face-expressible surface** (prop/EUF/quant/datatypes/Bool-ite); remaining = non-Bool-ite flattening + def-as-axiom optimization (inlining already sound+complete for non-rec defs) + the randomized z3-differential closing 후검증. **ROADMAP NEXT = the typed-Datalog/ASP face DISCUSSION** (user wants to discuss feature scope BEFORE building — the def/closed-world/stable-model side).

**STILL-DEFERRED (re-review per the standing rule below)**: (1) **nested recursive containers in the KERNEL** (`Rose : List (Tree A) → Tree A` with the container's functorial map/All) — **M3-7a un-deferred this at the FACE via monomorphization (kernel untouched)**, so the KERNEL frontier stays deferred unless a face needs *generic* (non-monomorphizable, e.g. truly polymorphic-in-a-stored-type) containers. (2) **mutual `fix`** — cross-call form needs a lexicographic/shared measure (single per-function measure unsound); a `define-funs-rec` face slice would need it. (3) **heterogeneous-universe mutual elim (relax R3)** — CANNOT be made sound without the omitted cumulativity/product reasoning; R3 kept verbatim.

**⚠️ STANDING PROCESS RULE (user, 2026-06-25): from M3 onward, RE-REVIEW at EVERY task whether the M2.8+ deferred items should remain deferred** ("M2.8+ deferred들은 M3 시작할 때부터 deferred로 유지할지 여부를 매 작업마다 검토하도록"). Don't treat the deferral as permanent — each task, check if a now-deferred frontier (mutual fix, heterogeneous-universe elim/relax-R3, nested containers) has become tractable/needed (e.g. M3 lowering or a surface face may require one), and if so un-defer it via the design→implement→adversarial-review→후검증 pipeline. The decision to defer is re-evaluated continuously, not set once.

**Roadmap (DESIGN.md):** M2.8+ STILL DEFERRED (re-review per the rule above) = mutual fix, heterogeneous-universe elim/relax-R3, nested containers (need the container's functorial map). M3 = **M3-1 AOT-bank DONE** + **M3-2 hash-consing DONE** + **M3-3 conversion-memo DONE** (all above) → **the cross-cutting AOT+JIT directive is now COMPLETE at the IR** (AOT bank ⊕ JIT conversion-memo). **M3-4 face slice 1 + M3-5 slice 2 (define-fun/let/ite/n-ary) + M3-6 slice 3a (datatypes → inductives, single+mutual; nested REJECTED) + M3-7a nested datatypes via FACE monomorphization (DECLARATION; kernel UNCHANGED, M2.8+ kernel-container frontier stays deferred) DONE** (above) → **M3-next, per the user's ORDERED roadmap** ("[NESTED datatypes] → lowering → [ASP face 기능 추가 관련 논의] → [arith theories] + `define-fun-rec` + [논의 결과 반영 ASP face 진행]"): (1) **lowering** (kernel→adsmt-core, the pipeline-closing headline — `def`→stable-model gate, `open`→theory/G-SAT gate, ∀ Π-into-Prop with definitional structure intact = the anti-trigger-hell win); (2) **DISCUSS the typed-Datalog/ASP face design WITH the user BEFORE building** (the def/closed-world/stable-model side); (3) **arith theories** + **`define-fun-rec`** (guarded fix); (4) **build the ASP face** reflecting the discussion. Also deferred within the datatype face: **M3-7b** = USE of a specialized container ctor (overload res / `(as ctor sort)`), datatype **selectors/testers**, standalone parametric datatypes (`def`→stable-model gate, `open`→theory/G-SAT gate, ∀ Π-into-Prop lowered with definitional structure intact = the anti-trigger-hell win). **Sound-by-omission frontier** (each only makes the checker reject MORE, never accept wrongly — right trade under prime directive): cumulativity (`Type(i)≤Type(j)`), η-conversion, universe polymorphism (levels are concrete u32). [hash-consing LANDED M3-2 — `Term` is now Arc-interned, `==`=ptr_eq O(1).] Relationship to `adsmt-core::Term`: that's the SOLVER's simply-typed HOL+HKT rep (hash-consed, CDCL hot path); the CIC IR is richer+upstream (dependent, inductives, def/open), the elaboration TARGET lowered TO the core form — not competitors.
