---
name: oxiz-audit-findings-2026-06-20
description: "Multi-agent incompleteness/soundness audit of external/oxiz (2026-06-20) — CCFV is SOUND; found pre-existing soundness holes (div/mod FIXED, SAT-core binary_graph + nlsat Sat-gate + others OPEN)"
metadata: 
  node_type: memory
  type: project
  originSessionId: 32a1dc0d-7730-4862-8df4-6958199ce84f
---

**2026-06-20 — exhaustive `external/oxiz` incompleteness/soundness audit** (13-agent adversarial Workflow `wf_6eaf5046-746`, run after the CCFV rc.39.3 work; output `/tmp/.../tasks/w618v2738.output`). 67 findings (4 soundness / 2 high / 9 medium / 18 low / 34 info). **CCFV itself (the audit's emphasis) is SOUND** — see [[ccfv_unified_instantiation_design]]. The soundness holes found are **PRE-EXISTING and unrelated to CCFV**.

**CCFV verdict = SOUND (adversarial agent + my own pass).** Firewall intact (every CDQI/ematch/enumerate binding → `emit`→`instantiate` ground.contains gate; emit the sole `Lemma` producer; double firewall with EUF interning only asserted ground terms). B/E self-match protection STRUCTURAL (`ground.rs add_rec` never descends into `Quant`). P4 `default_value`/`TotalView`/`Mode` = dead scaffolding, ZERO live consumer (verdict-flip deferred). `emit_existential_disjunction` range = `mk_int` literals (ground). No reachable panic/unwrap in the CCFV hot path. TWO fragile-but-currently-unreachable invariants flagged (LOW): (a) the host `substitute_cached` drops BV/string/let kinds w/o recursing (`query.rs:544`) so a bound var could ride inside an instance body — SAFE only because those same kinds map to `view`=Opaque (`clean_mbqi.rs:584`) which `add_rec` registers as a non-descended LEAF; the coupling (view-Opaque set ≡ substitute-drop set) is unasserted, and a future TermKind added to view-App but left in substitute's `_=>id` would reopen the B/E capture class. (b) `mod.rs:682-701 retains_bound` drops only the lemma's OWN quantifier's bound names → a sibling quantifier's bound var is indistinguishable from a declared constant (latent, gated by FO/UF/LIA/Array-only wiring). Both worth a defensive ground-Var guard; neither live today.

**CONFIRMED live soundness holes (pre-existing OxiZ, NOT CCFV):**
1. **div/mod parsed as subtraction → FULLY FIXED (OxiZ `8de523a` + `8683c44`).** `smtlib/parser/terms.rs` parsed `(div a b)`/`(mod a b)` as `mk_sub` ("placeholder"), confidently-wrong arithmetic on the LIVE `execute_script` path → `(= (div 6 3) 2)` solved as `(= 3 2)` → spurious `unsat` = FALSE PROOF for the adsmt delegation backend. **Two parts:** `8de523a` routes them to `mk_div`/`mk_mod` (`TermKind::Div/Mod`) → kills the spurious-UNSAT (false-proof) direction. BUT the theory layer does NOT decide div/mod (uninterpreted → over-approximation) → a SPURIOUS SAT remained (`(not (= (mod 7 3) 1))` → `sat`, z3 `unsat`) — **the EXACT "incomplete reasoning concludes Sat" failure the CCFV P4 verdict-flip was deferred to avoid** (user caught the inconsistency). `8683c44` closes it the same way (the `*_is_trustworthy` discipline): `Context::check_sat` downgrades a `Sat` → sound `Unknown` when an asserted term contains a `div`/`mod` subterm (`term_contains_div_mod`; `subterms` made pub(crate)); `Unsat`/div-mod-free reasoning untouched. NOW every div/mod verdict is sound (Unknown — never spurious sat, never spurious unsat). Constant/linear div/mod DECISION via the Euclidean axioms (`b≠0→a=b·div+mod`, `0≤mod<|b|`; for literal b → linear → simplex-decidable) = completeness follow-up. Regressions `div_mod_are_not_parsed_as_subtraction` + `undecided_div_mod_never_reports_a_spurious_sat` (lia_integration.rs). PROCESS: P4-caution and this are the SAME principle — never conclude Sat from incomplete reasoning; nlsat #288 is the third instance of this exact class.
2. **SAT-core `pop()` + `forget_learned_since` don't scrub `binary_graph` → FIXED (OxiZ `11db78d`).** `oxiz-sat/src/solver/mod.rs:1682-1693` `pop` removes clauses (frees ids) but leaves stale watchers AND binary_graph entries ("cleaned up naturally" — the EXACT assumption the bv_mul `forget_learned_since` fix already repudiated for watches); the next `add_clause` recycles the id → stale watcher/binary-implication loads a NEW clause → spurious propagation. `incremental.rs:64-68 forget_learned_since` scrubs `watches` but NOT `binary_graph`, and `propagate` (propagate.rs:19-35) consumes binary_graph DIRECTLY with no deleted-guard. Verifier REPRODUCED a concrete spurious UNSAT. LIVE on the incremental SMT push/pop path. FIX = add `BinaryImplicationGraph::remove(implied_lit, clause_id)` (only has add/get/clear/resize) + call it in BOTH `forget_learned_since` and `pop`, + scrub watches in `pop`. Needs theory_callback_fuzz/ground_soundness_fuzz (`--ignored` user `!` gate) validation.
3. **nlsat NIA/NRA Sat path UNGUARDED → FIXED (OxiZ `9125062`).** `oxiz-theories/src/nlsat.rs:370/546` return `Sat` unconditionally while only `Unsat` is gated by `unsat_is_trustworthy`. `extract_(real_)poly_atoms` silently drops top-level Or/Not/Implies/Ite/Xor (`_=>{}`) + Div/Mod/Apply/Ite operands → a model of the retained SUBSET violates a dropped constraint → spurious SAT (consumed as final verdict, mod.rs:472 bypasses CDCL(T)). FIX = a `sat_is_trustworthy` / all-assertions-consumed gate mirroring the Unsat side (→ Unknown when atoms were dropped). REFUTED sibling: `oxiz-nlsat/src/solver/decide.rs:148` value-picker under-approx is backstopped by `theory_propagate` re-eval — sound.

**MEDIUM soundness-relevant (sound-but-incomplete / mostly off the verus-delegation path):** simplex `make_feasible()` returns `Ok`=feasible on `max_pivots` exhaustion (`arithmetic/simplex.rs:578` → possible spurious SAT in LRA/LIA); `ArithSolver` LIA has NO branch-and-bound/Gomory/integer-model gate (`solver.rs:621` → LIA-over-rationals spurious SAT on non-integer-feasible); parser missing `/`,abs,to_real,to_int,is_int,divisible (→ uninterpreted, sound over-approx). **LOW soundness-relevant:** `datatype/solver.rs:552 assert_false` no-op (drops negated recognizer → spurious sat for datatypes); `combination.rs:401 extract_arrangement_from_arith` asserts FABRICATED disequalities between shared vars ("conservatively assume different" → potential spurious UNSAT). **Non-soundness incomplete (CLI/unused):** model_counter fabricates 0, proof_checker stamps valid w/o empty-clause, maxsmt/assumption-minimizer/recursive-minimizer placeholders — all peripheral, not on the adsmt delegation path.

**FIXED 2026-06-20 (all corpus byte-identical 166/168, 0 spurious; scoped suites green). OxiZ lineage on `0.2.4-redesign`, all UNPUSHED:** …`f411273`(P4)→`8de523a`+`8683c44`(div/mod #1)→`9125062`(nlsat Sat-gate #3)→`11db78d`(SAT-core binary_graph #2 — `theory_callback_fuzz`/`ground_soundness_fuzz` `--ignored` = standing user `!` gate before cut)→`2071119`(theories trio: simplex max_pivots→Unknown + polite-combine→Nelson-Oppen [drop fabricated diseqs] + datatype assert_false→Unknown)→`12b557e`(off-verdict-path honesty: opt optimize_maxsmt Optimal-on-Unknown→Satisfiable, cli --verify-proof→verify_complete + reject unknown rules, cli model_counter→NotImplemented error, smtcomp ModelVerifier→honest not-verified, sat assumptions minimizer→sound over-approx core, solver Model::eval Neg→mk_neg)→`298254e`(parser real `/`→Div+downgrade)→`9d0663f`(CCFV view-Opaque≡substitute-drop coupling invariant doc). **Total this sweep: 7 commits, ~13 findings fixed.** **STILL DEFERRED (tracked tasks #289-291, each a separate validated cycle):** (#289) LIA integer-feasibility gate — rational simplex says feasible w/o integer check → QF_LIA spurious sat; LRA is the live path (off verus), the sound gate risks the 2 lia Sat tests, needs branch-and-bound or careful validation; (#290) CCFV E/F structural fix = make `substitute_cached` TOTAL (≈20 kinds) → removes the bound-var-retention leak (currently UNREACHABLE, invariant documented); (#291) parser abs/to_real/to_int/is_int/divisible + extend the check_sat undecided-op Sat→Unknown downgrade (abs-via-ite still spurious-sat on undecided ground ite/arith). **OUT OF SCOPE (NOT bugs — unwired whole-algorithm features, no solver consumer, return None=harmless):** qe/* placeholders (cooper, ferrante_rackoff, array QE, datatype QE, bv bit-blast QE); + cosmetic/sound/dead: core printer/model.rs + model/completion.rs (unwired), opt smtlib_commands get_objectives (dead surface), model_builder unsat-core (sound over-approx), recursive_minimization (dead), preprocessing/advanced placeholders (optimizations only). Plus the div/mod COMPLETENESS follow-up (Euclidean axioms → decide constant/linear instead of Unknown).
