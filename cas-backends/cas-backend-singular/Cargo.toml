[package]
name = "cas-backend-singular"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Singular CAS subprocess backend for adsmt-cas — UNTRUSTED oracle (ideal-membership cofactors via `lift`, factorization). Every witness it returns is re-checked by adsmt-cas::admit; a bug here only ever yields Unknown, never a wrong verdict."

[dependencies]
adsmt-cas.workspace = true
num-bigint.workspace = true
num-rational.workspace = true
num-traits.workspace = true
